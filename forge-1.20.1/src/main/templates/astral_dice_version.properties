# 版本互通门槛(Version Gate)用的工程版本号。
# 由 build.gradle 的 generateModMetadata(ProcessResources + expand)在构建期从 gradle.properties
# 的 mod_version 展开生成,运行期经 com.merlinkitsune.astral_dice.network.VersionGate 读取,
# 截取 major.minor 作为网络互通号(仅二号位相同才允许客户端与服务端互联)。
mod_version=${mod_version}
