# 中文更新日志

> 本文件仅收录中文更新日志；英文版见 [`CHANGELOG.md`](CHANGELOG.md)。
> 两个文件按版本号一一对应：同一版本号在两边各出现一次，每次改动必须同时更新中英两份，禁止只改一侧。
> 约定：对当前版本已记录条目的后续改动，直接合并进原条目，仅保留改动后的最终版本，不追加“再次修改”条目。

## 1.3.1

### 新增内容

#### 音效

- **新增 8 条自定义音效**（2026-09-24 用户提供素材）：星币钱包**存钱**、取出**星币**、取出**星币袋**、
  **活体书页**命中目标（本次伤害**小于 8**）、活体书页命中目标（本次伤害**不小于 8**）、使用
  **狂暴 / 王之力 / 岿然不动**、对**其他人或目标**使用效果牌、对**自身**使用效果牌。
  素材统一收录为单声道 Ogg Vorbis（`assets/astral_dice/sounds/`：8 个 ogg ＋ 同名 `sounds.json` 声明 ＋
  `SoundEvent` 注册，事件 id 与音频文件名同名）。**存钱 / 取钱**属界面私有反馈，**只发给操作者本人**；
  其余为世界内动作，**附近玩家都能听到**。活体书页那条按**本次命中的完整伤害**（与伤害跳字同值）分档。
  ⚠️ 2026-09-25 用户裁决：**取星币袋**那条偏吵，音量**降低 40%** —— 音量写在原版 `sounds.json` 条目的
  `volume` 字段上（`coin_bag_withdraw` = `0.6`；缺省 1.0、必须 > 0），该字段由原版
  `AbstractSoundInstance#getVolume()` 直接乘进播放增益（`SoundEngine#play` 读的就是它）。

### 内容与平衡性调整

#### 文案与显示

- **临时牌新增 tooltip 提示行**（2026-09-24 用户裁决「临时牌缺少 tooltip 提示」）：战斗牌与效果牌**两个根类**的
  tooltip 都会在最前面多出一行黄字提示 —— 文案「临时牌：不可丢弃或转移，放入其它容器即销毁，女王特权结束后消失」
  （新增 lang 键 `tooltip.astral_dice.temporary_card`，三线 × 中英日，键数 811 → **812**）。
  ⚠️ 提示行**不显示剩余时间**：客户端只同步自己的效果实例（他人身上的效果不同步），显示时间会在看别人容器里的牌时出错。

- **卡牌费用行改用新符号与本地化标题**（2026-09-24 用户裁决）：战斗牌 tooltip 顶部的费用行原先是硬编码的
  `Cost: ⨀⨀⨀`（「Cost」未做本地化，符号是数学运算符 `⨀` U+2A00，部分字体下字形可疑）。现改为
  **本地化标题 +「◆」重复**（每 1 点费用一个 ◆）：中文「费用：◆◆◆」、英文「Cost: ◆◆◆」、
  日文「コスト：◆◆◆」，新增 lang 键 `tooltip.astral_dice.card_cost`（三线 × 中英日）；
  标题与符号仍**整行黄色**，位置不变。

#### 伤害与结算

- **磨刀石「保留 1 血」增加 1:00 冷却，并明确保命优先级「安全气囊 &gt; 磨刀石」**（2026-09-24 用户裁决）：
  磨刀石那条「生命值大于 1 点时，单次所受伤害至多使生命值降至 1 点」此前**没有冷却**，而它的生效条件
  （这一击会打死你）与安全气囊的「致命伤害」完全重叠 —— 结果是戴着磨刀石几乎不会被单次伤害打死，
  气囊（要花 6 层充能 + 1:00 冷却）的资源优势被抹平。现改为**一次保命耗一次冷却（1:00）**：
  只有 cap **实际削减了伤害**时才写入冷却，冷却中该保护不生效（低血量减伤 -2 与攻击力 +4 不受影响）；
  判定与写入都在服务端，新增玩家附件 `whetstone_guard_cooldown_end`（不 `.sync()`）。
  同时把保命优先级写成**显式契约**：**安全气囊 &gt; 磨刀石** —— 致命一击先交给气囊（消耗充能、完全免伤），
  仅在气囊**不可用**（未佩戴 / 冷却中 / 充能不足）时才由磨刀石兜底；气囊接管后本次结算直接返回，
  磨刀石连它的 -2 减伤都不参与。两者同时佩戴时气囊恒为第一顺位。
  文案同步：气囊 tooltip 与手册的「优先于不死图腾与末影骰子」清单补上「磨刀石」，
  磨刀石 tooltip 与手册标注冷却（三线 × 中英日）。

- **「星光 / 星币 / 治愈点」三项加伤改为独立结算的「额外加伤」（新伤害类型 `astral_dice:extra_damage`）**：手电筒（每 4 星光 +1）、
  星币锤（持有星币的 30%）、美工刀（+2 / +4 与治愈点数）此前是并进「攻击力」一起算的 —— 于是会被「攻击力快照」类效果
  （教主立牌降神的狐光攻击基数）连带放大。现改为**命中落地后单独结算一段伤害**，不再计入攻击力。
  ⚠️ 该类型与真伤 / 技能类伤害同待遇：**无视护甲值与盔甲韧性、并穿透受击无敌帧**（标称点数原样打出）。


- **「敌对目标」的判定口径重写为「敌对生物 ∪ 中立生物（宠物除外）」**（2026-09-24 用户裁决）：此前中立生物**必须已被激怒**（`NeutralMob#isAngry()`）才算敌对目标 —— 于是一头没被激怒的狼、一只没被激怒的铁傀儡 / 北极熊 / 蜜蜂在你眼里都「不算敌对」，只有先挨它们一下才会变成敌对。现改为**中立生物一律计入，仅排除已驯服的宠物**（`TamableAnimal`，即狼 / 猫 / 鹦鹉）。⚠️ 实测影响面：原版同时具备「中立生物」与「可驯服」两种身份的只有**狼**，所以这条实际等价于「**未驯服的狼现在算敌对目标，驯服后不算**」。该口径由**前置库** `starengine_lib` `1.0.3` 统一实现（「敌对目标」的唯一入口 `combat/HostileTargets`），所有依赖它的效果（骰神赐福、法伤加成、目标选择器可选中判定、电磁炮落雷选目标等）一并生效。
- **贯穿之铳移除「已使用伤害效果牌」前提**（2026-09-24 用户裁决「使其与忍术飞镖判定保持一致」）：原先它还要求「身上带着伤害类效果牌生效」（对怪激光 / 对怪板砖 / 轨道炮 / 定向爆破四个效果之一，或本次命中就是活体书页的法伤），使**先打出去的普通箭矢 / 投掷物 / 联动模组法术**拿不到加成。现改为：**只要佩戴贯穿之铳并对目标造成远程 / 魔法伤害即生效**。⚠️ 与忍术飞镖现已**完全同形** —— **不再做任何目标范围判定**（2026-09-24 用户裁决「目标判定也应当与忍术飞镖一致（包括中立目标）」）：旧实现额外要求「目标必须是敌对目标」，而该判据比法伤链的 `isBlessingTarget` 闸门**更窄**，使**非同队但从未攻击过你的玩家**与**非 `NeutralMob` 的中立生物（山羊 / 羊驼 / 狐狸等）**只吃忍术飞镖、不吃贯穿之铳。目标侧现统一由该闸门把关。tooltip 与手册条目已同步删去「敌对」限定。

- **四张伤害效果牌与电击手套的文案改为「远程和魔法伤害」开头**（2026-09-24 用户裁决）：原先写「**对敌对目标**造成的远程和魔法伤害 +N」，而实现上它们只要求「本效果在身」、**不做任何目标判定** —— 文案比实现窄。现统一改为以「远程和魔法伤害 …」开头（定向爆破与电击手套的波及描述亦由「其他敌对目标」改为「其他目标」），tooltip 与手册条目同步。
- **星币锤每次进入骰神赐福消耗 3 → 6 星币**（2026-09-24 用户裁决）。
- **标靶筹码不再把标记施加到「本次正在攻击的那个目标」**（2026-09-24 用户裁决）：改为在**本次攻击目标之外**选最近的敌对目标 —— 否则标记永远落在自己正在打的那只身上，失去「另立靶子」的意义。tooltip 与手册同步说明。
- **魔法箭袋：判定改为「必须先用过伤害效果牌」+ 文案按玩家原文重写**（2026-09-24 用户裁决第二版）：① 追踪**只统计伤害效果牌**（对怪激光 / 对怪板砖 / 轨道炮 / 定向爆破 / 活体书页，判定 `BaseEffectCardItem.isDamageEffectCard`），非伤害类效果牌不再参与；② **取消**首版「活体书页命中即必定触发」的例外 —— 活体书页本身虽计入该集合，但仍须「先使用过一张伤害效果牌」；③ 触发要求「对**已有标记**的目标造成远程 / 法术伤害」；④ 文案重写为「使用伤害效果牌，并对已有标记的目标造成远程或法术伤害后，返还第一张使用的效果牌并对目标施加 1 层标记。每 30 秒只触发一次」（tooltip 与手册同步）。
- **智能手表：物品栏卡牌补充阈值 10 → 6 张**（2026-09-24 用户裁决）。

#### 立牌与状态

- **临时牌新增「自毁机制」：被第三方容器（AE2 存储 / 机械动力物品舱口等）收走后再也不能"留着以后用"**（2026-09-24 用户实报「无法阻止临时牌被放入 mod 容器，导致临时牌永不消失」）：
  先说结论 —— 这类插入在**通用层面拦不住**：已实测 NeoForge 自己的 `ItemStackHandler#insertItem` **只**问
  `isItemValid`（缺省恒真）、**不**问物品的栈级钩子 `ItemStack#canFitInsideContainerItems`（只有
  `ComponentItemHandler` 会问），而 AE2 的存储总线直接写自己的存储实现、机械动力的仓库 / 舱口走
  `IItemHandler#insertItem` ⇒ 既有两道槽位守卫与栈级钩子对它们全都无效。故改为**拦不住就自毁**：
  - 新增数据组件 `temporary_card_expires`（long，绝对 `gameTime`，**仅持久化、不上网**）：发牌时写「现在 + 3:00」，
    让临时牌**自带**一个不依赖玩家的到期刻 —— 这是唯一能对"够不着的场景"做出判定的信息；
  - 新增自毁巡查（每 20 tick，`TemporaryCardUtil#purgeOutOfPlace`）：① **当前打开的任意容器菜单**里，
    非「玩家物品栏 / 副手 / 手中 / 本模组卡牌栏」槽位上的临时牌**立即销毁**，合法槽位上的牌按到期刻收口；
    ② 玩家附近 16 格内的**地面掉落物**直接 `discard`；
  - **三个对齐点**（`realignExpiry`：以效果实例剩余时长为唯一真值重写到期刻）：发放、**再次释放**
    （需求「再次释放把有效期重置为 3:00」对**已存在**的牌同样成立）、**玩家登录**（抵消"效果时长离线冻结、
    而绝对时刻照走"的墙钟漂移）。
  ⚠️ **明确够不着的地方**：第三方存储**内部**（AE2 网络里的存储元件、没打开的仓库）不会被主动清空 ——
  我们既扫不到也不该去扫。后果是它可能占着对方一个格子，但**任何一次取出**（落到物品栏 / 外部槽位 / 地面）
  都会在下一次巡查被销毁 ⇒ 「永不消失」不再成立。旧存档里没有到期刻的牌按「不设限」处理（不凭空定一个过去时刻，
  否则重登即被清），仍受死亡 / 效果结束 / 非法位置三条既有收口。

- **游戏大师立牌「鼠鼠护盾」：单次超过黄心的伤害不再穿透到红心**（2026-09-24 用户裁决）：护盾仍是 **5 黄心（10 点吸收）**、黄心打空即消失，
  但此前一发超过黄心的伤害只被吃掉「能吃的部分」，**超出的部分照常扣红心**（5 黄心挡不住一发 200 伤害里的 190）。
  现改为：**只要还有黄心，这一击就被完整吸收** —— 超出的部分被丢弃，只消耗掉剩余黄心；黄心打空后护盾照旧消失。
  顺带保证**保命类筹码**（安全气囊 / 磨刀石 / 怪力侦探减伤）不在这类「被护盾完整吃掉」的一击上白扣充能。

#### 筹码与资源

- **飞星（紫色 / 金色）的落体参数与目标档位调整**（2026-09-24 用户裁决「高度提升 3 倍，下落速度加快到 6 倍，双飞星间隔加快到 2 倍；飞星应当只对敌对目标生效」）：
  - **高度提升 3 倍**：落体行程 = 旧口径 × **3.6**（历史提速 1.2 × 本次抬高 3.0）⇒ 以 1.95 格高的僵尸计，起点自其头顶再上抬 **7.29 格**（原 2.43 格）；
  - **下落速度加快到 6 倍**：行程 ×3 且**时长减半**（`FALL_TICKS` 20 → **10 tick**，即 0.5 秒落地）⇒ 每 tick 下落距离恰为原口径的 6 倍（0.1215 → 0.729 格 / tick）；
  - **双飞星间隔加快到 2 倍**：`VOLLEY_GAP_TICKS` 20 → **10 tick**（0.5 秒）⇒ 两枚同装时紫色 `t=0→10` 命中、金色 `t=20→30` 命中；
  - **只对敌对目标生效**：目标判定回到 2026-09-24「中立生物一律计入」那次重写**之前**的判据 —— 中立生物须**已被激怒**（`NeutralMob#isAngry()`）才算敌对，于是**未激怒的狼 / 铁傀儡 / 北极熊 / 蜜蜂不再触发飞星**；敌对生物（含**平静时**的末影人 / 僵尸猪灵 —— 它们同时实现 `Enemy` 与 `NeutralMob`，故判据先看 `Enemy`）与消费方额外声明者（试验假人）照常计入。
    ⚠️ 这是对全局唯一入口 `HostileTargets#isHostile` 的**有意收窄**（不是漏用入口）：重写后的宽口径是为**主动技能**的释放目标判定（秘密侦探 / 枪匠）定的，而飞星是**被动自动触发**（路过即落星），不该凭空砸到平静的中立生物。判据集中在 `ShootingStarManager#isStarTarget`。

### 已修复BUG

#### 伤害与结算
- **磨刀石在 1.21.1 / 26.1.2 上按「吸收（黄心）之前」的伤害计算，导致带黄心时被过度削减**（2026-09-24 用户实报
  「1.21.1 不符合预期、1.20.1 正确」）：1.20.1 的 `LivingDamageEvent` 天然派发于**吸收之后**，磨刀石看到的就是
  「扣完黄心还要掉多少红心」；而 1.21.1 / 26.1.2 的 `LivingDamageEvent.Pre` 派发于**吸收之前**，磨刀石拿到的是
  **含黄心**的原始伤害 ⇒ 拿它跟 `getHealth()` 比，会把「黄心本来能吃掉的那部分」也算成致命，于是**连黄心都少吃**。
  典型症状：血量 5、黄心 10、挨 8 点 —— 本该「黄心吃 8、红心一点不掉」，实际被削成 4（黄心只掉 4）。
  现改为：先换算成「吸收后净掉血」再交给磨刀石，然后把**它省下的那部分**从事件伤害里扣回去
  （`newDamage = damage − 吸收后净掉血 + 修正后净掉血`）⇒ **黄心消耗量保持原样**，只有红心扣血被磨刀石改动，
  与 1.20.1 逐点一致（两线的 `modifyIncomingDamage` 本就是同一个函数 ⇒ 入参口径统一即完全等价）。
  气囊的致命判定本来就用「吸收之后」，不受影响；虚空伤害照旧不受保命影响。


- **试验假人（`dummmmmmy` 训练人偶）现被永远视为敌对目标**（2026-09-24 用户报告「试验假人未被声明为可命中目标」）：此前它既不是 `Enemy`、也不会被激怒，故「需要敌对目标」的效果一律把它排除 —— 最直接的后果是**活体书页选不中、也打不到它**。现改由**前置库**（`starengine_lib` `1.0.3`）新增的「额外敌对判定」注入入口解决：本模组启动时把「假人算敌对」的判定注入库的 `combat/HostileTargets`（「敌对目标」的唯一入口）⇒ 假人在**所有**此类判定中均算敌对：骰神赐福触发、法伤修饰符链、以及目标选择器的可选中判定（客户端射线 / 半径高亮 / 服务端确认）。

- **枪匠立牌被动不再额外限制「普通敌对生物」**（2026-09-24 用户裁决「释放目标判定应包含中立」）：1.21.1 / 1.20.1 两线的被动仍写着 `target instanceof Enemy`，而 26.1.2 线早已去掉 —— 于是**中立生物**（狼 / 铁傀儡 / 北极熊 / 蜜蜂、山羊 / 羊驼等）身上带「破绽」时**拿不到「弱点识破」**，而同处的骰神赐福照常触发。现两线照 26.1.2 回填，触发条件与骰神赐福完全一致（近战武器 + `isBlessingTarget`）。
  ⚠️ 其**释放**目标判定本就走 `SelectorTargets` 的 `ENEMY` 口径（= 敌对生物 ∪ 中立生物(宠物除外) ∪ 试验假人），**已含中立**；本次一并订正类注释里「仅 vanilla Enemy」这句自 2026-09-22 口径下沉起即已过时的说明。
- **四处「波及 / 增益」的目标判定收窄问题一并修正**（2026-09-24 用户裁决「修复审计发现的全部问题」）：定向爆破与电击手套的范围波及、大当家立牌的溅射、以及骰战「调查阶段增益」，原先都走较窄的 `HostileTargets` —— 于是打「非同队但从未攻击过你的玩家」「Boss」「会反击但原版未标中立」的怪时，主目标吃加成、波及却打不到同类目标。现统一改用**法伤链同一闸门** `isBlessingTarget`。 ⚠️ **同批修复**：电击手套与定向爆破的**波及选目标此前未排除施法者自己** —— 该闸门把「无队伍玩家」视为敌对（相对他人成立、对**本人**并不成立）⇒ 近距离攻击时会被自己的波及伤害打到；现已显式排除（`e != ctx.attacker`）。
- **伤害钩子优先级显式化**：全部「受伤改数值」处理器此前多数未声明 `EventPriority`（同档内执行顺序不确定）。现统一显式标注（放大 HIGH / 主链 NORMAL / 后置放大 LOW / 保命 LOWEST），本次不改行为，顺序不再依赖注册顺序。
- **速度轮滑（初级 / 中级 / 高级）tooltip 的 `%` 不再掉色**：原实现用 `Component.translatable` 直出含 `%%` 的文案，而原版会把 `%%` 拆成**独立的无样式片段**，落在高亮区里的 `%` 会掉成行底色。现改用项目约定的 `tt(...)`（即 R4 审计项），三级一并修正。
- 清理 27 个文件中**未使用的** `import ... Enemy` / `import ... Monster`（纯卫生，无行为变化）。
- **为「技能类伤害」建立独立伤害类型，并修正怪力侦探立牌误走法伤链**（2026-09-24 用户裁决「为技能类伤害创建单独的伤害标签，避免与法伤混用」）：新增 `astral_dice:skill_damage`（登记进 `bypasses_armor` ⇒ 仍无视护甲值 / 盔甲韧性；**不**进法伤白名单），并把「怪力投掷」的落地结算由 `astral_dice:card_spell` 改为它。此前那 2（+5）点固定伤害是**按法伤结算**的 —— 会被忍术飞镖（+标记层数）、贯穿之铳（+目标防御）、紫晶骰子（+骰点）、标记喷罐（再叠标记）、魔法箭袋、对怪激光 / 板砖 / 轨道炮 / 定向爆破与立牌加成一路放大，并可能触发电击手套的范围波及。现只结算自身点数；同时新增该类型的三语死亡消息键。该类型另登记进 `bypasses_cooldown`（与真伤一致）⇒ **穿透受击无敌帧**，技能伤害不会被目标的短暂无敌吞掉（2026-09-24 用户裁决「技能伤害增加真伤修饰器，使其始终造成真伤」）。

#### 立牌与状态

- **临时牌「不可丢弃」此前只在服务端成立 —— 客户端按 Q 会把牌"吃掉"（地上什么都不掉）**（2026-09-24 用户实报「战斗牌能被按Q丢弃且不会有掉落物」）：
  服务端 `ServerPlayer#drop(boolean)` 的**第一件事**就是问物品能不能被丢（`ItemStack#onDroppedByPlayer`，为假直接 return）⇒
  临时战斗牌（`CardItem` 覆写）在服务端**根本不会被丢**；但**客户端**的 `LocalPlayer#drop(boolean)`（1.21.1 / 1.20.1 / 26.1.2
  **字节码同形**）是「先 `Inventory#removeFromSelected` 本地删除、再发包」，且**从不问这件事** —— 客户端也从不产生掉落物实体
  （实体只在服务端 `CommonHooks#onPlayerTossEvent` 的 `addFreshEntity` 里存在）。于是客户端格子被清空、服务端静默拒绝，
  而服务端槽位没变 ⇒ `broadcastChanges` **不会**补发槽位包 ⇒ 客户端长期显示「牌没了、地上也没有东西」，
  直到某次全量同步（重登 / 重开容器界面）才回来。现新增客户端 mixin `mixin/client/LocalPlayerDropGuardMixin`：
  在 `LocalPlayer#drop(Z)Z` 的 HEAD 按**同一判据**（`TemporaryCardUtil#isTemporary`）直接拒绝并返回 `false` ⇒
  不移除、不发包、无任何副作用（只拦临时牌，不在客户端替第三方模组调用它们的 `onDroppedByPlayer`）。
- **临时效果牌此前没有覆写「不可丢弃」**：效果牌根类 `BaseEffectCardItem` 只覆写了光效与容器拦截 ⇒ 服务端会走完丢弃流程，
  之后才被 `ItemTossEvent` 兜住（取消实体 + 尽力退还进物品栏）：牌不会落地，却会**跳到物品栏的另一个格子**，
  且退还失败（物品栏满）时会被**直接销毁**（该处理器明写「绝不落地 ⇒ 放不下即销毁」）。现与战斗牌根类 `CardItem` **对称**地补上
  `onDroppedByPlayer` 覆写，两个根类走同一份判据（与「覆写体只允许委托共用判据」的既有约定一致）。
- **容器界面里对着临时牌按 Q 会先把牌摘出来（物品栏满时直接销毁）**（2026-09-24 用户实报「装备到骰子内的临时战斗牌也会直接消失」）：
  GUI 里的 Q = `ClickType.THROW`，原版顺序是**先从槽里摘出**（`slot.safeTake`）、再 `player.drop` ⇒ 牌先离开原位，
  之后才遇上被取消的 `ItemTossEvent` 与"尽力退还"（满包 ⇒ 销毁）；即使退还成功，卡牌栏里那张也会被**静默卸下**
  （菜单关闭时 `saveToDice()` 随即把它从骰子里抹掉）。现新增 mixin `mixin/container/ContainerClickGuardMixin`
  （挂 `AbstractContainerMenu#clicked` 的 HEAD —— 它是**所有**容器界面点击的唯一入口）：`THROW` 命中临时牌、
  或把光标上的临时牌拖出 GUI（`PICKUP` + 伪槽位 `-999`）⇒ 直接取消，什么都不会被摘出、不产生掉落物。
- **另有两条路径能让临时牌跨过清理（含死亡）**：`purgeAll` 会清空主物品栏、副手与骰子已装配的牌，
  但**当前打开的卡牌栏菜单内容**与**鼠标光标上那一张**不在扫描范围内 —— 前者在菜单关闭时还会被 `saveToDice()`
  **写回骰子**（死亡掉落的处理发生在清牌之后，而容器内容根本不在掉落清单里），后者在容器关闭时会被退回物品栏
  ⇒ 死亡时若卡牌栏开着、或光标上提着临时牌，这些牌能**跨死亡存活**。现 `purgeAll` 增补一段：打开的卡牌栏菜单
  由它自己的 `purgeTemporaryCards()` 清空，光标上的临时牌栈直接清掉（只动临时牌，其它物品不受影响）。

- **怪力侦探主动「怪力投掷」不再把多个目标叠在同一格**（2026-09-24 用户实报「聚集的怪隔几秒又被弹开」）：
  旧实现把范围内的全部敌对目标沿**一条线 ±0.9 格**铺开 —— 目标数 ≥ 4 时相邻落点间距不足 0.6 格、实体严重重叠；
  而原版 `LivingEntity#aiStep` 末尾**无条件**执行 `pushEntities()`（`noAi` 只关 AI 决策、**不阻止推挤**），
  对包围盒内每个可推实体各调一次 `doPush` ⇒ 叠在一起的怪被互相弹开（线性排布下两端受力同向叠加、弹得最明显），
  AI 再把它们赶回玩家身边 ⇒ 周期性复现。现改为**中心优先、按到中心距离升序**的网格落点：间距 ≥ 1.1 格
  （按目标中最大的碰撞箱宽度放大 ⇒ 铁傀儡 / 蜘蛛这类宽体怪也不会重叠），每个候选格点**独立求可落地面**
  （地形受限就跳到下一个候选）⇒ 落地即**互不重叠**，推挤自然消失。

- **教主立牌「降神」的额外加伤现在会「维持当前目标」**：旧实现对**已攻击过的目标**直接返回 0 ⇒ 加伤只对每个目标的**第一击**生效
  （实测同一靶第二击 `208 → 8`）。现改为：攻击**新目标**时消耗 1 层狐光并给出加成，**同一目标的后续攻击照常吃这份加成**（不再消耗）；
  层数已为 0 时攻击新目标则不追加伤害。
- **「狐光」效果现在可被清除**：① **卸下教主立牌即清空层数与图标**（旧设计刻意不清，本次按用户裁决改为清）；
  ② 层数归 0 时图标必定移除（移除改为「清到真的没有实例」的循环）；③ 图标层数按原版**显示上限**饱和 —— 原版只在
  `amplifier ∈ [1,9]` 时画罗马数字（即层数 2..10 才有数字），故层数 ≥ 11 现在固定显示 **X**（=「10 层及以上」），
  真实层数见立牌说明的「狐光：x / 20」。


- **蛟龙立牌（真龙形态）与人偶师立牌（人偶完成）：进入锁存状态后两个计数器即失效，不再继续累计** ——
  此前「觉醒」与「人偶制作」在状态成立之后仍照常计数：人偶师侧会在**归零后重新爬满 7 层、反复触发一次转换**，
  「人偶制作」图标也会再次出现；蛟龙侧则把「觉醒：8 / 8」永久挂在物品说明里。现改为**状态成立即计数失效** ——
  蛟龙进入真龙形态后不再累计觉醒（值恒为 8），物品说明亦不再显示「觉醒」计数器行、只留「真龙形态」标注；
  人偶师进入「人偶完成」后「人偶制作」不再接受任何层数写入。

#### 文案与手册

- **友情徽章 / 大碗炖肉**：删去括号内的注释（「按『治疗者 + 目标』成对去重…」「仅自己与同队玩家已驯服的宠物 / 坐骑…」）—— 属实现细节，玩家不需要知道。
- **普通瞄具 / 鹰眼瞄具**：① 首版按用户裁决「瞄具类攻击受**骰神赐福门控**，不按骰神赐福模式攻击（近战类）则不应该触发」把门控**补进代码**（`DiceCombatModifiers` 的攻击修饰器要求 `DICE_BLESSING` 生效；普通瞄具的固定 +2 仍无条件）；② 同日后又一裁决：**删除 tooltip 与手册中的「骰神赐福期间」字样（仅文本，代码门控保留）** ⇒ 文案不再体现该门控（玩家侧说明同步精简）。
- **探天卫星**：「本轮出牌数 +1（每 1:00 仅一次）」的括号改为逗号，统一句式。
- **复仇之戟**：删去「每类只触发一次」与「不叠加」的说明（tooltip 与手册同步）。

#### 输入与界面

- **手持效果牌时不再吞掉立牌主动技能键（J）**（2026-09-24 用户报告「玩家手持活体书页时，无法触发主动技能」）：
  选择器类效果牌（**活体书页**、加急加快、奢华大餐、你有我有、狂暴、符卡-福/祸…）**握在主手即自动开启目标选择器**；
  而目标选择会话激活期间，主动技能键被当作「取消选择」，服务端也在 `performSkill` 第 2 步直接返回
  ⇒ **只要握着这类牌就触发不了立牌主动技能**（客户端还会给出「已取消目标选择」的误导提示）。
  现把「**按键开启的会话**」与「**手持即选择会话**」分流：只有前者把 J 当取消；手持类会话不再拦截该键，
  立牌主动技能照常触发，且**手持的牌保持瞄准状态**（该类会话的取消手势仍是 下蹲+右键 / 移出主手）。
- ⚠️ 同批修正一处连带缺陷：`performSkill` 第 6 步「是否延后冷却」的判据也一并改用「按键开启的会话」——
  否则握着效果牌放出的技能会被误判成「本次主动开了选择会话 ⇒ 冷却待确认后再起」，导致
  **技能不进冷却 / 不进锁定，电流核心也不充能**。

#### 筹码与资源

- **银行卡（以及 ATM机 / 银行卡-用不完 / 星币锤）「装备加星光、卸除不加回」⇒ 反复装卸刷星光**（2026-09-25 用户实报「银行卡刷星光」）：上一批修好「从来不发」之后，**卸下时只释放了发放闸门、没有把星光扣回** ⇒ 反复「装备 → 卸下」可无限累积星光。银行卡更严重：卸下后基础值（下限）随之消失，原本**不可兑换**的那 4/7 点立刻变成可兑换 ⇒ 变成一条稳定的星币产线。现按用户裁决落实 **「卸除即扣除」全筹码底线**（`StarLightManager`）：
  - **发放**：统一走 `grantStarlightOnEquip`（一次性 +N）/ `grantStarlightFloorOnEquip`（银行卡等下限型）——申领装备会话闸门后，把**实际抬升量**写进新增附件 `starlight_equip_grant_amounts`（Int，每位 4 bit 打包；不 `.sync()`）；
  - **卸除**：统一走 `revokeStarlightOnUnequip` —— 按账本把本次装备**实际**获得的星光扣回，并释放闸门；四枚筹码的 `onChipUnequip` 全部改接（银行卡为新增）。
  - ⚠️ **必须按账本扣、不能按名义值扣**：星光已到上限（32）时装备**一点没涨**，照名义值扣就是白扣玩家自己攒的星光。
  - ⚠️ **扣回不走 `set()`**：卸下回调执行时槽位状态未必已更新，`set()` 的基础值下限会被**尚未移除**的银行卡重新抬回去 ⇒ 扣不动（这正是银行卡「卸下不掉星光」的根因），故直接写 `ModAttachments#setStarlight`。

- **「装备时获得星光」的四枚筹码从未发放过星光（Curios 回调参数语义写反）**（2026-09-24 用户实报「所有银行卡都无法提供星光点数」）：Curios 的官方签名是 `onEquip(slotContext, prevStack, stack)` —— 第 2 参是**槽位原内容**（往空槽装备时即 `ItemStack.EMPTY`）、**第 3 参才是刚装上的那件**（由 `ItemizedCurioCapability#onEquip` 以 `this.getStack()` 传入，恒非空）。这四枚筹码的旧代码把空槽守卫 `if (!prevStack.isEmpty()) return;` 写在了第 3 参上 ⇒ **恒为真、恒 return**，整段发放逻辑从来没有执行过：
  - 银行卡-余额少 / 余额多：装备时应把星光抬到基础值 **4 / 7**（即 `StarLightManager#getBasePoints` 的「基础星光不可兑换」口径），实际一直是 0；
  - 银行卡-用不完：装备时 +3 星光，从未发放；
  - ATM 机：装备时 +1 星光，从未发放；
  - 星币锤：装备时 +5 星光，从未发放。
  现按官方签名改正参数；其中「一次性发放」的三枚（用不完 / ATM / 星币锤）改由**玩家级「装备会话」闸门**判定（新增玩家附件 `starlight_equip_grant_flags`，位掩码）。**为什么必须加闸门**：Curios 只持久化 `stacks`、**不持久化 `previousStacks`**，于是**登录 / 重生 / 切维度**后首个 tick 会把「槽里有物品、上值为空」判成一次装备变化并**重放 `onEquip`**，而重放时 `prevStack` 恰好就是空栈 ⇒ **空槽守卫根本挡不住这条路径**（照原样只改成正确参数的话，每次登录都会白送一次星光）。闸门在装备时置位、卸下（`onChipUnequip`）时释放 ⇒ 装卸循环仍按设计每次装备发一次，但不吃登录重放。银行卡-余额少 / 余额多那两枚是**幂等的基础值下限**（`set` 只在低于基础值时抬升），故不加守卫，重复触发无副作用。
- **同款签名问题另波及两处，一并修正**：① 诅咒之剑筹码把「千咒刻印」写到了第 2 参上 —— 空槽装备时那正是 `ItemStack.EMPTY` **全局单例**，组件写进去后立刻被丢弃，装备时从未刻印成功（全靠 `curioTick` 每 tick 兜底才看似正常）⇒ 改用第 3 参；② 大当家立牌的「装备时重置『1 分钟未触发赐福』计时起点」同样从未生效 ⇒ 改用同一闸门判定（并避开登录重放把玩家刚攒的进度抹掉）。

### 安装要求


- ✅ **本版本起，前置模组 StarEngine Lib 已内嵌进本模组 —— 玩家无需再单独安装**：产物内自带一份
  `starengine_lib`（内嵌于 `META-INF/jarjar/`），由加载器在启动时自动载入。
- ⚠️ **请不要再单独放置 `starengine_lib-*.jar`**：加载器按 modId 去重时**优先采用 `mods` 目录里那一份、
  丢弃内嵌副本** ⇒ 如果那份是**更旧**的版本，就会盖掉本模组自带的新库（表现为前置版本不匹配）。
- **内嵌版本 = `1.0.3`，兼容区间 `[1.0.3,2.0)`**（与 `mods.toml` 里声明的前置区间同源）：该库仍是**必需**
  前置（自 1.3.0 起本模组大量共享实现位于其中），只是改为随本模组一起分发。
- 库的源码仓库：<https://github.com/merlin-kitsune/starengine_lib>。

## 1.3.0

### 新内容

#### 立牌与技能
- **目标选择器的操作方式改为「点选式」：左键确认、右键提示不可自用、右键+潜行取消**:选中目标后 —— **鼠标左键 = 确认**（准星没对准合法目标时只提示「没有可用的目标」、不会提交）；**鼠标右键 = 对自身使用**：默认所有技能都不能对自己使用，因此只弹一行「该技能无法对自己使用」的提示，**不会**提交选择、选择会话仍然保留，可继续瞄准别的目标（**例外：游戏大师立牌（ren）的主动允许对自身使用 —— 右键即对自身施放，指引行会多给出这一选项**）；**鼠标右键 + 潜行 = 取消**；**Esc** 照常打开暂停菜单，菜单一打开就取消选择；**J**（主动技能键）依旧可随时取消。与此同时，**回车确认键**与「选择期间把键盘按键吞掉」的旧行为已移除 —— 现在选择期间移动、F3、物品栏、聊天与其它模组的按键都照常工作，只有鼠标左右键与滚轮会被接管（⚠️ **效果牌「手持即选择」的会话不接管滚轮**，滚轮照常切槽 —— 见本版本「已修复BUG / 立牌与技能」）；命令聊天窗口仍可正常使用（自动化测试也依赖这条通道）。
- **目标选择框重新绘制:改为贴合生物实际可见外形的半透明棱柱**:此前边框只按碰撞箱画，僵尸张开的手臂、蜘蛛的八条腿、马的头颈都会露在框外；现在改为**碰撞箱 ∪ 逐帧实测的模型外形**，并在外侧再留出「线宽的一半」的余量 ⇒ 边框的左右、前后与顶面**只可能比生物略大、绝不内缩**（模型动画摆动、幼年体缩放都会跟着变）；**底面单独向上抬升 0.02 格**，避免边框下沿与地面贴平造成闪烁（此前下沿会插进地面方块顶面）。颜色与线宽沿用：友方绿 / 敌对红 / 中立黄，准星命中的目标线最粗（1/16 格）、准星命中但不可选稍细（1/24 格）、半径内其它可选目标最细（**1/128 格** —— 2026-09-19 再收细一半，加大它与「已指向」的区分度）；**准星只要落进目标的外框范围即视为已指向该目标**：判定与描边用的是**同一个盒**（碰撞箱 ∪ 逐帧实测模型外形，再外扩半线宽），因此僵尸张开的手臂、蜘蛛的腿、马的头颈这类「看得见但在碰撞箱之外」的部位也点得中，不再要求准星准确压在碰撞箱上；仍**只在本地渲染** —— 多人游戏里其它玩家看不到你的选择框。

- **新增史诗立牌「游戏大师」（ren）**：被动「鼠鼠救我」——佩戴者超过 5:00 没有鼠鼠护盾时，自动获得 1 张随机卡牌与鼠鼠护盾（作用对象就是佩戴者本人）；主动「熊孩子特权」——用目标选择器选择任意玩家（鼠标右键可直接对自身使用），使目标玩家获得 1 张随机卡牌与鼠鼠护盾（**目标选择器里显示的技能名为「鼠鼠护盾」；立牌 tooltip 与手册里的主动名仍是「熊孩子特权」**）。鼠鼠护盾 = 5 黄心（10 点吸收）+ 抗性提升 + 1 层反击：黄心被打空后护盾立即清空（连同抗性提升与未使用的反击层），护盾本身没有时限（效果栏上的倒计时只是刷新窗口）；带盾玩家被攻击时消耗 1 层反击，对攻击者注入一次反击伤害（与枪匠破绽、肉弹战车嘲讽同一套反击公式）。**效果栏只显示「鼠鼠护盾」与「反击」两个图标** —— 护盾附带的抗性提升隐藏图标（只作为数值生效），反击层数新增自己的图标。配方为基础骰子档（纸×4 + 星币×3），并已进入创造栏、`astral_dice:signs` 汇总标签、饰品槽标签、赏金奖励池与手册。
- **新增鼠鼠护盾的第三人称可见外观**：带护盾的玩家会被一层**完整的淡蓝色半透明能量球**笼罩（球心在身体中心、半径 1.2 格，把玩家整个包在里面），**其他玩家也能看到**（可见性由专门的护盾状态同步包广播 —— 原版不同步 mob effect 给本人与乘客以外的玩家，详见「已修复BUG」）；球面纹路持续流动，外面还叠了一层辉光外壳与一条自下而上的能量带，观感是流动的能量而不是静止的玻璃罩；第一人称下不绘制自己那一层，避免外壳铺满屏幕。

- **新增传奇立牌「风水师」（zhao）**：被动「**福祸相倚**」—— 骰战掷出的**最终骰点**结果为 **1** 时获得 1 张符卡-祸、为 **6** 时获得 1 张符卡-福（两张牌在发放那一刻就绑定获得者，他人无法使用；一次结算只判定一次，同一击命中多个目标也只发一张）；被动「**完美帮手**」—— 对**装备大当家立牌**的玩家施加「白泽赐福」时，该玩家额外获得 **1 层养精蓄锐**（沿用大当家既有的养精蓄锐层数，不新增任何效果）。主动「**白泽赐福**」—— 目标选择器类技能，可选择 **16 格内**的玩家或**对自身使用**：目标获得「白泽赐福」，效果期间目标的一切治疗在回满血之后**溢出的部分等量转化为攻击力**（回满血那一刻不产生溢出，加成也不会留在身上）；效果持续到**下一次骰神赐福结束**后消失 —— 施加时目标身上**没有**骰神赐福 ⇒ 该次骰神赐福一结束就消失；施加时目标身上**已有**骰神赐福 ⇒ **跳过当前这一次**，等**下一次**骰神赐福结束才消失。施法者同时获得 **1 张符卡-福**，并把自身持有的**全部符卡-祸**转化为等量符卡-福。配方为钻石骰子 + 黄金星盘（传奇档），并已进入创造栏、立牌汇总标签、饰品槽标签与手册（含手册条目）。
- **大当家立牌新增被动「心意相连」**：大当家立牌**使用主动技能时**，同队中**装备风水师立牌**的玩家各获得 **1 张符卡-福**（符卡-福绑定获得者）；**未组队时不生效**，自己也不会收到这张牌。
- **新增传奇立牌「教主」（teru）**：主动「**降神**」—— 目标选择器类技能，**只能选择其它玩家**（不能选生物与自身）：锁定该玩家 **50%** 的攻击力与防御力加给自己（施法瞬间锁定，目标后续成长不再计入），并在其身上留下「降神」；目标**每攻击一个新目标**（本次降神期间还没打过的新目标）消耗 **1** 层狐光，按「狐光攻击基数 + 消耗后剩余层数」追加一次攻击力；效果持续到**该目标自己的下一次骰神赐福结束**（施加时目标已在骰神赐福 ⇒ 跳过当前这一次），**降神生效期间无法再次施放**（按键只提示，不进入冷却）。被动「**狐光**」—— 层数资源（上限 **20** 层，**跨死亡保留**）：自身（以及降神目标）**合成 / 获得 / 装备攻击牌**时 **+1** 层/张，施放降神瞬间立即 **+3** 层；狐光攻击基数 = 施法前自身攻击力 + 目标攻击力的 50%（施法瞬间锁定）。**防刷口径**：拾取地面上的攻击牌**不计层**（丢弃后捡起拿不到任何层数）；反复把同一张战斗牌插入骰子卡牌栏再卸除也**不计层**（按「历史同时装备张数」去重，只有真的多持有/多合成卡牌才继续涨层）。施法者的死亡与登出**不影响**目标身上的降神，其加成只在降神结束时移除；目标死亡或登出时立即移除。配方为钻石骰子 + 黄金星盘（传奇档），并已进入创造栏、立牌汇总标签、饰品槽标签与手册。
- **新增稀有立牌「绿洲女王」（nardis）**：被动「**威压**」—— 骰子**已装配**的每张**攻击牌**使攻击力 **+2**、每张**防御牌**使防御力 **+2**（读时现算，卸下立牌或卸下卡牌立即失效；临时牌同样计入）。主动「**女王特权**」—— 立即获得 **2 张随机战斗牌与 1 张随机效果牌**（**不会给出专属牌**），并获得 **3:00** 的「女王特权」效果（HUD 显示以立牌图标为图标的计时器）；**临时牌只能装备与使用**：带附魔光效以作区分，**无法丢弃**、**无法放进箱子/末影箱/潜影盒等任何其它容器**（只允许待在物品栏、手中与骰子卡牌栏）；计时结束（或效果被移除、玩家死亡、离线期间到期后重登）时**立即清除全部临时牌，包括已装配到骰子里的那些**。**效果生效期间可以再次释放**（只受冷却限制，冷却自释放当刻起算）：新获得的临时牌会**叠加**在现有临时牌上（**不清空旧牌**），并把有效期**重置为 3:00**；**临时牌最多持有 9 张**（已装配到骰子里的也算），达到上限后无法继续释放。**物品栏空格不足 2 个时无法释放**（不会消耗冷却，并给出「物品栏空间不足」提示）。效果只由 **3:00 到期**、效果被移除或玩家死亡结束 —— **牌被用光不会提前结束效果**。给牌走本模组统一发牌漏斗，因此「获得卡牌」类联动（维生素药丸治愈点、教主立牌的狐光计层）照常触发。配方为黄金骰子（无星盘，稀有档），并已进入创造栏、立牌汇总标签、饰品槽标签、手册与**赏金奖励池**。
- **新增传奇立牌「蛟龙」（mamushi）**：被动「**湖沼之王**」—— 每当佩戴者**使其他角色获得卡牌**时，按**受益人数**获得「**觉醒**」层数（每名其他角色 +1 层，**单次最多 3 层**；给自己发牌不计），觉醒达到 **8 层**即进入「**真龙形态**」：**攻击力 +5**、主动技能**取消范围限制**（同维度全体），并把**手中、背包里与骰子卡牌栏里的「撕咬」全部转换为「龙之咆哮」**（此后主动技能发放的也是「龙之咆哮」）；**觉醒与真龙形态死亡不重置，只有卸下立牌才归零**（效果栏以立牌图标显示真龙形态）。主动「**连锁反应**」—— 自身立刻获得 **1 张「撕咬」**，并使 **12 格内的所有友方队友**各获得 **1 张随机卡牌**（**目标当前没有任何卡牌时额外再给 1 张**；**未组队时对象改为该维度内所有其他玩家**，同样只受范围与人数上限约束；**总人数最多 32 人**，按**距离由近到远**发放；真龙形态下取消 12 格限制 ⇒ 同维度全体）。**触发被动的不只是本主动 —— 使用「你有我有」以及今后任何给牌功能的卡牌同样计层。** 主动为**强制冷却 1:00，无法被充能、电流核心或任何其它方式缩短**。配方为**钻石骰子 + 黄金星盘**（传奇档，材料 = 海晶灯×4、金块、空白立牌、黄金星盘×2、钻石骰子），并已进入创造栏、立牌汇总标签、饰品槽标签与手册（含配方页）。
- **新增史诗立牌「怪力侦探」（sherry）**：被动「**侦探出击**」—— 每攻击一个**最大生命值不少于 20** 的**敌对目标**获得 **1 层「推理时间」**（上限 **5** 层）；每层「推理时间」使攻击力 **+1**、受到的伤害 **−1**；**每次骰神赐福效果结束后扣除 1 层**。被动「**挚友守护**」—— 同队伍内若有玩家装备**人偶师立牌**，则该玩家受到的伤害 **−1**。主动「**怪力投掷**」—— 把 **12 格内所有敌对目标**扔向面前，随后对被投掷目标造成 **2 点伤害**并施加 **1 层「标记」**；玩家的「推理时间」达到 **5 层**时，对被投掷目标额外造成 **5 点伤害**。**「推理时间」跨死亡保留，只有卸下立牌才归零**（效果栏以立牌图标显示并按层数计数）。配方为**钻石骰子 + 黄金星盘**（史诗档），并已进入创造栏、立牌汇总标签、饰品槽标签、**赏金奖励池**与手册（含配方页）。
- **新增稀有立牌「人偶师」（hanna）**：被动「**幻想千金**」—— **战斗骰点等于 6** 时获得 **1 星币**；**经过友方玩家**时获得 **1 星币**和 **1 层「人偶制作」**（**自身处于「魔女漂浮」状态时**，该玩家改为获得 **3 星币**）；**该能力每 1:00 仅触发 1 次**；「人偶制作」达到 **7 层**时**转换为「人偶完成」**（层数归零），此后路过友方玩家使该玩家**额外**获得 **迅捷 II (1:00)** 和 **3 星币**。被动「**挚友祝福**」—— **路过装备「怪力侦探」立牌的玩家**时，使该玩家获得 **力量 II (1:00)** + **抗性提升 (1:00)** 和 **1 层「推理时间」**；**该能力每 1:00 仅触发 1 次**。主动「**漂浮魔法**」—— 使自身获得 **魔女漂浮 (1:00)**：在此期间**移动速度 +20%**、**掉落伤害 -100%**、**任何对你的近战攻击都会被闪避**，且**无法使用末影珍珠进行转移**。配方为**黄金骰子（无星盘）**（稀有档），并已进入创造栏、立牌汇总标签、饰品槽标签、**赏金奖励池**与手册（含配方页）。

- **修复鼠鼠护盾的第三人称外观在他人视角下完全不渲染**（2026-09-23 用户实测）：渲染条件原先取 `entity.hasEffect(REN_SHIELD)`，而**原版从不同步 mob effect 给「本人 + 自己乘客」以外的玩家** —— 构造 `ClientboundUpdateMobEffectPacket` 的位置全 jar 只有 4 处（`ServerPlayer` 的效果增改 ×2、`PlayerList.sendActiveEffects`、`LivingEntity.sendEffectToPassengers`），全部只发本人或乘客；`ServerEntity` 内也不含任何效果同步代码。原版为「他人可见」单独开的两条通道（发光轮廓 `setSharedFlag(6)`、效果粒子 `DATA_EFFECT_PARTICLES`）都走 `SynchedEntityData`，**只同步外观、不同步效果实例**。⇒ 他人客户端上该判定恒为 false，护盾球只有在持有者自己（第三人称）能看见。现改为由**专门的护盾状态同步包**驱动：服务端在护盾授予/清空/被外部移除时，把「当前全服护盾持有者」的**全量列表**广播给所有玩家，玩家登录时再单独补发一份全量（`entityId` 只在单个服务端会话内唯一 ⇒ 全量替换同时承担跨服务器撞号的清理）。

#### 物品、筹码与交易
- 修复**效果牌 tooltip 的冷却行不会随时间刷新**（用户报告「手持效果牌时冷却倒计时不能实时刷新」，且这是**全局**问题）：该行原先恒显示**本次冷却的总时长**（充能减免后的静态值）⇒ tooltip 虽每帧重建，数值却永不变化。现改为**冷却进行中显示实时剩余秒数**（随 tick 递减），空闲时仍显示总时长。
- **新增专属效果牌「符卡-福」**（风水师立牌专属）：把牌拿在**主手**上即自动打开目标选择器（**没有倒计时**，牌离开主手即关闭、未确认不消耗），可选择 **16 格内**的**任意玩家（不限队伍）**，也可**对自身使用**；确认后目标恢复 **2** 点生命值，并让**当前出牌轮的出牌数 +1**（**每张各 +1**，与忍者立牌主动的一次性 +1 可以叠加，仍受「单轮最多 9 张」的全局上限约束 —— 贴着上限使用时这一次刚好抵消自己的消耗，不进入冷却）。该牌是**专属牌**：发放时即绑定获得者，**只有获得者本人可以使用**，他人拿在手上既开不了选择器也不消耗。
- **新增稀有专属效果牌「符卡-祸」**（风水师立牌专属）：把牌拿在**主手**上即自动打开目标选择器，**只能对敌对目标使用**（敌对生物、已被激怒的中立生物，**以及非同队伍的玩家**；**队友玩家、被动生物与自己都不可选**），确认后目标受到 **1** 点伤害。持有代价「**厄运**」：效果栏上的**厄运层数就等于你当前持有的符卡-祸张数**（张数归 0 时，厄运与周期伤害一并消失），并**每 2:00** 按**结算那一刻**的持有张数受到等量伤害 —— 这 2 分钟的计时器只在你**首次持有**时起算一次，此后**只由结算推进**：持有张数在 1 张、5 张之间来回变化**都不会**让它重新起算，所以进出几张既不会拖长也不会缩短周期。该牌可以自由丢弃，也可以放进潜影盒、收纳袋等任意容器（**放进容器后**不再计入厄运与周期伤害）。该牌同样是**专属牌**：只有获得者本人可以使用。
- **新增专属战斗牌「撕咬」**（蛟龙立牌专属）：费用 **2**、耐久 **1**、攻击力 **+3**。装备后**触发骰神赐福**时获得 **1 层「觉醒」**，并使攻击力 **+当前觉醒层数（最多 4 点）**，持续到本次骰神赐福结束（层数在赐福期间变化会**实时**反映；同时装备多张时每张各触发一次）；在 **7 层**时装备并触发赐福会**立即进入真龙形态**。与专属效果牌同口径：**只有获得者本人可以使用**，没有配方，也不进任何随机池与赏金池。
- **新增专属战斗牌「龙之咆哮」**（蛟龙立牌专属）：费用 **3**、耐久 **5**、攻击力 **+3**；命中目标时使其中**缓慢 III（1:00）**，并**减少 4 点防御力（1:00）**（重复命中只刷新时间，不叠加）。只能由处于**真龙形态**的蛟龙立牌玩家获得：进入真龙形态时，手中、背包里与**已装备在骰子里**的「撕咬」会**立即转换为「龙之咆哮」**（骰子卡牌栏内转换后**费用放不下的会被丢弃**）；同样是**专属牌**，只有获得者本人可以使用。

- **新增「飞星」系列筹码「紫色飞星」（史诗）与「金色飞星」（传奇）**：**路过敌对目标且不对其发动攻击**时，使其受到 **1 点**（金色飞星为 **2 点**）伤害，并使自身获得 **1 层「星光」**；金色飞星在目标为**精英怪物或 Boss** 时额外造成**自身当前「星光」层数**的伤害。**每 10 秒触发一次**，两枚筹码**共享同一个计时器**。伤害为真伤（无视护甲值与盔甲韧性），击杀归属玩家。粒子从目标**头顶上方**下落，**落到目标头顶即视为命中**并结算伤害；下落速度比原先**快 1.2 倍**（行程同步 ×1.2，而**总下落时间仍是 1 秒**，所以起点比原先更高）；两枚同时装备时**先落紫色粒子**，命中后**间隔 1 秒**再落金色。计时器**不创建任何效果**，剩余时间只在物品说明里显示。配方为**星光流派**（紫色：紫水晶碎片×3 ＋ 星币尘×2 ＋ 空白筹码 ＋ 星盘×3；金色：荧石粉×3 ＋ 星币尘×2 ＋ 空白筹码 ＋ 黄金星盘×3），并已进入创造栏、筹码汇总标签、饰品槽标签、**赏金奖励池**（紫色飞星；金色飞星为传奇不入池）与手册（含配方页）。
- **新增「星币钱包」**：星币与星币袋现在可以**存入钱包**（余额记在玩家数据上，**不受死亡掉落影响**）。物品栏**左上角**出现**星币钱包按钮**，点一下就把物品栏 36 格与副手里**全部**星币与星币袋按面额折算存入（**1 星币 = 1、1 星币袋 = 9**）；旁边另有两个**取出**按钮（星币 / 星币袋）—— **左键取 1 枚**、**Shift + 左键取尽可能多**（同时受钱包余额与物品栏剩余空间约束，取不出时不扣余额）。**新获得的星币默认直接进钱包**（物品发放与地面拾取两条路径都算；经商立牌用星光兑换的星币同样直接进钱包），而**从物品栏消耗星币的机制一律不变**（星币锤等仍只认物品栏与副手里的实物星币/星币袋）。界面由**一条余额条 + 三个按钮**组成：余额条是一块 **96×24 的深色圆角面板**，**右端右对齐显示当前钱包余额**（数值由服务端每 1 秒对账一次、**只在真的变了**才同步到客户端，因此无论是点按钮、捡星币、经商立牌兑换还是第三方模组改账本，余额条都会在一秒内跟上）；三个按钮各配**独立的悬停高亮贴图**（钱包按钮亮起暖金光晕、两个取出按钮整体转蓝框），两个取出按钮还带**原版物品栏槽位风格的边框**；界面原画（余额条与三张按钮贴图）由玩家提供，两版**逐字节一致**。控件位置与尺寸经过逐像素对齐，并**区分生存与创造两种界面**（创造模式只在「物品栏」标签页显示）。余额账本实现在前置库 `starengine_lib`（新增 `economy` 包与对外 API，供外部模组联动），调试命令 `/starcoin add|set|remove|get|rank`。新增 7 项配置：`enable_star_coin_wallet`（默认 `true`）、`deposit_star_coin_on_obtain`（默认 `true`，**仅在钱包启用时生效**），以及钱包按钮与两个取出按钮各自的 x/y 偏移。同时新增**不兼容列表**：检测到与本钱包功能完全重叠的第三方货币模组就识别为不兼容并**拒绝启动**（游戏停在加载错误界面）并提示玩家移除 —— 同时安装会让同一枚星币被两套系统各自记账。**2026-09-22 补充前提：只有在「星币钱包」处于启用状态（`enable_star_coin_wallet = true`，即默认值）时才拒绝**；把它设为 `false` 时本模组不再维护玩家级货币账本，与这类模组不再冲突，因此**完全放行**（启动时不做任何检查、不报错）。错误界面上的提示也据此改写成「两条出路，任选其一」——① 关掉 `[star_coin_wallet] enable_star_coin_wallet` 以共存；② 继续用钱包则移除该模组。⚠️ 因为判定要读配置，检查的执行时机从 **mod 构造阶段**移到 **common setup**（配置在构造阶段之后才加载，构造阶段读不到）；异常上屏的链路不变，玩家看到的仍是同一段文案。 **2026-09-22 修复**：① **切到创造模式后钱包栏不再整条消失** —— 原先把「只在『物品栏』标签页显示」的判据套在了**所有**控件上，而创造栏每次打开都会回到默认的「建筑方块」标签页，于是整条钱包栏被判定隐藏；现在**余额条与钱包按钮在创造栏的任何标签页都显示**，只有两个取出按钮仍限定在「物品栏」标签页（它们的位置压在原版物品格子上，挪到别的标签页会挡住格子）。② 同时修掉一处**控件可见性自锁**：旧实现在隐藏时把控件直接标记为不可见，导致**即使切回「物品栏」标签页也不会再出现、必须关掉界面重开**——这也正是「只有重新打开创造物品栏才会显示」的原因。③ **余额条同 tick 跟上**：点按钮存入/取出、拾取星币等**本模组自己**造成的余额变化改为**立即**推送到客户端，不再等下一次每秒对账（每秒对账保留为兜底，覆盖第三方模组与 `/starcoin` 命令造成的改动）。
#### 文案与手册
- **目标选择器的居中提示恢复并补全**：屏幕中央的那一行重新给出目标的**名称与距离**，并按敌我着色（友方绿 / 敌对红 / 中立黄）；本轮再追加**目标类型标签** —— 敌对生物 / 队友 / 宠物 / 中立 / 玩家（同队玩家、或自己拥有的宠物判为队友 / 宠物；敌对生物判为敌对生物；其余判为中立；非同队玩家判为玩家）。准星没有有效目标时中央**不显示**任何内容（与之前一致）。
- **目标选择期间的 actionbar 按键指引改为四态，并在末尾显示剩余时间**：未选中目标时用**白色**提示「请对准 <主动技能名称> 释放目标，按下 下蹲+右键 退出选择」；若该技能允许对自身使用，则改为「请对准 <主动技能名称> 释放目标，或按下 鼠标右键 对自身使用，按下 下蹲+右键 退出选择」。准星对准**正确**目标时转为**绿色**：「按下 鼠标左键 对其释放技能，下蹲+右键 退出选择」；对准**错误**目标（类型或距离不符）时转为**红色**：「只能选择 <有效目标>，下蹲+右键 退出选择」。上述四条末尾都用**黄色**追加「（剩余 N 秒）」，N 为本次选择会话的剩余秒数。技能名按动作显示为各自的名称（隐匿行动 / 虚弱印记 / 弱点反击），未登记的动作则直接显示其内部名。
- **立牌改名：看板 → 看板娘**：看板立牌（`mimi`）的玩家可见名称改为「**看板娘立牌**」—— 物品名（tooltip 标题行）、主动技能提示（actionbar）与手册「星光」页里的旧名一并统一；英文名按**日语罗马音的二次元写法**统一为 **Kanban Musume Sign**（此前英文三处写法各异：Board Sign / Noticeboard sign / Bulletin Board signs；本模组今后新增译名一律优先此风格，不再直译）。**仅改显示文案**：立牌 id、物品注册名 `mimi_sign`、类名、附件键、物品标签与配方全部不变，已存在的物品与存档不受影响。
- **全面清理立牌与筹码的 tooltip 与手册文案中的无效注释**:按「这句话玩家需不需要知道」逐条复核了**全部 143 个 tooltip**（筹码 69 + 立牌 74）与其对应的手册条目，删除四类无效内容 —— ① **全局设定**（同一规则已在别处 tooltip 说明过，如 `（汲取生效期间视为满血）`、`（护甲值向下取整）`）；② **实现细节 / 装备生命周期**（`（卸下筹码时清除）`、`（移除立牌后重置）`、记录上限、防刷机制、按键时序）；③ **自造术语**（`重置被动计数器` ⇒ 改为「本次攻击/防御点数取最大值」）；④ **自证式括注**（`（目标的骰点与防御牌加成仍然生效）` 这类读者无法据此决策的澄清）。**保留**影响收益预期或安全边界的说明（`优先于不死图腾与末影骰子`、`无视无敌的致死伤害同样有效`、`每类只触发一次、不叠加`、`卸下立牌不会清除已有的护盾`）与**排除项**（`（含专属牌）`/`（不含专属牌）`、`（需安装农夫乐事）`）。涉及筹码：紫色/金色飞星配方说明外的 `railgun`（删伤害公式括注与独立真伤行）、`warp_engine`（「不受该冷却限制」⇒「无冷却」）、`energy_recycler`、`whetstone`、`electric_sword`、`friendship_badge`、`revenge_halberd`、`candy`、`cursed_sword`、`cursed_sword_blue_curse`、`eight_sided_dice`、`electric_glove`、`hand_fan_big`、`magic_quiver`、`magic_tome`、`primordial_core`、`current_core`、`flashlight`、`cutter`、`cutter_blade`；立牌：`teru_passive`（删防刷机制整行）、`padman_active`、`padman_passive`、`pandaman_active`、`pandaman_passive`、`bonnie_active`、`bonnie_passive`、`nardis_active`、`rin_active`、`rin_passive`、`haiqing_active`、`komachi_active`、`parunan_active`。手册同步删除 `teru_sign.3` 整条（防刷机制）。
- **金色飞星配方改为「紫 → 金」进阶配方**:不再复用紫色飞星的基础图案，改为走通用升级模板（红石粉/钻石/上一等级 = 紫色飞星/黄金星盘）—— 与本模组其它进阶筹码一致。
- **删除调试命令重复别名 `resetcardcolddown`**:保留拼写正确的 `resetcardcooldown`（两者此前指向同一实现，属重复登记）。

#### 命令与调试

- **`/astralparty` 新增子命令 `finishsigncooldown [目标]`（立即结束主动技能冷却）**：把玩家级冷却附件 `sign_active_cooldown_end` 写成**当前时刻**，等价于「这一瞬间冷却正好走完」（与电流核心筹码的「立即完成冷却」同款写法），用于排障时立刻重试立牌主动技能。**只结束冷却计时**：不碰锁定（生效中）态 `sign_active_lock_*`、不碰冷却基准 `sign_active_max_cooldown`、也不碰蛟龙立牌（mamushi）的强制冷却硬闸门 —— 因此「技能仍在生效中」与「强制冷却窗口内」两种情况都不受影响（前者此时本来就没有正在跑的冷却：冷却要等生效中窗口结束才开始）。权限门槛（OP 权限级 2）、目标参数（可选 `@a`/玩家名，默认作用于执行者）与其它子命令完全一致，**不带任何数值参数**；执行后可用 `/astralparty dump` 的 `SIGN` 组核对 `sign_active_cooldown_end` 与 `sign_active_lock_*`。

### 内容与平衡性调整
- **忍术飞镖的加成条件放宽为「只要对目标造成远程/魔法伤害」**：原先还要求「已使用伤害类效果牌」（身上带着对怪激光 / 对怪板砖 / 轨道炮 / 定向爆破四个效果之一，或本次命中就是活体书页的法伤）—— 这使得先打出去的**普通箭矢 / 投掷物 / 联动模组法术**都拿不到「目标标记层数」的加成。现改为：**只要佩戴忍术飞镖、并对目标造成远程/魔法伤害，即按目标标记层数加成**（该修饰器本就只在法伤链内求值，「进入判定」本身已等价于「本次是法伤」）。tooltip 与手册条目已同步删去该前提。⚠️ 贯穿之铳（`PIERCING_GUN`）有相同的条件结构 —— 其加成前提与**目标判定**均已按同一裁决对齐（见上方「贯穿之铳」条目）。

#### 伤害与结算
- 修复**活体书页的命中跳字被覆盖、看不到法伤加成**（2026-09-24 用户实测「活体书页不吃标记伤害加成」）：加成本身**一直生效**（掉血数值正确），但客户端跳字是**每个实体一个槽位**（`Map<entityId, 数值>`，后到的包直接覆盖）—— 法伤修饰器链在 `hurt` 内先发出**含加成的完整伤害**，而活体书页在 `hurt` 之后又补发了一次**基础值**，把正确数字覆盖掉了。现把这次补发**移到 `hurt` 之前**：无加成时它就是最终值，有加成时被链内那次覆盖 ⇒ 两种情况都显示正确。⚠️ 仅修复显示，伤害数值未变。
- **活体书页改为「敌对目标选择器 + 高速飞行打击」,不再是被动的法伤增益牌**:使用方式改为**仅能对敌对目标使用**(敌对生物 ∪ 已被激怒的中立生物;不含玩家、不可对自己使用)——把牌拿在**主手**上即自动打开目标选择器(无倒计时;移出手持即关闭、未确认不消耗卡牌),**锁定范围 32 格且含垂直高度差**(目标在你正上方或正下方的坑里同样可选,不再只按水平距离算)。确认目标后,书页**以箭矢 4/5 的速度(2.4 格/tick,飞行时长由目标距离决定)高速飞向目标**,途中逐 tick 跟踪修正航线、**必定命中,且可穿透方块**(全程不做方块碰撞判定);飞行轨迹是一条**连续的发光粒子带** —— 粒子沿每 tick 的位移线段补点投放(不再只在每 tick 的采样点放一次,消除肉眼可见的分段/断点),此前的**深绿色光晕粒子已移除**;命中造成 **2 + 调查员(rin)已用页数** 点伤害,并施加 **1 层标记**。该伤害**登记为法伤**(新增伤害类型 `astral_dice:card_spell`,登记于 `minecraft:bypasses_armor` ⇒ 基础值**无视护甲值与盔甲韧性**,保护附魔与抗性提升仍会减免),因此受全部法伤加成影响 —— 忍术飞镖 / 贯穿之铳 / 紫晶骰子 / 标记喷罐 / 魔法箭袋 / 忍者立牌的「效果牌伤害增益」/ 书签**全部照常生效**。原有的「效果期间自身所有远程与魔法伤害 +2+已用页数」被动行为已**移除**;书页命中本身不进入骰战结算(与定向爆破、电击手套的范围伤害同一口径)。命中伤害数字改用**法伤绿**(与法伤加成跳字同色),不再跳普通伤害红;该牌改为**纯即时伤害**、**不再给玩家任何效果** —— 出牌后曾有的 1:00「活体书页」效果已整段移除(效果栏不再出现任何图标、也不冒药水粒子,忍术飞镖/贯穿之铳对书页命中的加成改按本次伤害类型判定,照旧生效);本轮冷却期间手持该牌**按下左键**会照自己和其它效果牌一样给出「本轮出牌数已用完!剩余冷却 N 秒」提示,不再毫无反馈。
- **伤害跳字的显示口径调整**：① **法伤跳字（草绿色）改为显示该次命中的完整伤害** = 原始伤害 + 法伤加成（四舍五入取整）—— 例：箭矢本身打出 6 点、法伤加成 4 点，现在跳 **10**（旧写法只跳加成的 4，与实际掉血不符）；② **攻击伤害与法伤跳字一律去掉「+」号**，直接显示数值。
- **活体书页的飞行特效不再遮挡第一人称视野**：发射起点从「眼睛位置」改为**沿瞄准方向前移 1 格**（目标更近时最多只前移到与目标距离的一半）；同时**凡落在任意玩家眼睛 1.25 格内的粒子一律不再生成**（拖尾、命中爆发与闪光一并适用）⇒ 第一人称下书页从镜头前方约 1 格处开始飞，白光不再糊在屏幕上。代价是**贴脸目标**（眼睛 1.25 格内）时看不到命中特效，伤害与标记结算照常。
- **新增公共配置项 `allow_firearm_damage`（默认 `false`）—— 枪弹/炮弹类伤害是否计入法伤改为可配置**：法伤模块对军火类弹丸（枪械 / 火炮 / 军火类模组等）的屏蔽此前是**写死的**：只要伤害类型标识或弹丸类名里出现 `bullet`/`gun`/`firearm`/`cannon`/`shell`/`missile`，该次伤害一律不计入法伤、也吃不到任何法伤加成。现在这一屏蔽改由公共配置 `allow_firearm_damage` 控制，**默认仍按原样屏蔽**（默认行为与之前完全一致，不需要做任何设置）；把它设为 `true` 后，这类军火弹丸**不再被关键词直接排除**，而是和箭矢/法术一样走正常的作用域白名单判定（例如继承原版箭矢类的弹丸会因此计入法伤、可享受法伤加成）。⚠️ 配置在**启动时读取**，改完需要重启游戏才生效。

#### 立牌与技能
- **充能流派固定效果改口径：有充能时立牌主动冷却至多 160 秒、效果牌冷却至多 20 秒**（原为「冷却 -20%」的按比例减免）：改为把**基础值**封顶 —— 立牌主动基础 180 秒 ⇒ 160 秒、效果牌基础 30 秒 ⇒ 20 秒；封顶之后再叠加其它减免（诡异骰子 -50% ⇒ 有充能时 160÷2 = 80 秒；枪匠「精密技巧」的 120 秒基础值低于上限 ⇒ 有充能时不受影响）。**已经在跑的倒计时不受影响** —— 只有新开始的冷却按新数值计算。旧的按比例减免常量与其入口已删除（共享前置库 `starengine_lib` 侧一并删除并升版）。

- **活体书页的「连续出牌」改为命中门槛**:出牌数 +1 不再在**使用**时无条件累加,改为**仅当命中前目标已有 3 层及以上标记**时补记 1 点本周期出牌数 —— 从 0 层起需要**第 4 次命中**才会首次触发(本次命中施加的那 1 层不计入门槛)。累加值仍严格受全局规则约束:**单轮出牌数封顶 9 张**。

- **史莱姆立牌的主动技能「治愈粘液」改为目标选择器技能**：按下主动键后进入目标选择，可指向任意玩家（**鼠标右键 = 对自身使用**）；确认后 —— ① 目标获得 **3 层治愈 + 瞬间治疗**（原为「自身」，现在跟着被指向的目标走）；② 以**目标为中心**，周围 16 格内的玩家/宠物/可骑乘生物获得瞬间治疗；③ 同一范围内敌对生物获得缓慢 (1:00)（原为这两项以**自己**为中心）；取消或 30 秒超时**不消耗冷却、也不发风扇筹码的牌**。被动「细胞分裂」（受到伤害 +1 层治愈、主动冷却 -10 秒）不变。

#### 物品、筹码与交易
- **四张「可对他人使用」的效果牌改为「手持即选目标」**：**加急加快**、**奢华大餐**、**你有我有**、**狂暴**。把卡牌拿在**主手**上就会自动打开目标选择器（**不需要按任何键**），**卡牌离开主手（换到副手/背包、丢弃或切换物品）立即关闭**，且此模式**没有倒计时**（不像立牌，立牌的选择会话仍有 30 秒窗口）。**确认之后才真正生效并消耗卡牌 —— 关掉选择器不消耗**；操作与立牌同一套：左键确认 / 鼠标右键对自身使用 / 下蹲右键或 J 取消，**取消后这张牌只要还在主手上就不会再次自动打开**（抑制闩，需先把它移出手持再重新拿起才会重新开启）。选择范围由原来的「准星 5 格内」统一为该牌的选择器半径（默认 16 格）；**狂暴改为只能对玩家或自己使用**（不再能对其他生物使用）；你有我有仍然不能对自己使用（按下鼠标右键只会提示「该技能无法对自己使用」，选择器保留）。旧方案里的「下蹲右键对其他玩家使用」路径已移除。卡牌 tooltip 相应改为**两行式**：第一行是精简用法「左键 对准其他玩家使用，右键 对自身使用」（你有我有不能对自己使用，只写左键那半句），第二行只写这张牌本身的效果，不再混写释放手势与消耗规则。
- **赏金奖励池不再提供「活体书页」与「命运的指引」，也不提供新出的「符卡-福」与「符卡-祸」**：这四张牌都是**专属效果牌**，而**专属效果牌不会进入任何赏金池** —— 专属牌只允许由对应的立牌发放（「符卡-福」「符卡-祸」只能从风水师立牌获得：骰点 1 / 6 的被动与主动「白泽赐福」），随机卡牌池与赏金奖励池一律不提供。两条发布线的赏金奖励池已把**原先就在池内**的那两张整条移除（新出的两张本就不在池内）—— 赏金奖励池不再刷出、也不再能兑换它们；其余非专属奖励与其它卡牌不受影响。
- **创造栏与手册中，专属符卡「符卡-福」「符卡-祸」移动到「命运的指引」之后**：两处顺序同步调整（创造栏中两卡紧跟「命运的指引」，手册条目的排序号紧随其后），原先排在它们后面的伤害效果牌（对怪激光 / 对怪板砖 / 轨道炮 / 定向爆破 / 活体书页）顺次后移。

#### 文案与手册
- **活体书页的说明文案精简为一句,并修正伤害数值未染色;手册条目同步重写**:卡牌 tooltip 现在只有一句 ——「瞄准敌对目标使用，对目标造成 §e<实时伤害>§7 点法术伤害，并施加一层“标记”。命中已有 §e3§7 层及以上标记的目标时，本出牌周期出牌数 §e+1§7」。原先单列的第一行用法「对敌对目标使用 活体书页」已并入这一句;飞向目标 / 必定命中 / 可穿透方块这些描述从 tooltip 撤下(完整口径仍写在手册条目里)。伤害数值随调查员已用页数**实时更新**,并修好了此前**未染色**(显示为灰色)的问题。手册条目按新的使用方式(手持即选、仅敌对目标、无倒计时)与门槛规则重写。目标选择器里该牌显示的技能名为「活体书页」。另新增该伤害类型的死亡提示文案(「被飞来的书页撕碎」/「被某人的活体书页撕碎」)。
- **26.1.2 的语言文件与 1.21.1 / 1.20.1 完全对齐，删掉 3 处只写在 26.1.2 上的实现细节注释**：这三处都不是玩家创建内容时给出的主动 / 被动原文，而属实现层说明 —— 能量回收筹码的「（单 tick 位移过大视为传送，不累计）」（手册条目与物品说明各一处），以及枪匠立牌被动多出的首行「装备时，主动技能冷却时间减为 120 秒」（该被动照旧生效，只是不再写进物品说明）。起因是 26.1.2 的语言文件此前按「缺键就补」的方式同步，**已有键的取值从不回改**，于是这三处一直留在该线上。现在三线（1.20.1 / 1.21.1 / 26.1.2）× 三语（中 / 英 / 日）共 **805 键逐键一致**，剩下的差异只有两处**平台性**差异：① 26.1.2 的按键分类键名是 `key.category.astral_dice.main`（由 26.1.2 的 `KeyMapping.Category` 决定的翻译键格式），1.20.1 / 1.21.1 为 `key.categories.astral_dice`；② 1.20.1 的**复仇长戟**清单不含 1.21 才加入的原版效果（蓄风 / 盘丝 / 渗浆 / 寄生 / 袭击之兆 / 试炼之兆）。同时修正 26.1.2 语言文件里 `msg.astral_dice.komachi_active_cooldown` 一行的缩进（8 空格 → 2 空格，与其余各行一致）。

### 已修复BUG

#### 伤害与结算
- 修复**活体书页首张命中伤害偏高（3 而不是 2）**：此前「调查员已用页数」在**使用卡牌时**就 +1，于是第一张书页命中时把这一页也算进了伤害（2 + 1 = 3），而 tooltip 显示的却是 2（不含本次）⇒ 显示与实际不一致。现改为严格按「**先造成伤害 → 再施加标记 → 最后累计页数**」结算：本次使用带来的那一页只让**后续**命中 +1 —— 第一张固定 **2**，其后 3 / 4 / …（佩戴调查员立牌时），tooltip 显示与实际命中数值一致。随之明确：**未命中的书页不再计入页数**（目标在飞行途中死亡 / 失效 / 换维度 / 超龄）。

#### 立牌与技能
- 修复**被激怒的中立生物无法被目标选择器选中**（2026-09-18 游戏内实测报告）：本模组的全局口径是「敌对目标 = 敌对生物 ∪ 已被激怒的中立生物」（唯一入口 `HostileTargets`），但目标选择器的可选中判定只认原版的「敌对生物」标记 ⇒ **被激怒的铁傀儡 / 狼 / 北极熊 / 蜜蜂**会被判成「对准错误目标」而无法确认（与主线版本口径不符）。现改为可选中判定统一走同一入口 —— 枪匠「弱点反击」的 `ENEMY` = 敌对生物 ∪ 已被激怒的中立生物；秘密侦探「隐匿行动」与占星师「虚弱印记」的 `ENEMY_OR_RIVAL` = 上述集合并上「非队友玩家」。客户端准星判定、客户端半径内其它目标的高亮、服务端确认三处判据完全一致（服务端仍是权威校验）。**不改变**任何技能的作用范围与效果，只是把「可被选中」的集合补齐。**注意**：被激怒的中立生物仍按中立配色显示（黄框 / 「中立」标签），与既有「标签与框色同源」的口径一致；杀伤类效果本身的敌对集合本就包含它们，二者现在一致。
- 修复**目标选择器立牌的专属提示被通用提示覆盖**：确认目标后，服务端先发出立牌自己的专属提示，紧接着又发出通用提示「已对 X 施加效果」，而屏幕下方的 actionbar 只有一格、后发者覆盖先发者 ⇒ 玩家**只看得到通用提示**（秘密侦探、占星师、游戏大师三条专属文案等于白写）。现在通用提示改为在施法**之前**发出，专属提示正常显示（游戏大师显示「熊孩子特权：已对 X 施加 1 张卡牌与鼠鼠护盾」）。
- 修复**多人服务器上离线后部分立牌的主动技能会提前解除「生效中」状态**：锁定的截止时刻按**绝对时间**计算，而技能效果的剩余时长在**离线期间不走** ⇒ 离线时长超过剩余锁定时间后重登，锁定会被判定为已结束、主动技能可提前使用（**经商立牌**的村庄英雄增益最长 15 分钟，最明显；护法、扫地机、吸血鬼、大当家、骇客、大侦探等同机制但时长短得多）。现在**锁定计时在离线期间一并暂停**（与效果同步：效果的剩余时长不走，锁定的剩余时间也不走），**忍者的宽限窗口**同样适用。另修复**骇客立牌**在跨离线重登后的三处判定：隐身仍在却会被生物重新索敌、攻击破隐的加成与效果会提前失效、本机视角的隐身渲染抑制会提前失效。
- 修复**手持效果牌时 ESC 菜单（暂停菜单）里所有按键都点不动**：效果牌是「手持即选择」，而打开界面时**只有立牌主动的会话会被取消**，效果牌会话被刻意保留（好让你按 ESC 只是临时盖住提示、关掉界面继续选）—— 但旧实现会在会话存活时**取消每一次鼠标按下**，导致原版暂停菜单收不到任何点击（背包、卡牌栏、聊天栏同样受影响）。现改为**界面打开时鼠标一律交回界面**：菜单按钮可正常点击、也不会顺手确认目标；关掉界面后选择会话照常继续（原有的「移出手持即关闭」与「立牌主动按 ESC 即取消」口径不变）。
- 修复**手持效果牌时滚轮被锁死、无法切换物品栏槽位**：效果牌是「手持即选择」（把牌拿在主手上就自动打开目标选择器），而旧实现把**所有**选择会话的滚轮一律拦截 ⇒ 玩家拿着加急加快 / 奢华大餐 / 你有我有 / 狂暴 / 活体书页时滚轮完全没用，牌像被焊在手上（既切不了槽也换不了物品）。现改为**只对立牌主动（按键开局）的会话拦截滚轮**；效果牌的会话**放行滚轮** —— 滚一下即可换槽，牌离开主手即退出选择（与既有的「移出手持即关闭」口径一致，取消后仍不会自动重开，需先移出手持再重新拿起）。
- **修复三种立牌因合成表重合而合不出来**：立牌的合成结果由「形状 + 材料」唯一决定，而**风水师 / 教主**、**绿洲女王 / 上班族**、**蛟龙 / 大当家**三对立牌此前共用**同一张**合成表 —— 同一个摆放游戏只会给出其中**一种**结果，于是每一对里总有**一种立牌永远合不出来**（在配方查看器里点它，合成台给的仍是另一种）。现在三对各自改用独立材料：**教主**改用**荧石**、**绿洲女王**改用**仙人掌**、**蛟龙**使用**海晶灯**（风水师 / 上班族 / 大当家的配方保持不变）⇒ 这六种立牌全部可正常合成，配方查看器显示与实际产出也一致。
- 修复**把鼠标指向「教主立牌」时游戏直接崩溃**（2026-09-21 实机报告）：该立牌的两条技能说明里的百分号（**50%**）写法不合法，游戏在绘制物品提示时解析失败、**当场崩溃** —— 创造模式物品栏、自己的背包、配方查看器等**任何会显示该提示的地方**都会触发（风水师立牌及本模组其它立牌、筹码、卡牌、效果牌的同类文案经逐条核对**均无此问题**）。现已修正该立牌的全部相关文案（中英同步），并给物品提示的绘制加上兜底：**今后即使文案再写错，也只会把该字符原样显示出来，不会再崩溃**。
- 修复**四个立牌的被动说明里重复了技能名**（大当家、风水师、怪力侦探、人偶师）：被动标题由界面单独给出，而正文首行又写了一遍技能名 ⇒ 同一名称连续出现两次。现删去正文里重复的名称前缀（大当家的条件句改为「拥有该效果时」，语义不变）。
- 调整**三条合成配方**（2026-09-23）：① **以毒攻毒**效果牌的无序配方由「谜之炖菜 + 红色蘑菇 + 兔子脚 + 星盘」精简为「**红色蘑菇 + 星盘**」（移除谜之炖菜与兔子脚）；② **教主立牌**的填充材料由**荧石粉**改为**荧石**；③ **蛟龙立牌**的填充材料由**海晶碎片**改为**海晶灯**。后两条只替换填充材料，配方形状、档位与产出不变（合成台与配方查看器显示同步）。
- 调整**怪力侦探立牌**主动「怪力投掷」的四项口径：① **落地伤害改用法术伤害** —— 原按「玩家近战」结算，会意外进入骰战（吃骰伤）并触发与近战伤害相关的筹码；现走本模组的法伤类型 `astral_dice:card_spell`，并显示**法伤颜色**的伤害数字。② **不再把墙后的目标拉出来** —— 候选目标须**视线可达**，被方块遮挡的一律不投掷。③ **落点改为「准星优先 + 自动回退」** —— 准星射线在 **1–6 格**内命中**地面或水面**时用该处；瞄准**更远处或天空**时自动改丢到**面前 6 格**；若落点被**掩体或高方块**挡住（自眼睛到落点的路径被挡、或该处地面之上不足 3 格净空），则**向内逐格靠近**重试；只有当**面前 1–6 格全部不可落**（被方块堵死）时才在动作栏提示，且**本次主动零消耗**（不进冷却、不充能、tooltip 文案同步为「将 12 格内所有敌对目标扔向面前 **1-6 格**的位置」（不再写「准星指向的地面」）。④ **落地后再冻结目标 AI 2 秒** —— 落点就在玩家面前，恢复 AI 会立刻反手攻击甚至击杀玩家；解冻时同时清掉其索敌目标。
- 收紧**飞星筹码**与**人偶师立牌**「路过」判定的范围口径：原判据只把一次三维膨胀的盒（`getBoundingBox().inflate(半径)`）当作命中，其竖直容差达「脚底 −3 … +4.8 格」、**比水平还宽** ⇒ 站在 4 格高的台子上仍会命中地面的怪/玩家（实机取证：+4 格被接受、+6 格被拒绝）。现改为「**水平圆柱**（水平距离 ≤ 3 格）+ **同高度窗口**（脚底高差 ≤ 2 格）」两级判据，「取最近」也改用水平距离与门限同口径 ⇒ 上下台阶（含跳跃约 1.25 格）照常触发，**跨 2 格以上的高台/坑洞不再触发**。

#### 物品、筹码与交易
- 修复**装备骰子时筹码栏偶尔不增加**（表现为装备后要等约 1 秒才出现，甚至当场看不到）：此前「装备骰子 → 立即获得对应数量的筹码栏」这一步实际从未生效，筹码栏只能靠每秒一次的兜底检查补上 —— 装备的瞬间（或刚打开饰品槽界面时）看到的仍是 0 格，过一秒再看才出现，因此像是「偶发不增加」；若在这一秒内退出游戏，这次应得的筹码栏位还会被当作 0 存进存档。现在装备骰子会**立即**按骰子品阶与星级给出筹码栏位，并在**进入世界 / 数据包重载 / 死亡复活**后各对账一次；筹码栏位数改为由本模组直接声明（不再用累加方式改写），因此**旧存档的残留数值、饰品槽被管理员重置、同步丢失**等情况都会在 1 秒内恢复到应有的格数，不会再出现「该有 2 格却一直是 0 格」或「格数越用越多」。换骰子时**不会**因为读数瞬时为空而把栏内已放的筹码弹出（栏内还有筹码时保持占用它的格数，直到你自己取走）。
- 修复**卡牌（含符卡）的多行说明中间出现方块乱码**：原版只在「放不下、需要折行」时才拆行，放得下时整段按**单行**绘制 ⇒ 说明里用作分行的换行符被渲染成**方块占位符**（是否出现还取决于屏幕宽度与鼠标位置，看着像随机）。现改为逐行拆分后添加，多行说明在任何位置都不再出现方块。

### 安装要求

- ✅ **本版本把前置模组 StarEngine Lib 内嵌进了本模组 —— 玩家无需再单独安装**：产物内自带一份 `starengine_lib`（内嵌于 `META-INF/jarjar/`），由加载器在启动时自动载入，你只要把本模组 jar 放进 `mods` 目录即可。
- ⚠️ **请不要再单独放置 `starengine_lib-*.jar`**：加载器按 modId 去重时**优先采用 `mods` 目录里那一份、丢弃内嵌副本** ⇒ 如果那份是**更旧**的版本，就会盖掉本模组自带的新库（表现为 `NoSuchMethodError` / `NoClassDefFoundError`）。此前手动装过库的，请把它删掉。
- **内嵌版本 = `1.0.3`，兼容区间 `[1.0.3,2.0)`**（与 `mods.toml` 里声明的前置区间同源）：该库仍是**必需**前置（自 1.3.0 起本模组大量共享实现位于其中），只是改为随本模组一起分发。
- 库的源码仓库：<https://github.com/merlin-kitsune/starengine_lib>。

## 2.0.0-SNAPSHOT.5

### 内容与平衡性调整

#### 立牌与技能
- **占星师 / 秘密侦探 / 枪匠的目标选择改为「前置门控」:按下主动键只开启选择会话,其余反馈全部推迟到确认目标之后**:此前按下主动键会**立即**走完原流程的剩余步骤 —— 发放手持风扇-大/小的效果牌、触发立牌主动响应事件(占星师与秘密侦探显示默认提示「<立牌名>:主动技能已启动!」,枪匠显示「请选择敌对目标向其施加『破绽』」),而随后取消选择或 **30 秒**内未确认并不会收回这些反馈(等于什么都没发生却白拿一张效果牌、还看到「已启动」)。现改为:按下主动键**只**开启目标选择会话(会话时长仍取自 `GameplayConstants.SKILL_WAIT_SECONDS`,不写死数字),发牌、主动响应提示、玩家级冷却/锁定与电流核心充能、技能效果**全部**推迟到**确认合法目标之后** —— 确认时由该次释放的动作写入冷却与充能,再由恢复点发牌并抛出主动响应事件;取消 / 超时(窗口时长不变) / 会话被新会话替换 / 登出 / 死亡一律清除待执行记录,该次主动等同「未使用」:不发牌、不提示、不进冷却、不消耗电流核心充能。枪匠的「请选择敌对目标向其施加『破绽』」提示改在**会话开始**(按下主动键)时发送,不再挂在确认之后的主动响应事件上(否则会在「破绽」已施加之后再要求玩家选择目标)。三个立牌之外的立牌、效果牌流程逐字不变(双版本一致)。

### 已修复BUG

#### 立牌与技能
- 修复**1.21.1 版加载模组时直接崩溃、根本进不去游戏**（2026-09-17 实机定位）：枪匠立牌（`MosesSignItem`）在「目标选择前置门控」改造后**不再注册任何事件订阅方法**，但类上残留了加载器的自动订阅标注 —— 1.21.1 的 NeoForge 对「标注为订阅者却没有任何订阅方法的类」会在**模组构造阶段**直接抛异常（`class … has no @SubscribeEvent methods, but register was called anyway`），继而 `Failed to register automatic subscribers` → `Failed to wait for future Mod Construction`，整个模组被拒绝加载（表现为启动即停在 `Mod Construction` 失败，与玩法无关）。现删除该残留标注，并在原处加注释说明本类**不得**再标注订阅者以免回归；1.20.1 侧同源的空标注一并清理（Forge 对该情形不抛异常，故此前从未暴露，但同属死标注）。修复后 1.21.1 实机冷启动正常进入世界。

## 1.2.1-hotfix



## 1.2.1

### 新内容

- **新增 `/astralparty` 管理员调试命令(4 个子命令 + 2 个别名,仅 OP 权限级 2)**:`cleareffect` 清除**全部 33 个**本模组效果;`clearcardeffect` 只清**效果牌施加的效果**(= `EffectCardPeriod` 的「效果待定」注册表 `EFFECT_PENDING_SOURCES`,共 9 个,即出牌锁第 ③ 条的来源);`resetcardlock` 重置出牌锁的**出牌数**(含全部「每轮一次」标记)与**出牌冷却**,其别名 `resetcardcolddown`(原话拼写)与 `resetcardcooldown`(拼写正确版)与主字面量**共用同一实现**;`dump` 是**只读**子命令:一条命令把关键状态倾倒成 `APDUMP|<组>|<键>=<值>` 行(组固定为 `HEAD`/`LOCKRAW`/`LOCKDERIVED`/`SIGN`/`EFFECTS`/`PENDING`),同时走玩家 chat 与 `latest.log` 两条通道(后者保证自动化能读到),行内不含颜色码(§ 会破坏正则)、数值十进制、tick 用 long、布尔 `true/false`,多玩家按选择器顺序逐段输出且段内行序固定(不依赖任何 `HashMap` 迭代序);输出**原始值优先于派生值**,`LOCKDERIVED` 组与 `is_sign_active_locked` 是**判定入口字段**(行尾标 `assert=forbidden`)、**禁止作为断言落点**;其目的正是替换「纯读探针」代码,让 PowerShell 侧直接对日志做正则断言。`dump` **不改变任何状态**(只经 getter 与 `is*` 判定,不输出原版或其它模组的状态)。四个子命令均 `requires(src -> src.hasPermission(2))`,**只在服务端**注册与执行,**随 jar 发布**、不设 dev/测试专用开关。能力边界严格限定为「重置 / 清除本模组自身状态」:不给予任何资源(物品/卡牌/筹码/星币/星光/治愈/最大生命),不提供「把任意数值设成任意值」的通用写入(没有 `seteffect`/`setcooldown`/`setplaycount` 之类入口),默认只作用于执行者、给了玩家选择器才作用于该玩家(绝不隐式作用于全体),不触碰原版或其它模组的玩家状态(如光谱箭施加的发光)。目标参数可选(`@a`/玩家名),执行者不是玩家且未给参数、或选择器解析不出玩家时返回可读失败文案而不抛异常。实现要点:效果移除统一经内部通道 `ModEffectRemoval`(外部清除拦截器会取消全部 `astral_dice:` 效果的移除,直接 `removeEffect` 会让命令完全无效;走内部通道同时自动遗忘计时器守卫的 `effect_timer_ends` 记录——否则守卫会把效果原样施加回来——并自动清 `undercover_source`),并在移除后清理与所清效果**同生共死**的耦合状态(`fate_active_until`、`weak_mark_source`、`sign_ready_type`/`sign_ready_expire`);`healing_points`/`healing_timer_end`、`investigation_stage`、`cursed_sword_*`、`empower_decay_at` 等资源与进度计数器,以及立牌锁定态 `sign_active_lock_*` 一律**不动**(锁定态由玩家级 tick 自动迁移为冷却)。新增只读入口且两版本同步:`ModEffects.ALL`(由 `DeferredRegister#getEntries` 的活视图派生,新增效果自动纳入)、`EffectCardPeriod.effectPendingEffects()`(由效果待定注册表去重派生)与 `EffectCardPeriod.effectPendingSources()`/`effectPendingSourceIds()`(同一注册表的逐条只读视图 + 稳定来源标识,供 `dump` 输出每个来源自己的 `isActive`);反馈文案走新增的 8 个 lang key(中英 × 双版本)。

### 内容与平衡性调整

#### 伤害与结算
- **电磁炮**:雷击伤害改为 **5 + 本次攻击(骰战最终)伤害 × 50%**(下限 5 点;此前为"本次攻击伤害的 50%、下限 5 点"),由 `STRIKE_DAMAGE_BASE`/`STRIKE_DAMAGE_RATIO`/`STRIKE_DAMAGE_MIN` 三个常量共同表达,实际数值经原版 `LightningBolt#setDamage` 覆写(两版本同源)。雷击的**伤害类型同时改为真伤**(`astral_dice:true_damage`,登记于 `minecraft:bypasses_armor` → **无视护甲值与盔甲韧性**;保护附魔与抗性提升仍会减免):原版 `minecraft:lightning_bolt` 不在该标签内(实机核对),故新增 `EntityThunderHitMixin` **只对电磁炮自己降下的闪电**替换伤害源(标记集合 `RailgunBolts`,在 `RailgunChipItem#strike` 中登记),雷击的**命中范围同时收窄为「仅敌对生物与已被激怒的中立生物(如被攻击后的末影人)」**——自己、友方宠物、中立动物、盔甲架等一律整段取消(既不受伤也不被点燃;原版闪电对这些实体是照打不误的)。原版闪电的其余行为(点火、闪电苦力怕、僵尸猪灵/女巫转化、`onEntityStruckByLightning` 事件)一律不变;并补齐该伤害类型的死亡消息文案(中英 × 双版本)。**命中范围的实施位置(2026-09-14 补充,独立验证发现并修复)**:白名单已从 `Entity#thunderHit` **上移到 `LightningBolt#tick` 的目标筛选**(新增 `mixin/LightningBoltStrikeScopeMixin`,`@ModifyArg` 把原谓词 `Entity::isAlive` 与白名单相与,定点依据已用 `javap -c` 从两版本合并 jar 核对),因为海龟/村民/猪/蘑菇牛**覆写了 `thunderHit` 且不调用 `super`** —— 旧实现只在 `Entity#thunderHit` 里 `cancel`,对这些覆写类完全无效(海龟仍被 `Float.MAX_VALUE` 秒杀、村民仍转女巫、猪仍转僵尸猪灵、蘑菇牛仍换肤),且 `onEntityStruckByLightning` 事件与 `CHANNELED_LIGHTNING`("Very Very Frightening")成就也不受实体白名单约束(成就吃的是同一份实体列表)。上移后非敌对目标**在进入循环之前就被剔除**:不受伤、不转化、不触发事件、不进成就;方块着火/避雷针等世界行为按原版保留。同时**排除雷击施放者自己拥有的宠物**(`OwnableEntity#getOwnerUUID()` == `bolt.getCause().getUUID()`)——被激怒的已驯服宠物(如自己养的狼)不再挨自己的雷击。**命中范围的敌对判定已对齐全局双参口径(2026-09-15)**:判定改用带上下文的 `HostileTargets.isHostile(viewer, target)`,其中 `viewer` = 落雷来源玩家(`bolt.getCause()`,由 `RailgunChipItem#strike` 写入),故**你视为敌对的玩家**(非同队伍、且曾主动攻击过施放者)同样会被电磁炮落雷命中,与其它范围/波及效果的口径完全一致(此前单参判定下玩家一律不计入);无闪电实例的退化重载拿不到来源玩家,玩家仍不计入敌对。
- **伤害效果牌(法伤)改走真伤**:远程/魔法命中的效果牌加成不再并进本次伤害事件(并进去会连同武器伤害一起吃护甲值/盔甲韧性/保护附魔的减免),改由 `ModDamageTypes.trueDamage(level, player)` 单独结算——**只有效果牌那部分穿甲**,基础武器伤害照旧。定向爆破与电击手套的范围波及伤害同步改真伤。
- **统一「敌对目标」判定口径**:新增唯一入口 `combat/HostileTargets.isHostile(Entity)`,口径 = **敌对生物(`net.minecraft.world.entity.monster.Enemy`)∪ 已被激怒的中立生物(`NeutralMob#isAngry()`,即 `getRemainingPersistentAngerTime() > 0`)**;双版本各 **24 处**玩法判据点(贯穿之铳门、定向爆破/电击手套/大当家溅射/标靶/手持风扇小大的范围筛选、调查阶段增益、嘲讽反击、诅咒之剑/电流剑/智能手表/探天卫星/手电筒/肾上腺素/电磁炮的触发与击杀计数、史莱姆「治愈粘液」的缓慢、骇客的解除隐身与被动类型、秘密侦探「关键线索」)全部改调该入口。此前只有电磁炮雷击一处做了 anger 判定,**被激怒的狼/铁傀儡/北极熊/蜜蜂**(全原版 `NeutralMob` 直接实现者仅 6 个,其中只有这 4 个不是 `Enemy`)在其余各处一律被漏掉;末影人/僵尸猪灵/猪灵等"敌对类中立生物"本身就是 `Enemy`,一直计入,且**平静(未激怒)时同样算敌对目标**(本次裁决维持现状)。顺带修正 **5 处用全限定名 `net.minecraft.world.entity.monster.Enemy` 绕过统一入口**的历史写法(定向爆破 AOE、标靶最近敌对目标、大当家溅射、骇客解除隐身、秘密侦探关键线索);**战利品注入池**(`LootInjectionHandler` 的 `instanceof Monster`,怪物类 0.3% 掉星盘)属掉落池口径、与"敌对目标"无关,刻意不改。
- **标记层数上限由 16 层提高到 32 层**:标记(`marked`)的可叠加上限现为**固定常量** `GameplayConstants.MAX_MARKER = 32`(原为配置项 `max_marker`,该配置项已删除,见下一条);施加标记的唯一入口 `MarkManager.apply` 仍按 `MAX_MARKER - 1` 钳制效果等级,故层数上限 **32 层**(层数数字由原版标签 `enchantment.level.N` 提供,**仅在 2~10 层(amplifier 1..9)时**显示在物品栏效果面板的标签与悬浮提示上,11 层及以上不再显示数字;原版 HUD 只画图标、不显示层数)(双版本一致)。
- **真伤结算不再被无敌帧削减或吞掉**:新增数据包标签 `data/minecraft/tags/damage_type/bypasses_cooldown.json`,把 `astral_dice:true_damage` 登记进去(**原版 1.21.1 与 1.20.1 的 client-extra 资源 jar 里都没有这个标签文件,故本模组的文件即其定义**;已逐条核对两版本 jar 的 `data/minecraft/tags/damage_type/` 目录),`LivingEntity#hurt` 里 `invulnerableTime > 10` 的早退分支因而不再对真伤生效(两版本同一处判据:1.21.1 `LivingEntity.java:1190` / 1.20.1 `:1131`)。受益者:效果牌法伤加成、定向爆破与电击手套的范围波及、大当家溅射 —— 它们都以嵌套 `hurt(true_damage)` 结算,恰好落在原版刚写好的 20 tick 无敌帧内,`加成 ≤ lastHurt` 时被整段丢弃(实测:箭 1 点 + 激光 +4 只落地 3 点)。**副作用如实写明**:电磁炮雷击走的是**同一个** `astral_dice:true_damage` 类型,因此也一并穿透无敌帧;`astral_dice:dice_damage`(反击注入与绯红骰/王之力自伤)**未**纳入该标签,行为不变。
- **敌对玩家规则(全局,2026-09-15 用户裁决)**:非同队伍、且**主动攻击过你**的玩家一律视为敌对目标(记录见新增的 `combat/PlayerHostilityTracker`,服务端内存静态表),故**大当家溅射与电磁炮落雷(2026-09-15 对齐,`viewer` = 落雷来源玩家 `bolt.getCause()`,见「电磁炮」条)现在也会对这类玩家造成同样效果**;`HostileTargets` 新增带上下文的 `isHostile(Entity viewer, Entity target)` 重载(原有单参重载语义不变,玩家不计入),大当家溅射、调查阶段增益、贯穿之铳门等调用点改用两参重载。**记录排除**本模组内部的范围/波及结算(`DiceCombatEvents.isInternalAoe()`)与反击注入(`isInCounterChain()`),也不记录自伤与任一侧非玩家目标的情形;玩家**死亡**(LOWEST,晚于保命方的"取消死亡")、死亡重生克隆与**退出服务器**时清除该立场(作为攻击方与作为受害方的记录一并清除,不持久化、服务器重启即清空)。

#### 立牌与技能
- **大当家立牌·溅射改为被动单次效果**:触发骰神赐福时若养精蓄锐已满 5 层,则**消耗 2 层**(替代常规的 -1 层,且这 2 层在溅射**真正生效时**才扣除),并在**本次攻击伤害定稿后立即**对**目标及其 6 格范围内(含目标本身)**的敌对目标造成本次攻击伤害 **88%**(**下限 5 点**)的伤害;伤害类型改为**真伤**(`astral_dice:true_damage` → **无视护甲值与盔甲韧性**,保护附魔与抗性提升仍会减免),命中时附带爆炸粒子与音效(**仅视觉表现**)、**不破坏方块**、**无友伤**(只命中敌对目标)。同时**修复溅射对主目标不生效**的缺陷:原版 `LivingEntity#hurt` 先用「骰战结算前的武器伤害」写好 `lastHurt`/无敌帧、再触发本模组的伤害事件,溅射因此落入"无敌帧内不更低的伤害被丢弃"分支而整段消失(实机:主靶只掉近战 3 点、5 点溅射凭空消失);现只对主目标临时清零无敌帧再结算,实测主靶恢复为「近战 + 溅射」全额。旧实现(主动消耗 2 层 → 下次赐福期间每次攻击持续扩散,6 格、其他敌对目标)已移除,主动技能不再消耗层数。
- **出牌轮收尾口径(2026-09-15 用户裁决)**:本轮**所有效果计时器结束后**同样进入一轮 30 秒冷却 —— 判据为出牌数未达上限且 `EffectCardPeriod.getRemainingBlockTicks(player) <= 0`(剩余被锁时长由已注册的"效果待定"源自动推导,不硬编码效果列表),时长与"打满上限"复用同一 `ChargeManager.cooldownTicks` 口径;但**不作废剩余出牌数**:该分支不把计数补齐到当轮上限,`isBurstFull` 因而保持为假,冷却期间仍可继续出牌,冷却到期后计数归零。"打满上限即进入冷却"的不变量不变;另规定一轮冷却**已在跑**时不得被冷却期间的补牌重启或延长(否则补得越晚冷却越长、持续出牌时周期永不结束)。同时**死亡路径不再调用 `EffectCardPeriod.clearRoundBonuses`**(相关附件键都不是 `copyOnDeath`,新实体上本就是默认值),该入口的调用点收敛为 `registerPlay` 的周期边界、`tick` 的周期结束与忍者宽限强重置 `forceResetRound` **三处** —— 2026-09-15 起三者共用同一套清理项:除一次性出牌数加成 / 可口糖果 / 探天卫星 / 活体书页本周期累计外,**电击手套本周期已武装的法伤扩散(附件 `electric_glove_aoe`)也统一在 `clearRoundBonuses` 里解除**(`ElectricGloveChipItem.disarmAoe`,下个周期可重新武装;此前该调用只写在 `tick` 情形 1,于是忍者宽限期满走的 `forceResetRound` 不解除武装,两条"轮次完全重置"路径的清理口径不等价)。
- **立牌主动技能改为「三态」:可用 → 锁定(生效中)→ 冷却 → 可用**:此前主动技能只有「可用 / 冷却」两态,凡施加带时长效果的主动一按下即起冷却,效果还在跑时冷却已经走完,可以立即再次触发。现细化为三态——主动触发成功后,若本次**确实施加了带时长效果/自身计时器**,则进入**锁定(生效中)**态:必须等**该技能施加的全部计时器跑完**(触发时刻算定的全部到期刻取 max,作为门控硬上界)才允许进入主动技能冷却,锁定期内**按键无效**并提示「%s:主动技能生效中!」(新键 `msg.astral_dice.sign_active_in_effect`);锁定结束**必起冷却、无空档**。判定入口为 `BaseSignItem.isSignActiveLocked`(硬上界未到 **且** 本立牌门控效果实例仍在),结束迁移为 `endLockAndStartCooldown`。不施加带时长效果的主动与三类待命技能(占星师/秘密侦探/枪匠)行为与改动前逐字一致(仍为"触发/释放即起冷却")。
- **需上锁的 7 个立牌 / 不上锁的 9 个立牌**:**上锁 7 个** —— 经商 `parunan`、扫地机 `jasmine`、护法 `misaki`、大侦探 `fanny`(**仅实际施加带时长效果的分支**,只发物品的分支不起锁、直接进冷却)、吸血鬼 `papara`、骇客 `nancy_lu`、大当家 `fen`;这些立牌的主动会施加自身侧带时长效果(或写入自身侧到期刻),触发后进入锁定态。**不上锁 9 个**及原因:看板 `mimi` / 调查员 `rin` / 上班族 `padman` **纯发牌**(不产生任何计时器);史莱姆 `lulu` / 肉弹战车 `pandaman` **仅目标侧效果**(敌对目标身上的缓慢/嘲讽,不代表施加者身上的主动仍在生效中);占星师 `haiqing` / 秘密侦探 `bonnie` / 枪匠 `moses` **仅 30 秒待命窗口**——判定**不算**本技能的计时器(待命期是"等待选目标的窗口"而非技能效果时长,且超时未命中不消耗冷却的现行设计必须保持,故不进入锁定态);忍者 `komachi` **无任何计时器**,改由下一条专用规则接管。
- **锁定期间使用的减免改为累计入池(不再改当前冷却)**:锁定态的主动**不写** `sign_active_cooldown_end`,故「命运的指引」「加急加快」(扫地机被动)与史莱姆 −10 秒这三类按比例/绝对量减免在此期间无处可改。现改为:锁定期间使用这些减免时**累计入池**(附件 `sign_active_reduction_pool`),等锁定结束、**真正进入冷却时一次性抵扣** —— **实际冷却 = 起冷却时的实际基准 − 累计池**(`effective = max(0, 基准 − 池)`),抵扣后池归零并把 `sign_active_max_cooldown` 改写为 `effective`;基准仍在**锁定开始时**就写进 `sign_active_max_cooldown`,故各减免方读取的键不变、算式不变(最小 diff)。非锁定态的减免行为与改动前逐字一致(仍直接改冷却结束时刻)。
- **忍者立牌专用:锁定跟随出牌周期 + 1:00 宽限保险**:忍者主动「忍术连击」本身不施加任何计时器(只把当前出牌轮出牌数 +1),故触发后**不立即进入冷却**,而是让**锁定跟随效果牌周期** —— **冷却从"该轮出牌状态完全重置那一刻"开始**(`EffectCardPeriod.onRoundFullyReset` → `BaseSignItem.onEffectCardRoundReset`,重置点仅 `tick` 情形 1 与 `registerPlay` 的周期边界两处)。另设**宽限期 1:00**(自触发主动起算):期内**自始至终未出任何效果牌** ⇒ **强制重置出牌状态并让主动技能进入冷却**(`EffectCardPeriod.forceResetRound` + 起冷却);一旦出过效果牌 ⇒ 宽限保险失效、改为遵循出牌周期自然重置(出牌佐证写附件 `sign_active_lock_played`,在 `registerPlay` 置真且**跨周期边界保持**——不能读 `effect_card_play_count`/`effect_card_bonus_plays`,两者都会被周期边界归零)。
- **电流核心筹码:锁定态严格禁用**:主动技能处于锁定(生效中)时,电流核心的「耗充能立即完成冷却」**不可触发、不消耗充能**,并抛与三态化**同一条**提示「%s:主动技能生效中!」——按键在 `BaseSignItem.performSkill` 的锁定分支即被拦下(该分支判定在冷却分支之前),`CurrentCoreChipItem.tryFinishCooldown` 另加同判据作纵深防御;该筹码**仅在真实冷却态**可耗充能立即完成冷却,完成后仍需再次按键释放技能(既有行为不变)。

#### 物品、筹码与交易
- **安全气囊:保命范围重定为「无视无敌」的致死伤害(虚空伤害例外)**:`ChipDamageHandler` 的伤害阶段不再按 `DamageTypeTags.BYPASSES_INVULNERABILITY` 排除伤害源(原口径为「无视无敌的致死伤害不参与」),因此 `/kill`(generic_kill)与其它模组的真伤等**无视无敌的致死伤害同样会被安全气囊无效化**;**但虚空伤害(`DamageTypes.FELL_OUT_OF_WORLD`)例外** —— 虚空击杀优先级最高,气囊与磨刀石的不可击杀保护一律不阻止(刻意只按伤害类型排除 `out_of_world`:同一个 `bypasses_invulnerability` 标签还含 `generic_kill`,一律查标签会把 `/kill` 也变成不可救)。致命判定统一按**吸收(黄心)之后**:1.21.1 的 `LivingDamageEvent.Pre` 派发于吸收之前(反编译源码 `LivingEntity.java:1789` 早于 `:1790-1792`),故按 `damage - getAbsorptionAmount()`(下限 0)换算后与生命值比较;1.20.1 的 `LivingDamageEvent` 在吸收之后派发(`:1669-1670` 早于 `:1680`),天然达标、不做换算。保留**死亡事件兜底**(`EventPriority.HIGHEST`,早于末影骰子的死亡处理):若致死绕过了伤害管线(如外部直接把血量清零),气囊同样取消死亡并把血量抬到 1。其余口径不变 —— 仍按最终伤害判定、仍消耗 6 层充能并进入 1:00 冷却,充能不足或冷却中不生效;tooltip 与帕秋莉手册保持「无视无敌的致死伤害同样有效(如指令 /kill)」的通用写法(未逐字写明虚空例外,本批不改文案)。

#### 状态、存档与同步
- **配置项精简:5 项回归固定常量**(公共配置现只剩 7 项):删除 `max_starlight`、`max_marker`、`effect_card_cooldown_seconds`、`max_effect_stacks`、`hand_fan_big_range` 五项,全部改用 `GameplayConstants` 的 **`static final` 常量**(依次 32 / 32 / 30 秒 / 3 层 / 16 格);消费方(`StarLightManager`/`MarkManager`/`EffectCardPeriod`/`BerserkCardItem`/`EffectCardItem`/`UnwaveringCardItem`/`FanBigChipItem`/`ModTooltipHandler`)本就只读这些常量,故**数值与行为完全不变**。公共配置只剩 `config_version`、`give_guide_book_on_first_join`、`[event_system]` 三项队伍开关、`[actionbar]` 两项时长。已存在的配置文件里这 5 个旧键由加载器剔除(文件版本低于 `CONFIG_VERSION` 时启动会先备份为 `.bak`);**配置版本号按用户裁决不递增**——规则是「只有修改了配置项才 +1」,当前版本保持 2(双版本一致)。

#### 文案与手册
- **高级外设**:文案由「触发骰神赐福时:充能 -1」统一为「**消耗 1 层充能**」(tooltip 与帕秋莉手册同改)。实现本就经 `ChargeManager.consume` 扣层,佩戴「原初核心」时的赋能返还一直生效;本次消除"看起来不入赋能"的歧义,并把该依赖写入代码注释。
- **全量精简立牌与筹码的描述文本(用户裁决「全面清扫」)**:覆盖 16 个立牌与全部筹码的 tooltip、帕秋莉手册正文(222 个语言键)与 `AGENTS.md` 的「立牌技能一览」「筹码一览」(76 行);**只删解释性用语,一切数值、时长、条件、作用范围、效果名与配方材料原样保留**。删除典型——立牌侧「（含目标本身）」「（无视护甲值与盔甲韧性；单次效果；无友伤、不破坏方块）」「（永久，直到目标死亡/消失）」「（隐身 + 调查阶段效果）」「（含自己在内、）」「（Pandaman，稀有）」「（溅射已移至被动，见下条。）」;筹码/手册侧「（装备期间生效，卸下移除）」「，无条件触发」「，不再判定生命值」「（防止传送刷充能）」「（含垂直）」「（已达标不再增加）」「（达到阈值后不再发放）」「（不会被单次伤害击倒）」「（排除野生坐骑与他人宠物）」「（治愈点为玩家级资源）」「（不拆袋、不扣币、不加成）」「:截断后不足 1 枚时按 1 枚计」「,不清零」「,以保证"仅一次"严格成立」等(中英成对删除)。**保留**「（下限 5 点）」「（每周期一次）」「（如 /kill、虚空伤害）」「（需安装农夫乐事）」「（含护甲值；目标的骰点与防御牌加成仍然生效）」「（各自上限 %s 点）」等条件/参数/举例。另按现行实现校正**秘密侦探 / 占星师**手册正文与 tooltip 的触发口径,并修正英文 `tooltip.astral_dice.sign.jasmine_active` 的 `For(§92:00§7):` 括号未闭合笔误(双版本、中英一致)。另按用户 2026-09-15 裁定全文统一物品名「标记喷罐」:`AGENTS.md` 原「『标记喷灌』为正确写法」的注记作废并改写为「唯一正确写法」,三处代码注释(`SpellDamageRegistry`/`ModRecipeProvider`/`MarkerSprayerChipItem`)中的「喷灌」一并更正(zh 显示名与 lang 本就是「喷罐」,英文名 `Marker Sprayer` 不受影响)。

### 已修复BUG

#### 伤害与结算
- 修复**末影骰子「雨中/水下受伤 +40%」在骰战路径上被整段丢弃**:原实现直接在 `LivingDamageEvent.Pre`(1.21.1)/`LivingHurtEvent`(1.20.1) 里改数值,而骰战以 `setNewDamage`/`setAmount` **覆盖式**写入自算的最终伤害,于是被赐福玩家被近战命中时这 +40% 凭空消失。现改为 `DiceCombatModifiers` 的**受击侧伤害修饰器**注册表(实时谓词、不落地状态:脱下末影骰或离开雨/水立即失效),由 `EnderDiceHandler` 的 HIGH 监听器作为**唯一应用点**求值一次并登记本次伤害实例的倍率,骰战路径只**搬运**同一倍率、不二次消费 —— **一次伤害事件恰好应用一次 ×1.4**。两版本按各自减免阶段生效(1.21.1 护甲后、1.20.1 护甲前)这一**既有差异保持不变**。**已修掉 1.20.1 的重复消费(×1.96)**:该版七咒 ratio 的捕获点在 `LivingHurtEvent@LOWEST`,**晚于**受击侧修饰器的应用点(HIGH),捕获到的 ratio 已含本次 ×1.4 并经骰战内置因子再应用一次,若骰战路径再搬运一次倍率就是 ×1.96,故 `dice_curse_ratio > 1` 时不再搬运;1.21.1 的捕获点在 `LivingIncomingDamageEvent`(早于应用点),ratio 不含该倍率,无需该规则。
- 修复**电击手套 3 格 AOE 波及伤害的基准跨版本不一致(1.20.1 偏大)**:两侧都在 `SpellDamageRegistry` 的 `onHit` 里以「本次伤害事件的值」为 AOE 基准,但**事件时机不同** —— 1.21.1 的 `DamageEffectCardHandler` 挂 `LivingDamageEvent.Pre`(**护甲 + 附魔减免之后**),1.20.1 却挂 `LivingHurtEvent`(**护甲前**,`LivingEntity.java:1665`),于是 1.20.1 用**护甲前原始值**做范围波及,带甲目标周围被多打一截。现按 `AGENTS.md` 既定的「伤害事件映射」(`LivingDamageEvent.Pre` ↔ 1.20.1 `LivingDamageEvent`,即护甲之后)把 1.20.1 的法伤主链路改挂 `LivingDamageEvent`,`combat/SpellDamageContext.event` 的类型同步(读取处 `ctx.event.getAmount()` 语义随之变为护甲后值,与 1.21.1 的 `getNewDamage()` 对齐);`AGENTS.md` 同步写明**禁止**再把该链路挂回 `LivingHurtEvent`。残余(平台固有、已记入 `AGENTS.md`):目标带**吸收(黄心)**时两侧基准仍有差(1.20.1 在吸收之后派发、1.21.1 在吸收之前),普通目标无吸收故实际影响可忽略。
- 修复**1.20.1 的伤害数字几乎看不见(存活时长只有 1.21.1 的三分之一)**:伤害数字的存活 tick 由客户端**每 tick 推进一次**,而 1.20.1 侧同一个客户端 tick 内被推进了**三次** —— `client/ClientTickHandler` 未判相位(Forge 的 `TickEvent.ClientTickEvent` 每 tick 派发 START + END 两次)各推进一次,`client/KeyBindingSetup` 的 END 回调又推进一次;同一个 `DURATION = 40` 因此只存活约 **13 tick(0.67 秒)**,且透明度在同一窗口内线性衰减到 0,玩家侧表现为存活窗口只有约 0.67 秒(这是一处**独立**缺陷:即便绘制正常也会明显偏短;1.20.1「完全看不到跳字」的**直接原因**是下一条的投影缺陷)。1.21.1 的对应入口是 `ClientTickEvent.Post`(每 tick 恰好一次 ⇒ 2 秒),**故只有 1.20.1 有此问题**。现把推进收敛为**唯一调用点**(`ClientTickHandler`,只认 `TickEvent.Phase.END`)并删除 `KeyBindingSetup` 里的重复调用。**实机取证(1.20.1,玩家持剑 + 佩戴骰子近战攻击存活僵尸)**:服务端 `send id=… type=minecraft:zombie` → 客户端 `add … hasEntity=true` → 覆盖层 `draw … x=432 y=169 alpha=242`(绘制路径确实执行,几何剔除 `reason=behind_camera` 亦按预期工作)→ 条目存活 **1963~1970 ms / 40 次 tick 调用**(= 每客户端 tick 恰好一次),与 1.21.1 的 40 tick(2 秒)一致。
- 修复**1.20.1 的伤害数字完全不显示(正前方目标被当成「在相机背后」整段丢弃)**:跳字的屏幕位置由客户端**手算世界→屏幕投影**,而 1.20.1 侧**照搬了 1.21.1 的视矩阵公式** `new Matrix4f().rotation(camera.rotation().conjugate())` —— 两版本原版的**视图旋转并不等价**:1.21.1 的 `GameRenderer#renderLevel` 用的正是该式(`neoforge` 源 1272–1273 行),而 1.20.1 用的是 `mulPose(Axis.XP.rotationDegrees(camera.getXRot()))` + `mulPose(Axis.YP.rotationDegrees(camera.getYRot() + 180F))`(`forge` 源 1127–1128 行),与前者相差**绕 Y 的 180°**与**pitch 符号**(1.20.1 的 `camera.rotation()` = `rotationYXZ(-yRot, xRot, 0)`,其逆不等于该式)。照搬的后果:正前方目标算出的 `w` **为负**,被 `w <= 0` 的「相机背后」分支整段剔除 ⇒ 跳字**永远**不绘制(1.21.1 因公式与自身原版一致,不受影响)。现按 1.20.1 原版同构构造视矩阵;1.21.1 侧写法**保持不变**并加注释,防止后人「同步」改错。**生产环境实机取证(用户整合包:自动在正前方 3 格生成僵尸靶 + 主手钻石剑真实近战,共 5 轮)**:服务端 `send` 5 → 客户端 `recv` 5 → `add` 5 → 存活 **40 tick(2 秒,与 1.21.1 一致)** → 覆层 `skip reason=behind_camera w=-3.0` **1064 次 / `draw` 0 次**(正前方 3 格的目标恒为 `w=-3.0`),与上述推导逐字吻合。**修复后实机复验(dev 1.20.1,同一套自动触发器:正前方 3 格生成僵尸靶 + 主手钻石剑真实近战 ×5 轮)**:`send` 5 / `recv` 5 / `add` 5,覆层 **`draw` 1042 次、`behind_camera` 0 次**(修复前为 `draw` 0 次 / `behind_camera` 1064 次),绘制坐标 `x=240`(正好 `guiW/2`,即目标位于正前方)、`y` 由 159 上浮至 144、透明度 248→121、存活 **40 tick(2 秒)**,与 1.21.1 完全一致。残余(平台固有):视矩阵不含 `ComputeCameraAngles` 的 roll(该值只存在于事件对象里、相机不保存,正常为 0),FOV 取 `options.fov()` 原值而非原版 `getFov(…, true)` 计算出的有效值(冲刺激活时略有偏差,量级很小)。
- 修复**骰神赐福的 HUD 图标缺失(只能在物品栏看到)**:赐福此前用 `MobEffectInstance` 的**五参构造** `(效果, 时长, 等级, ambient, visible)` 施加,而该构造在字节码层面等价于 `showIcon = visible`(两版本合并 jar 的 `javap -c` 均为 `iload 5; iload 5` → 六参 `(…ZZZ)V` 构造)——传 `visible=false`(本意只是关掉药水粒子)会把 **HUD 图标一并隐藏**;而物品栏的效果面板**不读 `showIcon`**,于是赐福只在物品栏可见,玩家在 HUD 上得不到「赐福还剩多久」的任何提示。现改用**六参构造** `(…, false, false, true)`,与本模组其它状态效果(充能/赋能/弱点识破/治愈/调查阶段,均为「关粒子、留图标」)口径一致;赐福图标连同剩余时间在 HUD 效果栏正常显示。两个施加点(攻击者自身、以及被攻击方也佩戴骰子时给对方施加的那次)同步修复,两版本一致。**实机确认**:1.20.1 dev 客户端触发赐福后,HUD 右上角效果栏出现骰神赐福图标(白底红点骰子,与 `textures/mob_effect/dice_blessing.png` 一致);两版本发货 jar 的该调用前均为 `iconst_0, iconst_0, iconst_0, iconst_1` ⇒ `showIcon=true`。

#### 立牌与技能
- 修复 **效果牌出牌状态机永久锁死（外部汇报的严重 BUG）**:出牌数达到当轮上限后,若**上限在周期中途下降**——卸下大背包/忍术飞镖(固定 +1)、卸下可口糖果/探天卫星筹码、命运的指引效果到期等都会让上限**实时变小**——`EffectCardPeriod` 的"已打满"判定(`isBurstFull`)就会永久成立,而 `tick` 因"没有冷却在跑"直接 `return`、永不清理出牌数,于是**效果牌永久不可用**,界面停在「本轮出牌数已用完!剩余冷却 0 秒」,且摘掉任何筹码/立牌都无法恢复(出牌数是玩家附件,与物品无关;外部汇报的"无法清除"正是这一条)。修复补齐了状态机不变量:① `tick` 发现"计数已达上限却无冷却"时,按既定口径「打满上限即进入冷却」**补上这一轮冷却**,使其在一轮冷却后正常清零;② `registerPlay` 的周期边界同样识别该状态(无论调用方如何都不会留下死状态);③ `getMaxAllowed` 增加防御性下界(附件异常为负时不得让上限退化为 0/负数,否则 `count >= max` 恒成立)。同时 **长按右键不再连续出牌**:客户端 `MultiPlayerGameMode#useItem` 在 `startPrediction` 里**无论** `Item#use` 返回什么都会把 `ServerboundUseItemPacket` 发出去(连物品冷却命中的分支也照样发包),所以在 `Item#use` 里返回 fail 拦不住服务端;改为在 `Minecraft#startUseItem` 的 HEAD 取消(`client/EffectCardUseGuard` + `mixin/client/AstralUseItemGuardMixin`)——同一次按住只出第一张,**必须松开再按**才算新的出牌意图,且只对手持效果牌时生效,不影响放置方块/进食/弓箭等其它右键行为。另:活体书页的"本周期出牌数累计 +1"补上封顶(只推到封顶 9 即可),避免无界累加。
- 修复 **忍者立牌主动「忍术连击」残留旧出牌银行与主动来源**:完全重写为**一次性**效果——只调用 `EffectCardPeriod.grantBonusPlay(player)` 把**当前出牌轮**的可出牌数 +1(附件 `effect_card_bonus_plays`,0/1,随出牌轮归零清除),不累积、不跨轮、不产生常驻状态;旧附件 `komachi_extra_plays`、旧常量 `GameplayConstants.KOMACHI_EXTRA_PLAYS_CAP`、以及 `EffectCardPeriod` 里为主动注册的 `ExtraPlaySource` **全部删除**(出牌轮清理收敛为唯一入口 `EffectCardPeriod.clearRoundBonuses`,禁止各处各列一遍)。释放前置改为三条:① **效果牌冷却进行中**不释放(新规则);② 出牌上限已达封顶 9 张不释放;③ 本轮已授予过 +1 不释放——三者均**不消耗**主动技能冷却,并各自给出 ActionBar 提示;立牌装卸**不再回收**已授予的 +1(避免「上限在周期中途下降」的不变量违例)。**本批补充三态化口径(见「内容与平衡性调整」第 4 条)**:忍者触发后**不立即进入冷却**,锁定**跟随出牌周期**(冷却从"该轮出牌状态完全重置那一刻"开始)、并有 **1:00 宽限**保险(期内自始至终未出任何效果牌 ⇒ 强制重置出牌状态并起冷却;一旦出过牌 ⇒ 保险失效),故本条的"释放成功"不再等于"立即起冷却"。探针新增 `komachicooldown` 用例覆盖①(双版本)。同时**清除忍者残留的计数效果器** `komachi_count`(效果注册、`textures/mob_effect/komachi_count.png`、中英语言键、以及死亡与卸下立牌处的移除调用一并删除):被动计数只保存在附件 `komachi_use_count`,不再用任何效果承载或显示 —— HUD 不会再出现「忍者立牌出牌」效果图标(效果总数 34 → 33);魔法秘典的 `magic_tome_count` 不受影响。两处被动计数改为在 tooltip 中显示:忍者立牌 tooltip 新增「已出效果牌:N/3」计数器(`tooltip.astral_dice.sign.komachi_effect_count`,读附件 `komachi_use_count`),魔法秘典筹码 tooltip 新增**同文案**计数器(`tooltip.astral_dice.chip.magic_tome_count`,读附件 `magic_tome_use_count`)——两者是**两套代码、各读自己的附件**,数据互不影响(中英 × 双版本)。tooltip 由**客户端**渲染,故这两个附件同时补上同步(1.21.1 走 `.sync(ByteBufCodecs.INT)`;1.20.1 走 `.sync()` 并登记进 `SYNCED_KEYS`,23 → 25 键)——此前二者均未同步,客户端读到的永远是默认值 `0`,tooltip 会**恒显示 0/3**(本轮独立验证发现并修复)。
- 修复**立牌主动技能冷却的减免基准不统一(枪匠被过度减免、加急加快会"免费立即完成")**:新增玩家级记录 `sign_active_max_cooldown`,在**4 个起冷却点**(`BaseSignItem` 与 `DiceCombatEvents` 的占星师/秘密侦探/枪匠释放路径)写入本次实际使用的最大冷却 tick(与 `sign_active_cooldown_end` **同步写入**;三态化后**锁定开始时先写基准**、真正进入冷却时再按 `max(0, 基准 − 减免池)` 抵扣改写,见「内容与平衡性调整」第 3 条),减免方一律读它 —— 「命运的指引」与扫地机被动「加急加快」不再按 180 秒基准减免,电流核心「立即完成冷却」的充能定价分母也改读实际基准(记录缺失时回退旧行为)。**数值对照(枪匠 `ACTIVE_COOLDOWN_SECONDS = 120`;诡异骰子减半;任意充能 −20%)** —— ① 枪匠单独(实际最大 120 秒):加急加快后剩余 **30 秒 → 60 秒**,命运指引后 **30 秒 → 60 秒**;② 枪匠 + 诡异骰子(实际最大 60 秒):加急加快 **0 秒(免费立即完成)→ 30 秒**,命运指引 **15 秒 → 30 秒**;③ 枪匠 + 诡异 + 充能(实际最大 48 秒):加急加快 **0 秒(免费立即完成)→ 24 秒**,命运指引 **12 秒 → 24 秒**。另修两处哨兵值缺陷:史莱姆「细胞分裂」的主动冷却 −10 秒不再把结束时刻写成「无冷却」哨兵值 `0`(改为夹底到当前时刻);命运指引与加急加快在冷却**已过期**时不再视为"冷却中"而写入新值(判据由 `cdEnd > 0` 改为 `cdEnd > now`)。**本批叠加三态化**:这三类减免在**锁定(生效中)**期间一律改为累计入 `sign_active_reduction_pool`、不改当前冷却(见「内容与平衡性调整」第 3 条),非锁定态的算式与判据逐字不变。
- 修复**立牌"待命"残留计时器导致主动技能永不冷却(可无限连按)**:冷却门槛此前只看原始计时器数值(`sign_ready_expire <= 0`),而"超时清除"写在各立牌自己的 `onCurioTick` 里 —— 立牌离身后(死亡掉落走 Curios `handleDrops`、不回调 `onUnequip`)该 tick 不再运行,残留的正计时器让门槛**永远成立**,于是该玩家**任何立牌**的主动技能都不再进入冷却。现改为**状态与计时器分离**:冷却门槛只看状态(`sign_ready_type <= 0`),待命超时由新增的 `BaseSignItem.tickSignReadyTimeout(player)` 在**玩家级 tick**(`event/PlayerTickEvents`,与立牌是否还在饰品槽无关)里把状态与计时器归零、并移除对应的"待命"提示效果;占星师/秘密侦探/枪匠三处 `onCurioTick` 里的重复超时清除随之删除(`isSkillWaiting` 的语义不变,仍要求"待命且未过期")。

#### 物品、筹码与交易
- **电磁炮在无可命中目标时白扣充能**:旧实现"攻击立即扣 6 层充能并进入 1:00 冷却,1 秒后才判断目标",被攻击目标在这 1 秒内死亡且周围 3 格无其它敌对目标时,充能与冷却被白白消耗;现改为**充能与冷却都推迟到雷击真正落下的那一刻结算**,取不到任何可命中的敌对目标(或充能已被其它筹码花掉)时**整次触发作废**——不消耗充能、不进入冷却、不生成雷击。同时加固雷击调度器:到期条目先整体摘出队列再逐条处理(避免处理期间新增条目引发的 `ConcurrentModificationException`),单条雷击失败只记 WARN 并跳过,不再中断服务端 tick 循环。
- 修复 **1.20.1 的星盘战利品修饰符全部失效**(宝箱与生物掉落里的星盘注入形同不存在):Forge 1.20.1-47.4.10 的 `net.minecraftforge.common.loot` **没有**内置 `add_table`(全 jar 不含该注册名,也没有 `GlobalLootModifierSerializers`/`AddTableLootModifier` 类),而 13 个 `data/astral_dice/loot_modifiers/*.json` 全部沿用了 1.21.1 的 `"type": "forge:add_table"`,于是被 `LootModifierManager` 逐条判为解码失败(日志 `Could not decode GlobalLootModifier ... Unknown registry key ... forge:add_table`),`getAllLootMods()` 为空。现按 Forge 的 `LootModifier` 基类补一个等价实现并注册为 `astral_dice:add_table`(自带命名空间,不与其它模组冲突),13 个 json 的 `type` 同步改写;子表**刻意用 `getRandomItemsRaw`** 生成(附加表自身不再跑一遍全局修饰符,避免被下游修饰符改两次,与 NeoForge 内置实现的取舍一致)。校验:实机 `getAllLootMods().size() = 13`,且 `minecraft:chests/simple_dungeon` 连滚 400 次产出星盘约 20 枚(单次 5%;修复前为 0),日志不再出现解码失败(1.21.1 侧未改动)。
- 修复**星光兑换可以白嫖**:`ResourceConversion.starlightToStarCoins` 现在只允许兑换**超过基础值**的部分(银行卡等提供的下限不可兑换出去),并按**实际扣除量**发币(取扣除前后的余额差值)—— 此前 `spend` 的扣除会被 `set()` 的下限抬回,出现"净扣 0 仍按请求量发币",佩银行卡时经商「套现」每 3 分钟白拿星币的路径由此关闭。无实际扣除时返回 0,经商主动随之**整个作废**(返回 fail:不进入主动技能冷却、不发随机增益、也不触发技能响应事件)并给出 ActionBar 提示。

#### 状态、存档与同步
- 修复 **1.20.1 缺少 Forge 版本门槛**(低于 Forge 47.4.10 的环境也能加载本模组,随后抛 mixin 错误):`mods.toml` 的加载器门槛此前写作 `loader_version_range=[47,)`,只卡到 **47.0.0**;现改为**由 `gradle.properties` 的 `forge_version` 自动派生两个区间**——`loaderVersion` 用 **javafml 主系列区间** `[<major>,<major+1>)`(当前 `[47,48)`),`modId="forge"` 强制依赖用**精确区间** `[<forge_version>,<major+1>)`(当前 `[47.4.10,48)`)。**为什么精确区间不能写进 `loaderVersion`(2026-09-14 实机验证)**:javafml 语言提供者的版本号就是 Forge 主系列号(47),与完整版本号不同构;把 `[47.4.10,48)` 写进 `loaderVersion` 会被 FML 判为 `Missing language javafml version [47.4.10,48) wanted by astral_dice-1.2.1+forge_1.20.1.jar, found 47` 并**直接拒绝加载本模组**(Forge 自身的 `mods.toml` 也只写 `loaderVersion="[24,]"`)。精确下限改放依赖后:低于 47.4.10 的环境仍在 FML 依赖排序阶段被拒(`Missing or unsupported mandatory dependencies: forge`),合规环境正常加载。校验:构建产物 `META-INF/mods.toml` 两处分别为 `loaderVersion="[47,48)"` 与 forge 依赖 `versionRange="[47.4.10,48)"`;离线侧用 FML 依赖排序所用的同一实现 `org.apache.maven.artifact.versioning.VersionRange` 实测——旧区间 `[47,)` 接受 47.0.0/47.3.0/47.4.9(BUG 复现),新区间三者全部拒绝,47.4.10/47.4.23/47.5.0/47.9.9 通过、48.0.0 拒绝;**并实机启动 1.20.1 客户端确认模组正常加载**(上一版写法实机直接加载失败,仅靠离线单测无法发现)。forge 子项目不再保留可与之冲突的 `loader_version_range` 属性。
- 修复 **1.20.1 的 reobf 生产包启动即 FATAL(Mixin 注解选择器映射缺失),并把 Mixin Booster 明确为硬前置**:1.20.1 生产运行的是 **reobf(SRG 名)jar**,而 mixin 的 `@Inject(method = "…")` / `@Shadow` 是**注解里的字符串选择器**(Mojmap 名),reobf **改不动注解字符串** ⇒ 必须由 **refmap** 在运行时翻译,三件套缺一不可:① 注解处理器(`annotationProcessor` org.spongepowered:mixin 0.8.5 + gson/guava/asm);② `mixin { add sourceSets.main, 'astral_dice.refmap.json' }`;③ `astral_dice.mixins.json` 里的 `"refmap": "astral_dice.refmap.json"`(**缺该字段 Mixin 根本不加载 refmap**)。1.2.0 曾以「Mixin Booster 会自动处理 Mojmap→SRG 重映射」为前提移除了 refmap 与注解处理器,但 Booster 只提供 `MixinModlauncherRemapper` 重映射**字节码引用**、**不翻译注解选择器**,于是生产客户端启动即 `InvalidInjectionException … could not find any targets matching 'startUseItem()V' … No refMap loaded` **FATAL**(dev 永远看不出:dev 类是 Mojmap 名,选择器直接命中、refmap 不参与)⇒ 生产自此必崩。现已恢复三件套;同时把 **Mixin Booster 从「可选依赖」改为硬前置**(`mods.toml` 的 `modId="mixinbooster"` + `mandatory=true` + `versionRange="[0.1.3,)"` + `ordering="AFTER"` + `side="BOTH"`):Forge 1.20.1 的 FML **没有 Mixin 集成**,缺 Booster 时本模组**不报任何错、全部 Mixin 静默失效**,故必须在 FML **依赖排序阶段**拒绝启动,拒绝文案为 FML 原生的 `Missing or unsupported mandatory dependencies:`(Mod ID / Requested by / Expected range / Actual version)。**已排除加载顺序**:ModLauncher 先加载 booster、Mixin 子系统以 booster 为 Source,本模组的配置随后才注册;22 条 mixin 错误全部属于本模组,其它带 refmap 的模组无一报错。**构建后回归判据**:jar 内存在 `astral_dice.refmap.json`、`mixins.json` 声明了它、且配置里每个 mixin 类在 refmap 中都有条目(1.20.1 现 11/11);防回归用例 `scripts/test/mt_loadergate.ps1`(读数 `AP_LG_MB`/`AP_LG_MB_RANGE`)。
- 修复 **tooltip 数值恒为客户端默认值 0 的三处附件同步缺陷**(与上一条同源,由独立验证逐条比对 `ModTooltipHandler` 取值点及其读取链发现):① `rin_pages`(调查员立牌「活体书页伤害增加：+N」);② `pandaman_max_health_bonus`(肉弹战车立牌「获得生命值: +N」);③ `nancy_lu_passive_type`(骇客立牌 tooltip 最下方「攻击力/防御力加成」行——它经 `NancyLuSignItem.getAttackBonus/getDefenseBonus` **间接**读取,不是 `ModTooltipHandler` 里的直接调用,故极易漏检)。三者此前均**未同步**,而这些数值全部由客户端渲染 → 恒显示 `0`(骇客被动 +3 同样恒为 0)。现按同一口径补齐:1.21.1 各加 `.sync(ByteBufCodecs.INT)`;1.20.1 各加 `.sync()` 并登记 `SYNCED_KEYS`(25 → 28)。修正后双版本 tooltip 涉及的全部附件(含间接读取链)均为已同步键,无遗漏。
- 修复**调查员/忍者立牌「效果牌伤害加成」的「死亡保留」从未真正生效**:1.2.0 起更新日志与两版本代码注释均称 `rin_pages`/`komachi_damage_bonus` 死亡保留,但全仓库没有任何附件标记「随死亡复制」(`copyOnDeath`)——NeoForge 附件的默认语义是**死亡不复制**(仅在维度切换与从末地返回时复制),1.20.1 的 `AstralData.onPlayerClone` 也在死亡分支直接 `return`,于是玩家一死,已累计的活体书页数与忍者伤害增益即归零(表现为 live 数值与法伤加成凭空掉回初始值,与文档及玩家可见更新日志不符)。现按记录的本意补齐:1.21.1 给 `rin_pages`/`komachi_damage_bonus` 加 `.copyOnDeath()`;1.20.1 在 `AstralData.onPlayerClone` 的死亡分支**只复制这两个键**,其余键维持「死亡不复制」(两侧保留集合必须一致)。注意死亡清理(`LivingDeathEvent`,在**旧实体**上执行)刻意不清除这两个键——保留逻辑正是从旧数据中取值,改动该清理时必须保留这一约定(双版本一致)。**但仅靠克隆复制不够(独立验证发现的 high 缺陷)**:默认 gamerule(`keepInventory=false`)下死亡会把立牌从饰品槽丢出,Curios 的 tick 轮询随即回调 `onUnequip` → 立牌 `clearSignData` 把这两个键清零,且**早于**克隆复制——于是「死亡保留」只在 `keepInventory=true`(测试世界)或死亡时未佩戴立牌时才成立,真实生存世界仍会归零。现补第二层机制:新增 `component/DeathPreservedBonuses`,在死亡清理时暂存、克隆(`priority=LOWEST`,必须在复制之后)与重生时回写(与 `ChargeManager` 充能死亡保留同一模式),双版本同步。同时明确一条口径(2026-09-15 裁决):**这两项加成只在佩戴对应立牌时生效**——判定入口统一为 `SpellDamageRegistry#effectCardDamageBonus`(忍者)与 `#livingPageBonusPages`(调查员),伤害结算与 tooltip 显示一律走它们,死亡掉落立牌后累计值虽保留但不再加成,直至重新装备。
- 修复 **1.20.1 客户端附件缓存跨会话残留、tooltip 显示上一局的旧值**:客户端缓存 `ClientAstralData` 是**静态字段**(不随客户端玩家实体重建),而全量同步快照只下发「服务端存在原值」的键——上一世界/上一会话的残留值会一直生效到该键被再次写入。实测路径:世界 A 用过 5 张活体书页 → 换到新世界(服务端 `rin_pages` 已是 0)时客户端 tooltip 仍显示 +5(与上一条「未同步恒为 0」是两类不同的错误:值本身是对的,但显示的是旧世界的值)。现双管齐下:① `ModNetwork.syncSnapshot` 改为对**全部 28 个 synced 键**下发,服务端缺失的键下发**显式默认值**(新增 `AttachedDataKey#defaultRawTag()`),覆盖登录/重生/切维度三种入口(重生时新玩家数据为空,正是此前漏发的情形);② 新增客户端会话清理 `client/ClientSessionEvents`,断线(`ClientPlayerNetworkEvent.LoggingOut`)时清空缓存,兜住「此后不再写该键」的离线残留。仅 1.20.1 需要(1.21.1 用原生附件、客户端数据随加入世界重建)。
- 修复**末影骰子保命时把不该清的效果也清掉了(两版本统一为只清有害效果)**:1.21.1 此前调 `removeEffectsCuredBy(EffectCures.PROTECTED_BY_TOTEM)`,而 `EffectCures.DEFAULT_CURES` 默认为**全部效果** —— 实际会把增益一并清掉;1.20.1 的 `removeAllEffects()` 同理。现改为遍历当前效果、只移除类别为 `MobEffectCategory.HARMFUL` 的那部分(两版本同步),`setHealth(1.0F)` 在前、清效果在后的既有顺序保持不变。移除统一经 `ModEffectRemoval` 的内部通道,故 `astral_dice:` 命名空间的效果同样会被清除;而**本模组自身效果仍由 `MobEffectEvent.Remove` 守卫保留** —— 该守卫(HIGH)会拦下一切非内部、非强制、非死亡态的外部清除(牛奶/蜂蜜/`/effect clear` 等)。**2026-09-15 补充:移除白名单在 HARMFUL 之外额外显式追加 `GLOWING`**(保持"只清 HARMFUL + 额外的 GLOWING"这一显式白名单,绝不退化为 `removeAllEffects()`) —— 标记 `marked` 由 `MarkManager.apply` 与 `GLOWING` **一同施加、同一寿命**,而 `GLOWING` 属 NEUTRAL、不在 HARMFUL 之列,只清 HARMFUL 会残留发光轮廓,与守卫处「标记仍存在时发光不得被单独清除」的同寿命口径不一致。为免误伤本模组之外的来源(原版光灵箭 `spectral_arrow`、`/effect`、其它模组),该追加**只在玩家确实带着本模组标记时生效**(收集阶段先于任何移除,故标记未清时判定成立):有标记时该效果实例即"标记随身的那份",随标记一同消失。
- 修复**死亡清理的优先级与清单**:`PlayerLifecycleHandler` 的死亡清理由默认优先级降为 `@SubscribeEvent(priority = EventPriority.LOWEST)` —— 保命方(末影骰子、安全气囊)在 NORMAL/HIGHEST 取消死亡后,不再被按死亡清理(此前同优先级顺序反转时,被救回的玩家仍会走玻璃骰子销毁与计数清空)。同时删除清单里 **23 项"把本来就回默认的附件写 0/false/空串"的无效项**(逐项列表见 `docs/scan2/P4-state-machines-pools.md`;外加 `EffectCardPeriod.clearRoundBonuses` —— 它同样只是写 4 个附件默认值),只保留带附件之外真实副作用的调用(静态暂存表、物品数据组件、移除 MobEffect、阻止效果被重新施加)。并**补齐**写在立牌物品组件上的累计值在死亡时的归零:与护法剑气同款,新增扫地机(`jasmine_atk_bonus`/`jasmine_def_bonus`)与上班族(`padman_atk_bonus`/`padman_def_bonus`/`padman_last_refresh`)的清除 —— 死亡掉落走 Curios `handleDrops`、不回调 `onUnequip`,此前没有任何路径会归零它们。
- 修复**饰品装卸清理的 Curios 参数绑定错误**:`ICurioItem.onUnequip` 的官方签名是 `onUnequip(SlotContext slotContext, ItemStack newStack, ItemStack stack)` —— 第 2 参 `newStack` 是**将要占用槽位的栈**(玩家真正卸下时为 `EMPTY`),第 3 参 `stack` 才是**被卸下的那件饰品**(已用 `javap` 从两加载器缓存 jar 逐条核对:`ICurioItem.onUnequip` 的三参桥接把 `aload_3` 交给 `ICurio.onUnequip`,`ItemizedCurioCapability.onUnequip(ctx, X)` 转调 `ICurioItem.onUnequip(ctx, X, getStack())`,`CurioStacksHandler.loseStacks` 传 `ItemStack.EMPTY`)。旧实现把第 2 参当成被卸下的物品,后果是①**换装(A→B)时被误判为"仍在槽位"而整段跳过清理**;②真正卸下时 `clearSignData` 的组件归零写到了错误的栈上(部分路径下即 `ItemStack.EMPTY` 这个全局单例)。现改为只按 Curios 传入的两个参数判定(`CurioSlotUtil.isIntentionalUnequip`:第 3 参为空 ⇒ 无可清理对象;第 2 参为空、或与被卸下者不是同一物品 ⇒ 真实移除 ⇒ 清理;同一物品 ⇒ 槽位内组件变更引起的回调 ⇒ 不清理),并把清理对象固定为**被卸下的那个栈**(第 3 参),立牌 `clearSignData`、筹码 `onChipUnequip`、骰子 `tryRemoveChipBonus` 三处同步。已知残留缺口(与改动前一致):玩家主动销毁饰品的路径(`CPacketDestroy`)第 2/3 参为同一栈,按"不清理"处理。
- 修复**《恋的规则书》在玩家死亡后重登会再发一本**:发放守卫是附件 `guide_book_given`,而它**不在「随死亡复制」集合内**(1.21.1 未标 `.copyOnDeath()`;1.20.1 的 `AstralData#onPlayerClone` 死亡分支只复制 `rin_pages`/`komachi_damage_bonus`),于是玩家死亡后新实体上该键回默认 `false`,而发放挂在 `PlayerLoggedInEvent` ⇒ **再次登录必再发一本**(测试世界 `keepInventory=true` 时书仍在背包,重复尤为明显),与「仅在玩家第一次进入世界发放一次、此后任何情况下都不再自动发放」的口径不符。现按用户裁决把该键纳入死亡保留:1.21.1 给 `GUIDE_BOOK_GIVEN` 加 `.copyOnDeath()`;1.20.1 把 `ModAttachments.GUIDE_BOOK_GIVEN` 加入 `AstralData#onPlayerClone` 的死亡白名单(两侧保留集合仍严格一一对应,现为 `rin_pages`/`komachi_damage_bonus`/`guide_book_given` **三个**)。发放时机(`PlayerLoggedInEvent`)、配置开关(`give_guide_book_on_first_join`,类初始化读一次、改配置须重启生效)与「发放前只查守卫、**不查背包**」的语义均未改动。新增双版本回归用例 `GUIDE-BOOK-FIRST-JOIN-ONLY`(探针命令 `/astralprobe guidebook <tag>` 读 `given`/`count`;该用例需**跑两次**——全流程 = 首次进入世界,单独 `--phase launch` = 死亡后再次登录——两次都必须 `given=1:count=1`,修补前第二次会读到 `given=0` 随后补发第二本)。注意手册物品本身是 `patchouli:guide_book`,`astral_dice:astral_guide` 只是写在数据组件里的书籍 id。

#### 文案与手册
- 修复**英文描述两处缺失信息(与中文、实机不一致)**:① `tooltip.astral_dice.sign.haiqing_active` 英文写作 "the first target you hit",漏掉中文与实现都具备的「符合骰神赐福触发条件」——`DiceCombatEvents` 的 `isBlessingTarget` 外层判定要求命中目标满足该条件;② 手册 `astral_dice.guide.entry.fanny_sign.1` 中文列有「获得滋养 (2:00)（需安装农夫乐事）」,英文只写 "gain Nourishment (2:00)",漏掉安装前提(同条 tooltip 英文早已写 "(Farmer's Delight only)";手册补齐后与 tooltip 口径一致)。

## 1.2.0

### 新内容

- 新增充能流派基础设施:图标取自 `images/充能.png`(效果纹理 `mob_effect/charge.png`);最大层数写入 `GameplayConstants.CHARGE_MAX_STACKS = 20`;拥有至少 1 层充能时使立牌主动/效果牌冷却时间 -20%(经 `ChargeManager`,接入立牌冷却入口与效果牌冷却;**不提供任何防御力/护甲加成**);充能层数死亡不丢失(死亡前 `ChargeManager.preserveOnDeath`、重生恢复);获得/消耗来源统一调用 `ChargeManager.addStacks/consumeOne/removeAll`(双版本一致)。5 个充能筹码(跃迁引擎/能量回收器/电流剑/高级外设/永动机)的 tooltip 统一追加「当前充能」计数器(显示 `当前层数 / CHARGE_MAX_STACKS`,经 `ModTooltipHandler.addChargeCounter` → lang `tooltip.astral_dice.chip.charge`)(双版本一致)。
- 立牌主动技能 ActionBar 提示重构:新建独立响应事件 `SignActiveTriggeredEvent`,各立牌在立牌类中注册自身提示——忍者(出牌数+1 及剩余出牌数)、看板(新卡牌数与星币数)、骇客(完全隐身时长)已注册专属提示;占星师/秘密侦探提示文本更新为「主动技能已激活，请选择敌对目标向其施加…」;未注册的立牌(大当家/扫地机/史莱姆/护法/上班族/吸血鬼/经商/调查员)显示默认提示「<立牌名>：主动技能已启动！」。
- 新增目标选择器(Target Selection)框架并完成立牌主动技能迁移:新增通用目标选择框架(服务端权威的 `TargetSelectionManager`,配套 `TargetSelectionAction` / `TargetSelectionRegistry` / `TargetType`),需要指定目标的立牌主动技能不再通过「攻击动作」指定目标——激活后进入目标选择模式,准星对准 **16 格**内的合法目标并确认后生效;取消、或 **30 秒**内未确认,均视为未命中,不消耗主动冷却、也不计入「电流核心」充能。三个立牌全部迁移至该路径:占星师(虚弱印记)、秘密侦探(隐匿调查)、枪匠(破绽)。半径与等待期固定为常量(`GameplayConstants.TARGET_SELECT_RADIUS = 16`、`GameplayConstants.SKILL_WAIT_SECONDS = 30`);确认生效后统一经 `WeirdDiceHandler.signCooldownTicks` 开始玩家级冷却(含诡异骰子 -50%% 与充能递减),并经 `CurrentCoreChipItem.onActiveSkillUsed` 计入电流核心充能(双版本一致)。
- 卡牌界面:骰神赐福期间的锁定红色提醒由界面顶部移至界面下方(选择区域以外)。
- 卡牌选择界面:卡牌背包背景贴图按骰子星级(0-3)自动切换(`card_inventory_0~3.png`),`CardInventoryMenu` 暴露 `getStarLevel()`(1.20.1 同步,双版本一致)。
- 新增骰子「下界之星骰子」:全新 T4 奇异品阶(`astral_dice:dice_t4`),可由任意 T3 骰子(下界合金/绯红/末影,经 `dice_t3` 标签)升级——配方为外圈 4 颗下界之星十字环绕(`" N " / "NDN" / " N "`,N=下界之星,D=T3 骰子);筹码栏 = 4+s(0★4/1★5/2★6/3★7,比 T3 多 1 格);卡牌槽与费用点数始终按最高档 3★ 配置(卡牌栏 12 格/攻防费用上限各 6,经 `DiceCurioItem.configStarLevel`,菜单持久化时保留实际星级);骰子每提升 1 星级攻击力 +2、防御力 +2(折算护甲 +4,经 `NetherStarDiceItem` 瞬态属性修饰器实时刷新);物品自带附魔光效(`isFoil` 恒真)(1.20.1 已同步,双版本一致)。
- 新增骰子「末影骰子」:与下界合金骰子同阶(`astral_dice:dice_t3`),可由下界合金骰子升级(ZYZ/YDY/ZYZ,Z=末影珍珠,Y=末影之眼,D=下界合金骰子);受到致命伤害时触发一次不死图腾效果(复活至 1 生命、清除全部效果、生命恢复 II 0:45、伤害吸收 II 0:05、火焰抗性 0:40,与原版图腾一致;图腾粒子/音效/手持高亮动画使用末影骰子图标),随后进入 5:00 冷却(经 `EnderDiceHandler` 在 `LivingDeathEvent` 取消死亡并附加到玩家);不死图腾触发后会尝试瞬移到 16 格内安全地面(排除空气/水面/船上,玩家在水中不触发传送),并播放末影粒子与传送音效;主手/副手原版不死图腾触发保命时,若佩戴末影骰子也会执行同款瞬移(不进入末影骰子冷却);但装备期间雨中/水下(`isInWaterRainOrBubble`)受到的伤害 +40%(在 `LivingDamageEvent.Pre` 最终减免后放大)(1.20.1 已同步,双版本一致)。
- 新增骰子「紫晶骰子」:与钻石骰子同阶(`astral_dice:dice_t2`),可由钻石骰子升级(AAA/ADA/AAA,A=紫水晶块,D=钻石骰子);远程/魔法攻击命中时也触发战斗骰(1-6)并追加骰点伤害(经 `SpellDamageRegistry` 法伤修饰器统一结算:作用域沿用白名单——箭矢/投掷物/原版与联动模组魔法,与法伤效果牌同一链路),但不触发骰神赐福、不消耗卡牌耐久(1.20.1 已同步,双版本一致)。
- 新增骰子「绯红骰子」:与下界合金骰子同阶(`astral_dice:dice_t3`),可由下界合金骰子升级(YJY/JDJ/YJY,Y=下界疣块,J=绯红菌,D=下界合金骰子);战斗骰高点数(4-6)出现概率提升 50%(权重法:4/5/6 权重 ×3.0,P(高点数) 50%→75%(相对提升 50%,与文案一致),对攻击骰与玩家防御骰均生效;攻击方掷骰/玩家防御骰/反击伤害掷骰统一经 `rollCombatDie` 分发至 `CrimsonDiceHandler.rollD6`),但掷出 1 点时立即受到 6 点伤害(骰子伤害类型,与王之力自伤同一来源)(1.20.1 已同步,双版本一致)。
- 新增骰子「诡异骰子」:与钻石骰子同阶(`astral_dice:dice_t2`),可由钻石骰子升级(CJC/JDJ/CJC,C=缠怨藤,J=诡异菌,D=钻石骰子);立牌主动技能冷却时间 -50%(所有开始冷却入口统一经 `WeirdDiceHandler.signCooldownTicks` 取值),但战斗骰低点数(1-3)出现概率提升 50%(权重法:1/2/3 权重 ×3.0,P(低点数) 50%→75%(相对提升 50%,与文案一致),对攻击骰与玩家防御骰均生效)(1.20.1 已同步,双版本一致)。
- 新增骰子「下界岩骰子」:与黄金骰子同阶(`astral_dice:dice_t1`),可由黄金骰子升级(NNN/NTN/NNN,N=下界砖,T=黄金骰子);佩戴后在下界采集矿物(石英矿/下界金矿/远古残骸)时有 30% 概率额外掉落 1 枚星币、5% 概率额外掉落 1 个星盘(同一次采矿最多一种,先判星盘),且猪灵对佩戴者保持中立(经 `PiglinAiMixin` 视同身穿金甲)(1.20.1 已同步,双版本一致)。
- 新增骰子「黑曜石骰子」:与钻石骰子同阶(`astral_dice:dice_t2`),可由钻石骰子升级(OOO/ODO/OOO,O=黑曜石,D=钻石骰子);佩戴后获得 3 点基础防御力(按 1 防御力 = 2 护甲折算为 +6 护甲属性修饰器),且受到的火焰伤害(原版 `is_fire` 标签)减少 70%;因加入 `dice_t2` 标签,可作为母体合成更高阶的下界合金骰子(1.20.1 已同步,双版本一致)。
- 新增骰子「绿宝石骰子」:与钻石骰子同阶(`astral_dice:dice_t2`),佩戴后与村民交易时用星币代替绿宝石支付,并享受 20% 折扣(向下取整,至少 1 枚);配方 = GPG/PDP/GPG(G=绿宝石块,P=星盘,D=钻石骰子)(双版本)。
- 新增骰子「玻璃骰子」:与黄金骰子同阶(`astral_dice:dice_t1`),战斗牌点数始终取最大值(仅随机骰牌:中/大/特大/名刀/防御牌;固定牌暗影突袭/蓄力/全力攻击副作用保留),但死亡会丢失该骰子及其已装备的全部卡牌;配方 = GGG/GTG/GGG(G=玻璃,T=黄金骰子)(双版本)。
- 新增枪匠立牌(Moses,史诗):弱点识破——任意来源的闪避/反击(每名目标至多一次)以及攻击带「破绽」目标都会获得 1 层(最多 4 层),每层攻击/防御 +1、骰点最低数 +1,骰神赐福结束减 1 层;主动「弱点反击」——等待 30 秒,攻击普通敌对目标施加「破绽」2:00,未触发不进入冷却,主动冷却经被动「精密技巧」减为 120 秒;破绽目标骰点只能为 0,会被枪匠闪避,闪避后自动反击(反击含弱点识破攻击加成);tooltip 将「弱点识破/精密技巧」两条被动合并展示,把「破绽」效果说明并入主动描述,并将弱点识破计数器置于 tooltip 最下方;配方为 GPG/TCT/PZP(G=弩、P=星盘、Z=钻石骰子、C=空白立牌、T=红石块),图标取自 images/枪匠立牌.png(1.20.1 已同步,双版本一致)。
- 新增立牌「肉弹战车立牌」(Pandaman,稀有):被动「有好有坏」——使用「汉堡」获得 2 点最大生命上限(不超过 100,卸下清除);使用「汉堡」或「巧克力蛋糕」后,使 8 格内所有友方玩家获得 2 点治疗与 1 点治愈;装备时反击若生命未满,增加缺失生命值等值的伤害。主动「大吃特吃」——获得一张随机治疗类效果牌(汉堡/巧克力蛋糕/奢华大餐),并对 16 格内所有敌对目标施加「嘲讽」1:00;被嘲讽目标攻击施加者时触发反击(沿用反击伤害公式,不消耗反击层数)。新增嘲讽效果:目标只能攻击对其施加嘲讽的玩家。配方 = WHW/HCH/BDB(W=白色混凝土,H=黑色混凝土,C=空白立牌,B=星币,D=骰子);tooltip 底部显示「获得生命值」与治愈点计数器;图标取自 images/肉弹战车立牌.png(双版本一致)。
- 新增首次加入赠送规则书选项:配置新增 `give_guide_book_on_first_join`(默认 `true`),并同步 GameplayConstants 常量;开启时玩家第一次加入世界会获得《恋的规则书》(帕秋莉手册),每个玩家每个世界只发一次(双版本一致)。
- 新增充能筹码「跃迁引擎」(`warp_engine_chip`,稀有):触发指定传送后充能 +2 并获得迅捷 0:10(计入末影珍珠、末影骰子不死图腾后的安全瞬移、维度传送门、Waystone;传送门与 Waystone 共享 5:00 冷却,末影珍珠与末影骰子瞬移不受该冷却限制);配方 = `XXX/WCW/BBB`(X=末影珍珠、W=导电线材、C=空白筹码、B=星币)(双版本一致)。
- 新增充能筹码「能量回收器」(`energy_recycler_chip`,稀有):累计**三维位移(含垂直)**每 150 米充能 +1(单 tick 位移 > 10 视为传送/切维度,不累计;卸下清除进度);**当前充能不少于 5 点时移动速度 +5%**(条件属性,随充能升降实时生效,低于门槛或卸下立即移除);配方 = `SHS/LCL/BBB`(S=燧石、H=活塞、L=导电线材、C=空白筹码、B=星币)(双版本一致)。
- 新增充能筹码「电流剑」(`electric_sword_chip`,稀有):每拥有 4 层充能攻击力 +1(向下取整);累计击杀 10 个敌对目标充能 +2(计数卸下清除);配方 = `HDH/LCL/BBB`(H=红石粉、D=钻石剑、L=导电线材、C=空白筹码、B=星币)(双版本一致)。
- 新增充能筹码「高级外设」(`advanced_peripherals_chip`,史诗):充能层数 ≥ 4 时攻击力 +4;每次触发骰神赐福移除 1 层充能;配方 = `TET/LCL/PPP`(T=红石火把、E=回响碎片、L=导电线材、C=空白筹码、P=星盘)(双版本一致)。
- 新增充能筹码「永动机」(`perpetual_motion_chip`,传奇):触发骰神赐福时若当前充能不足 6 点则补充至 6 点(补足式,已达标不再增加;传奇品质筹码,不进入 Bountiful 奖励池);配方 = `BNB/LCL/GGG`(B=金块、N=下界之星、L=导电线材、C=空白筹码、G=黄金星盘)(双版本一致)。
- 新增治愈类筹码「大碗炖肉」（`big_bowl_stew_chip`,传奇）:骰神赐福效果结束后,对 16 格范围内所有友方目标生效——玩家 +1 点治愈并恢复 2 点生命值;驯服/可骑乘友方生物(宠物、马、猪、炽足兽、骆驼)仅恢复 2 点生命值(治愈点为玩家级资源);图标取自 images/大碗炖肉.png(双版本一致)。
- 新增无流派筹码「会员推荐信」（`member_recommendation_chip`,稀有）:每次触发骰神赐福时获得一张随机卡牌(卡池为普通随机池,不含专属牌);图标取自 images/会员推荐信.png(双版本一致)。
- 新增无流派筹码「书签」(`bookmark_chip`,稀有):使伤害效果牌伤害加成 +1(无上限、无计数,与忍者立牌「效果牌伤害增益」经同一汇总出口 `SpellDamageRegistry.effectCardDamageBonus` 计入激光/板砖/轨道炮/定向爆破/活体书页的法伤,tooltip 同步显示加成后数值);图标取自 images/书签.png(双版本一致)。
- 新增无流派筹码「小猪存钱罐」（`piggy_bank_chip`,稀有）:每使用 2 张效果牌获得 3 星币(计数与魔法秘典/忍者立牌独立,卸下清除);图标取自 images/小猪存钱罐.png(双版本一致)。
- 新增无流派筹码「智能手表」（`smart_watch_chip`,史诗）:物品栏中卡牌数量不足 10 张时,每击杀 1 个敌对目标获得一张随机卡牌(无冷却、无计数,达到阈值后不再发放);图标取自 images/智能手表.png(双版本一致)。
- 新增充能筹码「电流核心」(`current_core_chip`,史诗):①使用主动技能时充能 +1(需指定目标的立牌在其等待释放成功时计入,等待超时未释放不计);②主动技能冷却中按下主动技能键时,按剩余冷却时长占 180 秒的比例消耗充能(每 1/6 一档,最低 1 点、最高 6 点,向上取整)并**立即使主动技能冷却完成**——完成后不直接释放技能,需再次按键使用;充能不足则不生效并提示。tooltip 追加「当前充能」计数器;图标取自 images/电流核心.png。配方 = `UEU/WCW/PPP`(U=红石比较器、E=避雷针、W=导电线材、C=空白筹码、P=星盘)(双版本一致)。
- 新增 4 种合成材料(均已加入 `astral_dice:materials` 物品标签与图鉴「合成材料」分类,双版本一致):「再生试剂」(`regeneration_reagent`)与「星币尘」(`star_coin_dust`)使用动态贴图(再生试剂 2 帧、星币尘 7 帧),「导电线材」(`conductive_wire`)为 10 帧动态贴图(源图末帧为首帧循环标记,已裁剪),三者均生成对应 `.png.mcmeta` 动画文件;「标记涂料」(`mark_paint`)为静态贴图。图标分别取自 images/再生试剂.png、images/导电线材.png、images/星币尘.png、images/标记涂料.png。均配合成配方(双版本一致):再生试剂 = 红石粉+粘液球+金西瓜片+蜂蜜瓶(无序);导电线材 = DLD/DLD/RRR(D=钻石,L=线,R=红石粉);星币尘 = 星币+下界石英+荧石粉(无序);标记涂料 = 空/Y/空、IRI、CGC(Y=岩浆膏,I=铁锭,R=红石粉,C=铜锭,G=金锭)。
- 新增充能筹码「电击手套」(`electric_glove_chip`,史诗):使用效果牌时充能 +1;当充能不少于 4 点时,使用伤害类效果牌会消耗 4 层充能,使该次伤害在本周期内同时命中目标 3 格内的其他敌对目标(每周期最多一次);计入的伤害牌白名单为活体书页/对怪激光/对怪板砖/轨道炮/定向爆破;图标取自 images/电击手套.png(双版本一致)。
- 新增充能筹码「安全气囊」(`airbag_chip`,史诗):受到致命伤害时消耗 6 点充能使该次伤害无效,随后进入 1:00 冷却;充能不足或冷却中不生效(经 `ChipDamageHandler` 在最终伤害阶段以最低优先级拦截,确保按**最终伤害**判定,扣除充能后播放音效并提示);**保命优先级高于不死图腾与末影骰子**——只要充能足够且不在冷却中,必定先由安全气囊接管,原版不死图腾与末影骰子的伪图腾均不会被消耗;与两者一致,无视无敌(bypasses_invulnerability)的致死伤害不参与;图标取自 images/安全气囊.png(双版本一致)。
- 新增充能筹码「电磁炮」(`railgun_chip`,传奇):充能不少于 6 点时攻击力 +5(被动加成,冷却中仍然生效);攻击敌对目标时消耗 6 层充能,并在 **1 秒后**对目标 3 格内的敌对目标降下雷击(雷击伤害 = 本次攻击(骰战最终)伤害的 50%,**下限 5 点**(常量 `STRIKE_DAMAGE_MIN`),经 `LightningBolt#setDamage` 覆写原版默认 5.0F,仍会点燃并生成闪电苦力怕;该雷击为独立的原版伤害源——不计入本次攻击的伤害,也不会触发骰神赐福),随后雷击进入 **1:00** 冷却(冷却中攻击既不消耗充能也不再触发雷击;1 秒延迟经独立的服务端 tick 边界队列实现——原版 `MinecraftServer#tell(TickTask)` 会被 `managedBlock` 当 tick 排空、延迟会塌缩为 0);配方 = `RCR/WBW/GGG`(R=红石块、C=末地水晶、W=导电线材、B=空白筹码、G=黄金星盘);传奇品质筹码,不进入 Bountiful 奖励池;图标取自 images/电磁炮.png(双版本一致)。
- 新增充能筹码「原初核心」(`primordial_core_chip`,传奇):每消耗 1 层充能获得 1 层「赋能」;每层赋能攻击力/防御力 +1(防御力折算为真实护甲);赋能每 0:30 递减 1 层,层数为 1 时直接归 0;卸下筹码时清除全部赋能层数;传奇品质筹码,不进入 Bountiful 奖励池;图标取自 images/原初核心.png(双版本一致)。
- 新增无流派筹码「磨刀石」(`whetstone_chip`,史诗):生命值为 50% 或更低时攻击力 +4、受到伤害 -2;生命值高于 1 点时单次伤害不会超过剩余生命值(即不会因该次伤害被击杀);图标取自 images/磨刀石.png(双版本一致)。
- 新增效果「赋能」(`empower`,效果纹理 `mob_effect/empower.png`,图标取自 images/赋能.png):叠加式增益,每层攻击力/防御力 +1(由筹码「原初核心」承载,消耗充能转化为赋能层数),每 0:30 递减 1 层、层数为 1 时直接归 0(经 `EmpowerManager`),提供 `addStacks/getStacks/consumeOne/removeAll`(双版本一致)。

### 内容与平衡性调整

- 效果牌出牌系统调整:保留出牌窗口(基础出牌数 1 + 固定/临时 +1 来源 + 忍者立牌主动的本轮 +1),单轮出牌上限恢复为**固定常量 9 张**(`GameplayConstants.MAX_EFFECT_CARD_PLAYS = 9`,`getMaxAllowed` 取 `min(1 + 加成, 9)`;固定常量、**不写入配置文件**,原 `max_effect_card_plays` 配置项不再提供),加成来源可叠加但不得超过该上限;效果牌轮次按新定义判定(周期结束 = 所有效果牌进度走完 **且** 出牌冷却走完)。
- 看板立牌被动重做:合成或主动返还卡牌时每获得一张战斗牌 +1 星币(奖励/复制不再触发);主动每返还累计 25 张战斗牌获得一个随机筹码(原为每累计 25 星币)。
- 扫地机立牌被动追加:使用「加急加快」效果牌后,主动技能冷却立即减少最大冷却的 50%。
- 调查员立牌主动:获得一张活体书页,若使用前物品栏中没有活体书页则共获得两张。
- 秘密侦探立牌:调查阶段隐身期间(隐身 + 调查阶段效果)不会被生物索敌。
- 骇客立牌主动:消耗的战斗牌仅从主物品栏选取(不再从末影箱/背包类容器);攻击力加成最低 +2,主物品栏无战斗牌可消耗时同样获得保底 +2。
- 岿然不动:防御力 +2 → 护甲 +8(对应骰战防御力 +4),迁移为效果属性修饰器,真实护甲与骰战均生效且不重复计算;并**按口径实现叠层**(2026-09-13):每层护甲 +8、最多 3 层,重复使用叠 1 层并把时长刷新为 max(旧时长, 3:00),第 4 次使用不再提升;护甲修饰器随层数线性放大(1.21.1 用 `MobEffect#addAttributeModifier` 曲线重载,1.20.1 覆写 `getAttributeModifierValue`;两者与 vanilla 默认 `amount × (amplifier + 1)` 逐位等价,属显式化),抗性提升仍固定 II。
- 全力攻击:耐久 2 → 5。
- 诅咒之剑配方:中轴材料由「谜之炖菜」标签改为「哭泣黑曜石」;删除废弃的 `suspicious_stews` 物品标签。
- 青之诅咒描述:盔甲韧性「归 0」→「-100%%」(数值本身不变)。
- 治愈体系:治愈点上限改为固定 32(原 max(10, 最大生命值÷2));医疗箱-紧急/完备移除「装备时立即恢复 2/6 点生命」(赐福加点保留)。
- 维生素药丸:触发范围由「合成或奖励途径」扩为「合成或获得卡牌时」;探天卫星补充轨道炮改经统一发牌路径。
- 探天卫星第三项能力:使用「轨道炮」后本轮出牌数 +1 由「每轮最多一次」改为「每 1:00 仅一次」。
- 命名统一:活体书页相关 Java 标识符/效果注册 id 统一为 `LIVING_PAGE`/`living_page`(物品 id `effect_card_living_page` 不变)。
- 防御力折算规范:效果牌/立牌/筹码提供的防御力一律折算为**真实护甲**(1 防御力 = 2 护甲值,经 ARMOR 属性修饰器),不再参与骰战防御修饰器——骰战防御修饰器仅保留战斗防御牌(区间变动)。受影响来源:扫地机(2 护甲/点,护甲上限 40)、上班族(-4~+8 护甲)、吸血鬼(半血 +6 护甲)、复仇之戟(+12 护甲)、骇客(被动防御 +6 护甲)、大当家(养精蓄锐 +4 护甲);抗性提升不再折算骰战防御点(原版减伤仍生效)。
- 怪物防御-护甲折算与玩家同步:怪物防御公式由「护甲÷4」改为与玩家一致「护甲÷2」(1 防御力 = 2 护甲值);贯穿之铳的目标防御计算同步。
- 贯穿之铳配方:下界合金碎片 → 下界合金锭(双版本)。
- 新增筹码「肾上腺素-一般」(史诗)恢复,「肾上腺素-高效」(传奇)配方回滚为升级链:一般=生命值为 50% 或更低时攻击/防御 +3;高效=+8 且触发时被敌方攻击有 20%% 概率闪避单次攻击。配方:一般=ZXZ/DCD/PPP(Z=再生药水,X=下界之星,D=凋零玫瑰,C=空白筹码,P=星盘);高效=RZR/ZOZ/PPP(R=红石粉,Z=钻石,O=肾上腺素-一般,P=黄金星盘)(双版本)。
- 标靶筹码:触发骰神赐福后,对**距离最近的敌对目标**施加 1 层标记(移除原「标靶范围内随机」逻辑与 `target_chip_range` 范围配置,不再限定作用距离)。
- 夹心饼干-美味:最大生命值 +12 → +8;移除「生命值低于一半时每 1:00 获得 1 层「反击」」;新增:最大生命值超过 20 点时,超出部分每 4 点生命值 +1 攻击力。
- 创造物品栏骰子顺序调整:下界之星骰子移到下界合金骰子正后方;玻璃骰子、下界岩骰子移到下界合金骰子之后(1.20.1 已同步,双版本一致)。
- 立牌显示顺序调整:创造栏与帕秋莉手册按稀有度低→高排列(蓝稀有 → 紫史诗 → 金传奇,同稀有度保持原内部顺序)(双版本一致)。
- 文本/手册整理:合并本地修改至双版本——Moses 手册被动页合并、移除 integration 第 4 页(千咒刻印)、Pandaman tooltip 嘲讽改为蓝色;同时移除肉弹战车立牌 tooltip 两个计数器之间的多余空行;枪匠立牌 tooltip 被动技能标题删除“弱点识破”,仅保留“精密技巧”;再统一 8 处材料/筹码文案(再生试剂/导电线材/星币尘 手册描述、大碗炖肉/书签 手册描述、星币尘/再生试剂/导电线材 tooltip)——措辞与效果及流派体系对齐(「研磨而成的粉末」→「制成的合金材料」、「导引电流」→「储存和传递能量」、移除「(无上限、无计数)」),并让材料 tooltip 措辞一律以手册正文为准对齐(再生试剂「再生之力」→「重生之力」),英文与 1.20.1 同步。其后又据全量文案审计(立牌/筹码/卡牌三类的 tooltip 与手册条目)统一术语与书写格式:充能/治愈/星光/标记量词一律「层」(充能类:电流剑/高级外设/原初核心/电流核心/电击手套/安全气囊/电磁炮;治愈类:医疗箱/糖果/友谊徽章/缓冲盾牌/大碗炖肉/维生素药丸/史莱姆立牌/肉弹战车;星光类:ATM/银行卡/手电筒/星币锤/八面骰/望远镜);「血量」→「生命值」、「最大生命上限」→「最大生命值」、「随机手牌」→「随机卡牌」、「伤害效果牌」→「伤害类效果牌」;引号统一为「」、冒号与逗号统一全角、修复 `%%` 后多余空格与状态效果描述缺空格、复仇之戟多行列表缩进对齐;所有自带染色的 tooltip/effect 描述行尾统一为 `§r`(原为 `§7`,避免颜色残留到下一行);删除无任何代码引用的死键 `tooltip.astral_dice.sign.lulu_healing`;并保留以下专有名词的原始写法:卡牌名「攻击(特大)/(大)/(中)」「防御(特大)/(大)/(中)」「攻击-特大」(大侦探立牌手册与随机事件文案同步)、筹码名「美工刀-锋利/美工刀/医疗箱-完备治疗/医疗箱-紧急治疗/夹心饼干-美味/可口/一般/肾上腺素-高效/一般/摩托头盔-一般」(英文同步 Cutter Blade Chip / Cutter Chip / Medkit - Complete / Medkit - Emergency / Sandwich - Gourmet / Delicious / Basic / Adrenaline - High-Grade / Common / Moto Helmet - Basic)、材料「标记喷罐」(《吉星派对》原始专有名词,早期误写为「标记喷灌」)、吸血鬼立牌「同时视为满血和半血状态」(《吉星派对》原始标注,保留原名而不改写为生命值 60%%/50%% 门槛描述)(双版本中英一致)。
- 文本/手册裁定续:按「手册一律以中文为准」统一手册旧句式,并在手册补充效果牌单轮出牌数上限 9 张的提示、补全「狂暴」的使用方式(须在骰神赐福前预先放入卡牌栏等)、将「友情徽章」说明改写为「同一治疗者与目标配对 1 秒内去重」的正确版本(双版本中英一致)。
- 修复骰子 tooltip 显示攻击牌时加成误标为「骰子」及染色问题:攻击牌(中/大/特大/名刀)统一显示为「攻击」,范围不再带「+」前缀,与防御牌/独立牌 tooltip 格式一致。
- 骰子卡牌栏平衡:卡牌栏总格数改为仅由星级决定(与骰子品阶无关)——0★=4(攻防各2)、1★=6(各3)、2★=8(各4)、3★=12(各6);实际可用格严格按星级,无隐藏可用格(双版本)。
- 骰子升级配方改用阶层标签作为升级母体:新增 `astral_dice:dice_t0`(基础骰子)/`dice_t1`(黄金骰子)/`dice_t2`(钻石骰子)/`dice_t3`(下界合金骰子)四个物品标签;黄金/钻石/下界合金骰子的升级配方输入由具体物品改为对应标签(双版本)。
- 移除反击流派/效果:删除 `counterattack` 效果、层数与持续反噬目标周期;保留具体触发源(枪匠破绽闪避、肉弹战车嘲讽目标攻击)的**单次反击伤害注入**,触发时经 `DiceCombatEvents.injectCounterDamage` 即时计算一次并结算;同步移除手册“反击流派”页与效果条目(双版本一致)。
- 效果等级角标改为阿拉伯数字并扩展上限:原版显示罗马数字(II~X)且仅到等级 10,现改为阿拉伯数字角标(如「治愈 3」「治愈 32」)并支持到等级 100(新增 `EffectRenderingInventoryScreenMixin`;1.20.1 经 Mixin Booster 运行时重映射,`getEffect()` 无 Holder 包装)(双版本)。
- 配置文件精简重写:公共配置版本重置为 1;移除 client 配置文件;移除效果牌伤害加成上限、活体书页伤害上限、立牌主动冷却、立牌等待期、所有立牌数值、骰神赐福时长、诅咒之剑上限配置项——这些数值固定为 GameplayConstants 常量(数值保持不变);需要选择目标的立牌(占星师、秘密侦探、枪匠)其选择半径与等待期固定为常量(16 格 / 30 秒)。另随事件系统失效链路清理,移除已无作用的事件作用范围与女仆开关配置项(`event_range`/`event_apply_maid`),公共配置版本 1 → 2。
- 移除法伤模块伤害加成上限:活体书页“每次使用叠加伤害”与忍者立牌“效果牌伤害增益”均改为无上限(卸下/移除立牌仍会重置),删除不再使用的 `MAX_DAMAGE_EFFECT_BONUS` 及固定常量 `LIVING_PAGE_BONUS_CAP`/`KOMACHI_DAMAGE_BONUS_MAX`;手册与 tooltip 同步移除“上限 20/10”文案(双版本一致)。
- 友方/队伍发放规则:玩家已加入队伍时,向友方玩家发放的奖励/加成仍只影响同队/队友(EventTargetCollector 统一收集 MC/FTB/OPAC);若玩家未加入任何队伍,则改为作用于全服在线玩家。已统一接入事件随机增益、银行卡-用不完星币、调查员立牌活体书页与真相揭露效果;手册/tooltip 同步说明“无队伍时全体玩家”(双版本一致)。
- 敌对目标血量门槛统一：秘密侦探立牌「关键线索」与诅咒之剑筹码的击杀判定由「超过 20 血」改为「不少于 20 血」——恰好 20 血(10 心)的敌对目标现在同样计入(工具提示/手册文案同步改为「不少于 20 血」)(双版本一致)。
- 筹码品质规范：新增筹码品质一律按**图标边框颜色**判定（蓝=稀有/紫=史诗/金=传奇，已写入 AGENTS.md 强制规范）；据此校正 4 个筹码品质——会员推荐信/小猪存钱罐（史诗 → 稀有）、智能手表（稀有 → 史诗）、大碗炖肉（史诗 → 传奇）（双版本一致）。
- 充能类筹码展示顺序调整：「永动机」移至「电磁炮」紧邻之前（创造物品栏与帕秋莉手册「充能类筹码」章节同步重排，两处顺序保持一致；调整后顺序为跃迁引擎→能量回收器→电流剑→高级外设→电流核心→电击手套→安全气囊→永动机→电磁炮→原初核心）（双版本一致）。
- 筹码配方统一规范（全部 59 个筹码）：①**第三行一律按品质取物**——稀有=星币、史诗=星盘、传奇=黄金星盘；②**第二行中位一律为空白筹码**；③**流派类筹码第二行两侧按流派替换**——治愈=再生试剂、星光=星币尘、标记=标记涂料、充能=导电线材；④**进阶筹码保持原有「通用升级」配方不变**（蓝→紫 = `LGL/GTG/PPP`、紫→金 = `RDR/DTD/GGG`；L=青金石、G=金锭、R=红石、D=钻石、T=上一等级筹码，P/G 为对应品质的星盘/黄金星盘），不并入上述统一；⑤为 10 个此前无配方的筹码补齐基底配方（大碗炖肉/会员推荐信/书签/小猪存钱罐/智能手表/电击手套/安全气囊/电磁炮/原初核心/磨刀石），新补配方一律左右对称且不含星币/星盘/黄金星盘/空白筹码/空白立牌；星币锤第一行中位为 1.21 独有的重锤，1.20.1 以铁砧替代（双版本一致）。
- 无流派筹码展示顺序调整：「会员推荐信/书签/小猪存钱罐/智能手表/磨刀石」五项统一移至「肾上腺素-高效」之后（五项之间原有相对顺序不变，其余条目位置保持不变；创造物品栏与帕秋莉手册「无流派筹码」章节同步重排，两处顺序保持一致）（双版本一致）。
- 原初核心筹码配方调整：第一行中位材料由「下界之星」改为「龙首」（配方变为 `XHX/WBW/GGG`；X=回响碎片、H=龙首、W=导电线材、B=空白筹码、G=黄金星盘）（双版本一致）。
- 磨刀石筹码配方调整：第一行两侧材料由「铁锭」改为「下界合金锭」（配方变为 `XLX/GBG/PPP`；X=下界合金锭、L=砂轮、G=燧石、B=空白筹码、P=星盘）（双版本一致）。
- 充能/赋能不能再产生原版药水粒子：两个效果的效果实例一律以 `visible=false` 构造（`MobEffectInstance` 第 5 参），持续存在的资源类效果不再有粒子糊住视野；**图标/层数/倒计时显示不受影响**——显示闸门是 `showIcon`，粒子闸门才是 `visible`（已核实两版本原版源码：1.20.1 的 `PotionUtils.getColor` 会跳过不可见效果、无可见效果时返回 0，`LivingEntity` 据此不生成粒子；1.21.1 由 `LivingEntity#updateSynchronizedMobEffectParticles` 逐效果经 `EffectParticleModificationEvent` 判定，事件默认取 `MobEffectInstance#isVisible()`）（双版本一致）。
- 效果更名:「嘬一口」→「汲取」——吸血鬼立牌主动施加的状态效果,仅改显示名:效果注册 id 仍为 `papara_bite`,纹理与内部标识不变,主动技能名「嘬你一口」保持不变;英文名同步由 "Vampire Bite" 改为 "Drain",立牌 tooltip 中的效果名引用一并更新(双版本中英一致)。
- 事件优先级整理:末影骰子的雨中/水下伤害放大提升为 `HIGH`、狂暴与虚弱印记的伤害加成由 `LOWEST` 调整为 `LOW`,使三者均先于 `ChipDamageHandler`(安全气囊/磨刀石,`LOWEST`)执行;安全气囊因此恒按最终伤害判定致命性,保命优先级稳定高于不死图腾与末影骰子(双版本一致)。
- 平衡性调整:「赋能」层数上限改为 **10 层**(原为 20 层、与充能上限对齐,现与充能上限解耦):赋能由「原初核心」筹码按实际消耗的充能层数累积,达到 10 层后不再增加;每层仍提供攻击力 +1 与防御力 +1,每 0:30 递减 1 层、层数为 1 时直接归 0(双版本一致)。
- 平衡性调整:手电筒-强光「同一目标仅 +1 层星光」由文案落地为代码(原描述写有该约束但从未实现,可对同一目标反复刷取星光):现记录已发放星光的敌对目标 UUID(附件 `flashlight_granted_targets`,逗号分隔、上限 256 个、记录满后不再发放),再次攻击同一目标不再发放;卸下筹码或玩家死亡时清空记录(双版本一致)。
- 效果牌冷却的起算点修正为「出牌数打满之后」:`EffectCardPeriod.registerPlay` 此前在每次出牌后都启动 30 秒冷却,现改为**仅在本次出牌使出牌数达到上限**(`getMaxAllowed`)时才启动(未打满不开始倒计时);周期定义仍为「所有效果牌进度走完 **且** 出牌冷却走完」,可口糖果「每轮一次」标记随周期归零清除(双版本一致)。
- 出牌数上限的优先级明确化:任何增加出牌数的手段(忍者立牌主动的本轮 +1、大背包/忍术飞镖固定来源、活体书页/命运指引/可口糖果/探天卫星临时来源)都只能把 `getMaxAllowed` 推到 `GameplayConstants.MAX_EFFECT_CARD_PLAYS`(9),**该封顶优先于一切加成、永远不能被绕过**(双版本一致)。
- 命运的指引:主动技能冷却的减少量由「剩余冷却减半」改为**「当前最大冷却 ÷ 2」**(例:180 秒冷却一次减少 90 秒,数值随冷却减免实时取值),与文案「减少主动技能最大冷却倒计时一半的时间」完全一致(双版本一致)。
- 战斗机制与口径调整:① **骇客立牌**主动的解除隐身与战斗牌加成立即由「近战左键命中」扩为**任何攻击行为**(近战/远程/投掷物/魔法)命中敌对生物或玩家;② **扫地机立牌**(300 米)与**能量回收器筹码**(150 米)的移动距离改为按**三维位移(含垂直)**累计;③ **嘲讽**只对敌对生物生效,代码层加守卫确保永不施加给玩家;④ **八面骰**在星光已满时**保留**累计点数不再清零(双版本一致)。

### 已修复BUG

- 修复绿宝石骰子交易时客户端「交易列表」始终显示绿宝石:村民报价的换币替换此前只走取值覆写,而报价数据包的网络编码被投递到 Netty 事件循环后才执行(`Connection.sendPacket` 的 `eventLoop().execute(...)` 分支),那时线程局部的交换上下文已经关闭,客户端收到的永远是未替换的绿宝石费用(服务端实际扣款仍是星币,表现为「显示与实际不一致」)。现改为在数据层替换发往客户端的报价副本(1.21.1 经报价复制构造注入,1.20.1 在发送前重建报价),村民本体报价不受影响;并在成交后立即重发一次报价,使客户端的职业经验条与职业等级即时刷新(原版只在开界面/补货/升级时重发)。(双版本一致)。
- 修复「赋能」效果没有倒计时、层数变化看不出来:效果实例时长此前为 `Integer.MAX_VALUE`(无限),而物品栏效果面板与其悬停提示显示的时长直接取自该值(`MobEffectUtil.formatDuration`),于是面板永远显示一个天文数字、不倒数;现改为「递减间隔 + 20 tick 余量」(`EmpowerEffect.DURATION_TICKS = 620`,`DECAY_INTERVAL_TICKS = 600` 与之同源),面板显示 0:31 → 0:01 的真实倒计时,归零前由 `EmpowerManager.tick` 减 1 层并重新起算。留 20 tick 余量是必需的:递减必须**先于**效果自然到期发生,否则实例先到期会让层数直接归零(表现为「层数凭空消失」而非减 1 层)。(双版本一致)。
- 修复 1.2.0 新增充能筹码遗漏 `curios:chip` 饰品槽标签:安全气囊/电击手套/电磁炮/原初核心/磨刀石 5 个筹码此前只补进了 `astral_dice:chips` 汇总标签,遗漏 `curios:chip` 装备槽标签——1.21.1 筹码槽启用 `curios:tag` 校验会直接拒绝装备(5 个筹码效果完全失效),1.20.1 无校验仍可装备,造成双版本行为不一致;现已补全(双版本一致)。
- 补全枪匠立牌 3 个新增效果的名称键:`weakness_reveal`(弱点识破)/`moses_broken`(破绽)/`moses_ready`(待命：破绽)此前缺少 `effect.astral_dice.*` 语言键,玩家状态栏会显示原始翻译键(同类 `haiqing_ready`/`bonnie_ready` 均有键);现已补入双版本中英文(双版本一致)。
- 修复 `PiglinAiMixin` 注入签名:1.21.1 原版 `PiglinAi.isWearingGold` 参数为 `LivingEntity`,此前按 `Player` 编写导致整合包运行时 Mixin 应用失败(Invalid descriptor);现改为 `LivingEntity` 并在内部仅对玩家执行下界岩骰子判定。
- 修复新增骰子 tooltip 缺失:黑曜石/下界岩/诡异/绯红/紫晶/末影/下界之星骰子此前未进入骰子 tooltip 分发入口,导致这些骰子缺少专属说明;现已统一纳入骰子 tooltip 处理(1.20.1 已同步,双版本一致)。
- 修复 tooltip 染色在 `%%`/占位符转译后断裂的问题:含 `%%` 或 `%s` 的 tooltip 必须先把译文经 `translationString` 展开、再放入 `Component.literal`——原版 `TranslatableContents.decomposeTemplate` 会按格式占位符把译文拆成多个片段,其中 `%%` 变成独立的无样式片段(`FormattedText.of("%")`),落在高亮区内的 `%` 会掉成行底色。本轮补齐星币锤筹码工具提示(该行此前仍走 `Component.translatable`:实测「30%」的 `%` 渲染为灰 `(168,168,168)`、`30` 为黄 `(252,252,84)`),改为 `tt()` 后数值区整段保持黄色(1.20.1 已同步,双版本一致)。
- 修复所有骰子 tooltip 中「骰神赐福 + 时间」染色中断:通用 `dice_desc` 行不再把时间放进会被翻译/占位符拆断的彩色区间,改为拆成「前缀/赐福效果+时间/中间说明/按键/后缀」独立染色组件,确保「骰神赐福 (1:00)」整体保持蓝色——时长写法由「60秒」统一为 `M:SS`(常量经 Java 侧格式化为 `1:00`),与「时间值染蓝」规则的时间写法一致(1.20.1 已同步,双版本一致)。
- 修复 tooltip 行内「回落码」用错、导致数值之后的文字掉色(下界之星/末影/黑曜石/绯红/下界岩/绿宝石/诡异骰子、战斗牌升星提示、立牌冷却行等 14 个语言键共 110 处):tooltip 每一行都经 `Tooltip.splitTooltip -> Font.split(component, 170)` 渲染,其 defaultStyle 传的是 `Style.EMPTY`(`Font.java:328` → `StringSplitter.java:259` → `StringDecomposer.java:114`),因此 `§r` 复位为「无颜色 = 白」、`§7` 复位为灰,两者都不等于该行主色——上一轮把 `§7` 改成 `§r` 只是把「灰溢出」换成了「白溢出」;现统一按「回落码 = 本行底色码」修正:金底 `§6`、浅紫底 `§d`、青底 `§b`、蓝底 `§9`、红底 `§c`、绿底 `§a`、暗绿底 `§2`、黄底 `§e`、灰底 `§7`(用 `\n` 连接的多行值同样处理),末影骰子的「不死图腾冷却」「安全瞬移」两行即由此修正(1.20.1 已同步,双版本一致)。
- 删除紫晶骰子 tooltip 中易歧义的「不会触发骰神赐福」条目,仅保留“远程/魔法攻击也触发战斗骰并追加骰点伤害”(1.20.1 已同步,双版本一致)。
- 修复部分战斗牌 tooltip 的耐久度数字未染黄:中/大/特大/暗影突袭/名刀/蓄力/防御中/防御大/防御特大仍走 `Component.translatable`,使 `%s` 处的耐久度变成无样式片段;已统一改为 `tt()` 先展开再以单字面量渲染,剩余次数数字保持黄色(1.20.1 已同步,双版本一致)。
- 绿宝石骰子补充 `curios:dice` 饰品槽标签(此前遗漏,导致无法装备进骰子栏),并同步补入 `astral_dice:dices` 汇总标签(双版本)。
- 死亡清理调整:不死图腾等取消死亡时不再执行任何清理;护法立牌死亡丢失全部「剑气」层数(tooltip 追加死亡提示);吸血鬼立牌死亡清除主动技能效果;秘密侦探死亡保留调查阶段进度(仅卸牌时清除);忍者/调查员立牌的效果牌伤害加成死亡保留;移除死亡清理中无读取者的 DamageEffectBonus 残留调用。
- 忍者立牌主动「忍术连击」重做为**仅当前出牌周期有效**的出牌数 +1:取消可累积、跨周期保留的「出牌数银行」,同一附件 `komachi_extra_plays` 改作本周期标记(0/1)并并入 `EffectCardPeriod` 的临时出牌数来源,周期归零时清除(不再跨周期累积);**冷却中无法触发**(由 `BaseSignItem.performSkill` 统一拦截);**出牌数已达封顶 9 张**或**本周期已生效**时不释放,且不进入主动技能冷却,分别提示「出牌数已达上限(%d 张)」与「本轮出牌数 +1 已生效」;常量 `KOMACHI_EXTRA_PLAYS_CAP` 由「银行容量 9」改为「每周期至多生效 1 次」(双版本一致)。
- 1.20.1 配方修复:「恋的规则书」(patchouli:guide_book) 配方此前放错目录(recipe 单数)未加载,已移至 recipes;诅咒之剑配方生成文件误用 1.21 result.id 格式导致解析失败,已改回 1.20.1 item 格式(双版本生成源同步修正)。
- 修复部分筹码 tooltip 中换行符被渲染成方块占位符的问题(夹心饼干-美味/肾上腺素-高效/卫星/复仇之戟/诅咒之剑神秘遗物联动等多行 tooltip):改为按 lang 值内 `\n` 逐行拆分添加,不再整段组件内嵌真实换行符。
- 补全物品汇总标签:此前新增的筹码/立牌遗漏进 `astral_dice:chips` / `astral_dice:signs` 汇总标签——筹码 12 个(肾上腺素-一般/高效、跃迁引擎/能量回收器/电流剑/高级外设/永动机/电流核心、大碗炖肉/会员推荐信/书签/小猪存钱罐/智能手表)、立牌 2 个(枪匠/肉弹战车);现已补全,两版本汇总标签含全部筹码(55)与立牌(17)(双版本一致)。
- 修复 `tooltip.astral_dice.chip.smart_watch` 中英染色码数量不一致(英文漏染「1」),现中英染色结构对齐(双版本一致)。
- 修复 1.20.1「调查阶段」事件无法触发:秘密侦探立牌被动 3(击杀「隐匿调查」目标触发调查阶段)此前只在 1.21.1 实现,1.20.1 缺少全局击杀钩子 `onUndercoverInvestigationKill`(类上亦无事件订阅),使 `triggerByKill` 成为死代码——1.20.1 击杀隐匿调查目标不会推进调查阶段;现补齐全局击杀触发,并补上目标效果移除时清空 `undercover_source` 来源的兜底清理(仅 1.20.1,与 1.21.1 对齐)。
- 修复「充能」在**部分消耗**时从未真正扣减(只扣了返回值、效果层数不变):`ChargeEffect.consume`/`consumeOne` 在「消耗后仍有剩余层数」时直接 `addEffect` 一个更低层数的实例,而原版 `MobEffectInstance#update` **只接受更高的 amplifier**,降级实例只会被塞进 `hiddenEffect`,可见层数永不下降——于是电磁炮(需 6 层,而玩家充能通常多于 6 层)、电流核心(消耗充能立即完成主动冷却)、高级外设(每次赐福消耗 1 层)的消耗全部失效;恰好消耗到 0 层的路径走「移除」分支,故此前测试未暴露。现改为**先移除旧实例、再写入新层数**(与已修正的 `EmpowerEffect.consumeOne` 同法),`consume` 返回值与实际扣减一致(双版本一致)。
- 修复效果图标未使用其「载体物品」图标(约定:效果纹理 = 对应物品图标,如 `misaki_burst`=`misaki_sign`、`cutter_ready`=`cutter_chip`):① 枪匠立牌(moses)的 `moses_ready`(待命)/`moses_broken`(破绽)/`weakness_reveal`(弱点识破)原为感叹号、灰十字、黄菱形占位图;② 占星师主动施加的 `weak_mark`(虚弱印记)与秘密侦探主动施加的 `undercover_investigation`(隐匿调查)原为通用「标记」图。现五张 `textures/mob_effect/*.png` 依次改为 `moses_sign`/`moses_sign`/`moses_sign`、`haiqing_sign`、`bonnie_sign` 的图像(与对应物品纹理逐字节相同);诅咒之剑筹码的青之诅咒 `blue_curse` **不使用筹码图标**、保留其专用图标(`images/青之诅咒.png`);`pandaman_taunt` 核对后本就等于 `pandaman_sign`,未改动(双版本一致)。
- 修复枪匠立牌被动「弱点识破」层数在骰神赐福结束后**不减少**:`WeaknessRevealEffect.consumeOne` 直接 `addEffect` 更低层数的实例,而原版 `MobEffectInstance#update` 只接受更高的 amplifier(降级只进 `hiddenEffect`),层数永不下降;现改为先移除旧实例、再写入新层数(双版本一致)。
- 修复「标记」每 60 秒不是减 1 层而是**整层消失**(并残留「发光但无标记」):原版 `LivingEntity.tickEffects` 在 `iterator.remove()` **之前**发出 `MobEffectEvent.Expired`,此刻旧的高层实例仍在 `activeEffects`,在事件里 `addEffect` 低层实例会被 `update` 忽略并随后随旧实例一起被移除,而伴随的发光已被刷新到 1200 tick;现改为把「减 1 层」登记到**下一 tick 的服务器 tick 末尾**补写(那时旧实例已移除,`addEffect` 正常生效),发光同步刷新。未采用「取消该次移除 + `forceAddEffect` 原地替换」的写法:1.21.1 的 `Expired` 实现 `ICancellableEvent` 可取消,但 1.20.1 Forge 的同一事件明确 not Cancelable,两版本写法必须通用(双版本一致)。
- 修复「治愈」效果面板等级不随治愈点下降:治愈点被 30 秒计时器减半或被消耗后,`HealingManager.updateEffect` 仍以更低的 amplifier 调 `addEffect`,被原版忽略,面板等级停在旧值;现层级下降时先移除旧实例再写入(双版本一致)。
- 修复 1.20.1 多层「标记」到期一次性全部消失:1.20.1 缺少标记到期递减实现,层数到点后整体清除;现补齐——到期时层数 -1 并重置计时,层数归零时移除伴随的发光效果,与 1.21.1 行为一致(仅 1.20.1)。
- 修复磨刀石手册条目字面百分号未转义(中英 `50%` → `50%%`):1.1.3 曾修复同类问题,本次新增条目再次漏转义,会在手册中显示 `Format error`;现已修正并通过语言一致性校验(双版本一致)。
- 修正立牌/筹码文案与实际规则不符或缺失关键说明的问题:**吸血鬼立牌**主动「嘬一口」的「被视为满血和半血」改为「同时视为处于『生命值 60% 或更高』与『生命值 50% 或更低』两种状态(仅作用于美工刀与本立牌被动的门槛判定)」,被动门槛由「低于最大生命值一半」改为「生命值 50% 或更低」(源码判定为 `>= 0.6×最大生命值` / `<= 最大生命值÷2`,原中文写法漏掉边界点);**磨刀石**「所受伤害不会超过剩余生命值」明确为「单次所受伤害至多使生命值降至 1 点(不会被单次伤害击倒)」;**大碗炖肉**区分「友方玩家恢复 2 点生命值并获得 1 点治愈」与「友方生物仅恢复 2 点生命值」;**秘密侦探/占星师/枪匠**主动改为「进入目标选择模式:30 秒内在 16 格范围内选择目标并确认后生效;取消或超时视为未命中,不消耗冷却」(枪匠限定「普通敌对目标」、占星师标注「须满足骰神赐福触发条件」);**占星师/秘密侦探**手册条目改为描述目标选择器,不再写「下次攻击的第一个目标」;**电流核心** tooltip 补「完成后需再次按键才释放技能;充能不足则不生效」;**看板**主动由歧义的 `N+1` 改为「回收背包内全部卡牌(含专属牌)……(回收张数 +1)……(不含专属牌)」;诅咒之剑手册由「最大有上限」改为具体「最多累计 +16」(`CURSED_SWORD_BONUS_MAX`),攻击/防御牌与名刀手册补「掷 2 次取最大值」;中文 tooltip 补齐英文与手册已有的限定词与前置条件(电击手套/定向爆破/对怪激光/对怪板砖/轨道炮补「对敌对目标造成」,蓄力/暗影突袭补「固定/骰神赐福期间」)(双版本中英一致)。
- 补齐 15 个物品缺失的帕秋莉手册配方页:11 个筹码(`airbag_chip`/`big_bowl_stew_chip`/`bookmark_chip`/`current_core_chip`/`electric_glove_chip`/`member_recommendation_chip`/`piggy_bank_chip`/`primordial_core_chip`/`railgun_chip`/`smart_watch_chip`/`whetstone_chip`)与 4 个材料(`regeneration_reagent`/`conductive_wire`/`star_coin_dust`/`mark_paint`)此前只有说明页、没有 `patchouli:crafting` 页,玩家在手册里查不到获取配方(配方本身一直存在且可合成);现每个条目补一页 `patchouli:crafting`,手册含配方页的条目由 104 → 119,缺口归零(双版本一致)。同时把 1.2.0 新增的 11 个筹码(6 充能/1 治愈/4 泛用)全部补入《恋的规则书》图鉴,并新增「充能类筹码」章节(`chips_charge`,父级「筹码」,排序在标记类与泛用之间,`chips_other` 顺延),补全后 55 个筹码均有手册条目(双版本一致)。
- 帕秋莉手册「流派」介绍章节此前停留在三流派旧版（只讲星光/治愈/标记，泛用筹码一笔带过），现重写为四流派：新增独立「充能」页（玩家级层数资源、上限 20，由移动/传送/击杀/触发骰神赐福积累，**只要拥有充能，立牌主动与效果牌冷却即 -20%**，层数还能换算为攻击力与移动速度，或由电流核心消耗以立即结束立牌主动冷却）；首句由「三类资源决定流派方向」改为「四大流派围绕各自的资源与机制展开」，泛用筹码与队友组合技各自独立成页（页面 6 → 7，中英与双版本一致）。
- 修复星币锤筹码「整袋过扣」:零散星币不足时原先会扣掉**一整袋**(按 9 枚计)且不找零(1 枚零散 + 若干袋时净亏 7 枚价值),与「消耗 3 星币」不符。现改为**先做可扣除性与可存放性预检**——可扣除星币(零散 + 星币袋折算)不足、或拆出的星币无处安放时整次消耗放弃(不拆袋、不扣币、不加成);预检通过后**拆开 1 个星币袋**(优先拆单袋槽位,拆空后该槽位直接容纳拆出的星币),只扣走本次所需枚数、其余留在物品栏,**绝不因物品栏已满而让星币掉落到地上**(双版本一致)。
- 修复大碗炖肉筹码「友方生物」判定过宽:原先对 16 格内**任意**猪/炽足兽/骆驼/马(含野生个体与他人宠物)回血,与「友方生物」不符。现改为:已驯服的宠物/坐骑必须**主人为自己或同队玩家**(排除野生坐骑与他人宠物),无归属的被动生物(猪、炽足兽)仍视为友方;主人离线时按非友方处理(双版本一致)。
- 修复筹码 tooltip/手册与实现不一致(逐条校正文案,不改变数值;双版本一致):① 摩托头盔三档的**防御力口径**修正——文案保持「防御力 +2/+4/+6」不变,代码层改为按 1 防御力 = 2 护甲值折算为真实护甲 `Attributes.ARMOR`(+4/+8/+12,常量 `MotoHelmetChipItem.ARMOR_PER_DEFENSE = 2`),即**用代码换算对齐文案**,而不是把文案改成护甲值;② 拳击手套中文 tooltip 缺「骰神赐福」限定(英文与手册已有);③ ATM 机「+40%%」补注**向下取整且下限为 1**(截断后不足 1 枚时按 1 枚计,`ResourceConversion.starlightToStarCoins`);④ 普通/鹰眼瞄具的标记施加补注「骰神赐福期间」;⑤ 美工刀补注「处于汲取状态时视为满血」与「不低于 60%%」边界;⑥ 友情徽章补注「同一治疗者 1 秒内仅触发一次」;⑦ 魔法秘典/魔法箭袋/八面骰/电流剑/能量回收器/跃迁引擎/原初核心/安全气囊分别补注卸下清除计数或追踪、共享 5:00 冷却、赋能上限 10 层与卸下清空、无视无敌的致死伤害不参与;⑧ 手持风扇手册补注「需指定目标的立牌在按下进入待命时即触发」;⑨ 物品名「标记喷罐」——早期误判为《吉星派对》原始专有名词「标记喷灌」并保留原名,现按最新裁定更正为「标记喷罐」。
- 修复 1.20.1 复仇之戟描述中的不可达条目:该描述列出 17 个触发效果,其中 蓄风/盘丝/渗浆/寄生/袭击之兆/试炼之兆 为 1.21 新增、1.20.1 原版不存在(该版本永不可达),现按版本把 1.20.1 的 tooltip 与手册文案删减为 6 个攻击类 + 5 个防御类;1.21.1 保持完整列表。**这是两子项目 lang 允许差异的唯一登记项**(已写入 AGENTS.md「语言文件同步规范」)。
- 修复效果牌「复制计数」范围与文案不一致:忍者立牌、魔法秘典与魔法箭袋的计数说明曾写作「仅伤害类效果牌」,而 `BaseEffectCardItem` 对**全部效果牌**(治疗/伤害/互动/专属牌)统一登记计数——现按实现为准,文案与手册统一为「复制计数范围为全部效果牌」,不再恢复已删除的 `countsForCopy()` 类型过滤(双版本一致)。
- 修复效果牌伤害/治疗结算中三处与描述不符:① **定向爆破**的范围追加伤害原为固定 5 点、未计入「效果牌伤害加成」(忍者立牌效果牌伤害增益/书签),现与主目标一致按 `5 + effectCardDamageBonus(attacker)` 结算,目标身上显示的伤害数字同步使用加成后的数值;② **奢华大餐**原先治疗 6 格内**所有**玩家,缺少友方/队伍过滤,现统一经 `EventTargetCollector.collectTeamPlayers`(已加入队伍时仅同队,未加入任何队伍时为全服在线玩家);③ **活体书页**的基础伤害按设计确认为 **2**(法伤 = 2 + 调查员已用页数),修正此前文档中的错误数值(双版本一致)。
- 修复立牌描述与实际效果不符的五处:① **占星师**被动「命运的指引」的获得者修正为**击杀者**(可能是占星师自己;非玩家击杀不发牌),3 星币仍归占星师;② **枪匠**的「破绽」在持续 2:00 内**始终有效**——该期限内目标的每一次攻击都会被闪避并触发一次反击(附件 `moses_dodge_counter_rewarded` / `moses_broken_attack_rewarded` 只用于限制「每段破绽的弱点识破层数 +1」,不再被当作闪避门槛;重新施加破绽时两个标记一并重置),释放条件同时改为与骰神赐福完全一致(近战武器 + 赐福目标判定,不再额外要求「普通敌对生物」);③ **吸血鬼**主动期间,**所有以血量条件决定是否生效的效果一律视为条件通过**——美工刀(生命值 ≥ 60%)、肾上腺素与磨刀石(生命值 ≤ 50%)、可口糖果(满血触发出牌数 +1)、磨刀石(生命值 > 1 时的不可击杀保护)均已接入;④ **上班族**被动「骰点为 6 时无视目标防御力」的**实现**修正:此前会把目标的**防御骰**一并抹掉,现按本模组「目标防御力」口径(与贯穿之铳同一公式 `2 + 护甲÷2 + 1.4×韧性`,其中的护甲已包含由防御力折算而来的部分)**只抹掉防御力整项**,保留目标的**防御骰**与**防御牌**加成(PvP),中英文案同步校正;⑤ **调查员**立牌描述中的占位符 `%s` 丢失导致显示异常,`sign.rin_passive` 已恢复为 `§e%s§7`(双版本一致)。
- 修复**手电筒-强光**「同一目标仅 +1 层」未严格成立:原实现按固定容量做记录满后不再发放淘汰,同一目标在记录被挤出后可以再次获得星光,与描述不符;现改为**严格的每目标一次**——已发放目标的 UUID 记于附件 `flashlight_granted_targets`(上限 256 个),记录满后不再发放、**不做淘汰**,卸下筹码或玩家死亡时清空(双版本一致)。
- 补充**王之力**与**狂暴**的效果描述:两张牌重复使用时**叠层并刷新时长**,时长取 `max(旧时长, 3:00)`,层数上限取配置 `max_effect_stacks`(默认 3);攻击力/受到伤害加成按层数结算(双版本一致)。
- 修复效果牌文案与冷却显示:`tooltip.astral_dice.card.effect_cooldown` 改为显示**实际冷却**(经 `ChargeManager.cooldownTicks` 计入充能 -20% 减免,如 30 秒 → 24 秒),不再直接显示配置基准值;并统一效果牌操作方式文案——可对他人使用的牌(奢华大餐/加急加快/狂暴)一律写「右键-自身使用,下蹲右键-对其他玩家使用」,仅能对他人使用的「你有我有」写「仅能对其他玩家使用(下蹲右键-对其他玩家使用)」;**蓄力**补注「须在赐福前放入卡牌栏」;**攻/防牌**手册的耐久消耗时点由「每轮赐福开始时」改为实际时点(攻击牌在触发赐福的那次攻击、防御牌在 PVP 骰战中被命中防御时);**奢华大餐**中文由「各恢复自身最大生命值」更正为「各恢复**使用者**最大生命值」(与代码 `user.getMaxHealth()*0.3` 及英文一致)(双版本中英一致)。
- 修复立牌文案三处:① **秘密侦探**主动与待命提示统一为「首次命中**符合骰神赐福触发条件**的目标」(与占星师/枪匠同口径);② **经商**主动补注「星光不足时不生效且不进入冷却」;③ **大侦探**主动的中文补上英文与手册已有的「需安装农夫乐事」(双版本中英一致)。另修正**护法立牌**英文 tooltip 的误译(`Dice Blessing damage +4` → `Attack +4`)。
- 修复文档/注释与实现的偏差并清理死代码:① 补齐「**护甲值只进代码、文案只写防御力**」的口径(此前只清了摩托头盔手册):**黑曜石骰子** tooltip「基础防御力 +3(护甲 +6)」→「基础防御力 +3」、**下界之星骰子** tooltip「攻击力 +2、防御力 +2(护甲 +4)」→「…防御力 +2」,连同同源的手册条目「黑曜石骰子」(3 点基础防御力(+6 护甲))与「下界之星骰子」(每提升 1 星级…(+4 护甲))一并清掉护甲括注;② 物品名「标记喷灌」更正为「标记喷罐」——早期「保留原名」的判断作废,现按最新裁定全量更正;③ 同步陈旧注释——狂暴(2:00→3:00)、岿然不动(1:00→3:00)、以毒攻毒(生命恢复 15 秒→30 秒)、魔法箭袋(「功能效果牌」→「全部效果牌均参与复制计数」)、`EffectCardPeriod`/`BaseEffectCardItem`(打满上限才进入冷却)、`ModItems` 筹码注释(拳击手套 +2/+4/+8、速度轮滑 +5%/+15%/+25%、摩托头盔防御力 +2/+4/+6)、`InvestigationEventUtil` 与 `AstralEventSystem` 的事件触发注释;④ **忍者立牌/魔法秘典**的「最后一张效果牌」记录改为**读回附件**取值(此前只写不读);⑤ 删除**骇客立牌无敌**死代码(`INVULNERABLE_TICKS` 与 `nancy_lu_invulnerable_until` 从无授予点),1.21.1 侧同时删除完全无调用的 `damage_effect_bonus` 死键(1.20.1 因该键在同步协议内而保留);⑥ **养精蓄锐**改为死亡清零(双版本一致);⑦ **下界之星骰子** tooltip 删除青色「可装备筹码数量 §e+1§b(T4:0★4/1★5/2★6/3★7)」**整行**——该行只是重复 T4 骰子自身的筹码数,无需额外说明(lang 键 `nether_star_dice_chip` 与其发出点一并删除,四份 lang 键集仍完全一致)。
- 补全骰子铁砧升星白名单并修正 T4 tooltip:升星白名单由 6 种扩为**全部 13 种骰子**(此前黑曜石/下界岩/诡异/紫晶/绯红/末影/下界之星有升星提示却无法升星);**下界之星骰子**(T4)不再显示「星级费用行」(其卡牌栏与费用上限恒为最高档、不随星级变化,原显示会自相矛盾);**活体书页**补齐专属绑定——发放时绑定获得者,首次使用时对未绑定副本兜底绑定(指令/创造栏取出的副本不再人人可用)(双版本一致)。
- 修复磨刀石筹码受伤上限钳制的负值缺陷:汲取(`papara_bite`)期间「不可击杀」保护会无条件激活,此时上限取自「剩余生命值 - 1」;当生命值 ≤ 1(如被压到 0.5 点)时该值为负,`reduced = cap` 会把受到的伤害改写为负值(等效于受伤回血/异常)。现改为 `Math.max(0, 生命值 - 1)`,下限为 0(双版本一致)。
- 骇客立牌三项修复:① **清除过时「免疫任何攻击伤害」残留**——死亡清理中的 `player.setInvulnerable(false)` 会强制解除任何来源(其他模组/指令)授予的无敌,已删除;② **被动「网络防火墙」免疫末影珍珠摔落伤害改为在伤害判定最前置处取消**(1.21.1 `LivingIncomingDamageEvent`、1.20.1 `LivingAttackEvent` + `setCanceled(true)`,取代原先只把伤害改成 0 的做法):旧实现下 `LivingEntity.hurt` 仍会走完流程,`ServerPlayer.indicateDamage` 照常发送 `ClientboundHurtAnimationPacket`(红屏/屏幕震动)并播放受伤音效,现在 `hurt` 直接返回 false,无任何受伤反馈;③ **主动「远程侵入」改为真正的完全隐身**:隐身效果实例 `visible=false`(不再冒药水粒子),并由客户端订阅 `RenderPlayerEvent.Pre`/`RenderHandEvent` 抑制自身盔甲/手持/Curios 装饰渲染(原版隐身只隐藏本体,渲染层不检查 `isInvisible()`);`nancy_lu_hidden_until` 增加客户端同步以驱动该抑制(双版本一致;旁观者视角的隐藏不在本批范围内)。
- 消除「层数递减类」效果在客户端 HUD 的「即将到期」闪烁:原版 `Gui#renderEffects` 对剩余时长 ≤ 200 tick(10 秒)的效果按 `MobEffectInstance#endsWithin(200)` 计算图标 alpha 脉冲(背景不受影响;物品栏效果面板本身无此逻辑),而治愈/标记/赋能这类**以层数递减为机制**的效果在每周期末段必然落入该窗口,表现为「层数还在却一直闪」。现新增客户端 mixin `mixin/client/GuiMixin`(注入 `Gui#renderEffects` 内的 `endsWithin` 调用),对治愈/标记/弱点识破/赋能四类效果直接返回 false——**层数 > 0 期间图标恒定不闪烁,归零时正常消失**;其余效果(原版与其他模组)一律委托原逻辑,零影响。效果的实例时长/层数上限/递减间隔/面板倒计时语义均未改动(双版本一致)。
- 修复闪避只把伤害归零、无法阻止攻击命中后附加效果的问题(如空手尸壳命中后施加的饥饿):枪匠「破绽」闪避与肾上腺素-高效的 20%% 闪避原先写在伤害阶段(`LivingDamageEvent.Pre`/`LivingDamageEvent`)只把伤害改成 0,`LivingEntity.hurt` 仍返回 true,攻击方 `Mob#doHurtTarget` 的命中后附加效果照常施加;现改为在**伤害判定最前置处取消**(1.21.1 `LivingIncomingDamageEvent`、1.20.1 `LivingAttackEvent` + `setCanceled(true)`),使 `hurt` 直接返回 false——附加效果不再施加,且不再产生受伤反馈(无 `hurtTime`、无受击动画包、无受伤音效)。闪避概率(20%%)、触发条件(生命值 ≤50%% 或汲取期间、佩戴对应筹码)、枪匠判定与全部文案均未改动;原先由伤害阶段派发的受击联动(史莱姆立牌 `onHurt`、缓冲盾牌)在取消路径上**显式补发一次**,保持「被闪避的那一击仍算受击」的既有语义;1.20.1 侧另补 `isImmuneToDamage` 复刻 `hurt` 的创造无敌/无敌/濒死短路,保证双版本判定对等(双版本一致)。
- 修复枪匠「破绽」反击的异常递归(双方均带破绽时会互相反击、无上界递归至栈溢出):原实现用单个静态布尔 `counterProcessing` 作重入守卫,但破绽闪避入口(`onMosesBrokenDodge`)与肾上腺素闪避入口从不读取该守卫,且 `injectCounterDamage` 只守卫「注入」而把伤害计算(`computeCounterDamage`,其中绯红骰掷出 1 会自伤)留在守卫之外;嵌套时内层 `finally` 还会提前清零守卫(而它是 static 全局,残留会让全服跳过骰战结算)。现改为**结构化拒绝再入**:守卫改为深度计数 `counterDepth` + `isInCounterChain()`,`injectCounterDamage` 自身在链内直接返回(计算与注入整体入链),并让破绽闪避/嘲讽反击/肾上腺素闪避/骰战结算四个可达入口一律先判定;反击链内的伤害不再参与破绽/闪避/反击判定(反击不可被闪避、也不再反噬)。深度计数 + `try/finally` 保证嵌套时只减不置零、异常与提前 `return` 都会复位,守卫不会卡死(双版本一致)。
- 活体书页出牌数口径改为**累计式**:此前活体书页只以「效果存在即 +1」的开关式提供出牌数,导致全局出牌数上限最多只能到 7(再加忍者立牌主动自身 +1 也只到 8),封顶 9 因此无法被触发、忍者主动「已达封顶则不释放」的分支成为死代码。现改为**每次使用在本周期内累计 +1**(附件 `living_page_cycle_bonus`,随周期归零清除、已同步到客户端以驱动 HUD 上限显示);命运的指引仍为覆盖式 +1(不累计)、可口糖果与探天卫星仍为触发式(每周期一次 / 每 1:00 一次)、大背包与忍术飞镖仍为固定 +1、忍者主动仍为每周期一次。封顶 9 依旧对全部来源与全部立牌**一律生效(这是全局限制,不是忍者专属上限)**:「手牌足够多就能连续出牌、最终由封顶 9 拦住」由此成立;活体书页的叠层(法伤 = 2 + 调查员已用页数,无上限)与本周期出牌数加成是两个互不相干的计数(双版本一致)。
### 工程
- 测试环境集成 JEI 便于配方查验(forge-1.20.1 15.56.0.205 / neoforge-1.21.1 19.39.0.372);1.20.1 dev 的整合包模组改为 curse maven modImplementation 依赖,修复 dev 环境无法加载生产 mixin 模组的问题。
- 1.20.1 引入 Mixin Booster 并改为强依赖(`mixinbooster` 0.1.3):运行时以 Sponge Mixin 接管 mixin 执行并自动处理 Mojmap→SRG 重映射,移除 MDG LegacyForge 的 `mixin` 扩展/refmap/注解处理器(村民交易 mixin 不再需要构建期 refmap)。
- 将 1.21.1 的新增骰子(黑曜石/下界岩/诡异/绯红/紫晶/末影/下界之星)、创造栏排序、tooltip 染色与末影骰子瞬移等改动同步移植到 forge-1.20.1;1.20.1 的下界岩猪灵中立 `PiglinAiMixin` 沿用 Mixin Booster / Sponge Mixin 规范(与 neoforge/fabric 一致),未使用 Forge 原生或其他第三方 Mixin API(1.20.1 同步)。
- 测试环境集成 ModernFix:1.21.1 从整合包复制 `modernfix-neoforge-5.27.24+mc1.21.1.jar` 到 `run/1.21.1/mods`(`install_test_mods.ps1`);1.20.1 经 build.gradle `modImplementation "maven.modrinth:modernfix:OvpPdk44"` 注入 dev run classpath。两版 launch 验证改为识别 ModernFix 的 `Total time to load game and open world was` 加载完成日志(基础等待 30s,未出现则每 15s 再检测)。
- 构建默认自动推送整合包:双版本 `gradlew build` 现在会同时推送 run/mods、根目录 build/libs 与各自整合包 mods 目录(原需 `-PdeployToPack`,现默认触发;该参数保留兼容;pack 根不存在仍自动跳过)。⚠️ **整合包推送改白名单(2026-09-12,同日由黑名单改为白名单):`pushToGame` 由当前 git 分支对 `packPushBranches = ['multi-1.20.1-1.21.1']`(仅发布线)判定** —— 只有发布线放行,其余分支(含 DSH/WorkBuddy 自动创建的 `wt/*` worktree 分支)仅打印一行日志后跳过、不算失败;传 `-PdeployToPack` / `-PpackPush` 可强制恢复(手动出包用)。理由:整合包是需要「成对匹配」的环境 —— 前置库 jar 与本模组 jar 必须版本互配(本模组在编译期锁定库版本,`mods.toml` 又声明了库版本区间),而开发分支上两者各自演进,自动推送会把不匹配的组合灌进测试包,表现为启动期 `NoClassDefFoundError` 或「缺失必需前置」。分支在**执行期**读 `.git/HEAD` 判定(不在配置期求值,故对 configuration-cache 友好、切分支立即生效),并兼容两种形态:普通仓库 `.git` 是目录,worktree 下 `.git` 是**文件**(内容 `gitdir: <path>`)。无法确定分支时(detached HEAD、无 `.git`,如 CI)同样跳过,取安全侧。强制推送时该任务还会读整合包内的 `starengine_lib-*.jar` 与 `starengine_lib_version` 比对,不一致或缺失则告警(仅告警,不阻断构建)。
- 帕秋莉手册补全:此前新增的 11 个筹码(6 充能/1 治愈/4 泛用)全部补入《恋的规则书》图鉴,并新增「充能类筹码」章节(`chips_charge`,父级「筹码」,排序在标记类与泛用之间,`chips_other` 顺延);补全后 55 个筹码均有手册条目(双版本一致)。
- 修复 CI 分支过滤器指向已废弃分支名:`.github/workflows/build.yml` 仍监听带斜杠的旧名 `multi-1.20.1/1.21.1`,导致往当前工作分支 `multi-1.20.1-1.21.1` 推送不触发 CI(lang 同步检查与双版本构建守门失效);现改为无斜杠的新分支名。
- 事件优先级整理:末影骰子的雨中/水下伤害放大提升为 `HIGH`、狂暴与虚弱印记的伤害加成由 `LOWEST` 调整为 `LOW`,使三者均先于 `ChipDamageHandler`(安全气囊/磨刀石,`LOWEST`)执行;安全气囊因此恒按最终伤害判定致命性,保命优先级稳定高于不死图腾与末影骰子(双版本一致)。
- 新增 tooltip 统一染色规则(权威副本为 `ModTooltipHandler` 类头 javadoc,可读版见 `docs/tooltip-color-rules.md`):① 所有**非时间**数值连同其前后符号统一染黄 `§e`(如 `§e+50%%§7`、`§e×2§7`);② 时间数值统一染蓝 `§9`,覆盖 `M:SS` 与 `N 秒`/`Ns` 两种写法(含 `0:00` 与 `0 秒`);③ 形如「<效果名> (<效果时间>)」的条目整段染蓝(如 `§9黑暗(3秒)§7`、`§9Burst(2:00)§7`);优先级上规则 3 > 规则 2 > 规则 1,负面数值保留红 `§c`、重置码 `§r`、按键提示 `§f` 与行内语义色(攻击牌品阶色等)按既有约定保留;并新增只读审计脚本 `scripts/audit/tooltip_color_audit.py`(位置感知,检查 R1 数值未黄 / R2 时间未蓝 / R3 效果条目未整段同色 / R1b 符号外置),双版本语言文件与硬编码 tooltip 全量按规则处理完毕后审计 0 违规（双版本一致）。
- 新增前置库模组 **StarEngine Lib**（`starengine_lib`，独立仓库，现托管于 <https://github.com/merlin-kitsune/starengine_lib>），将 1.20.1 与 1.21.1 可共同引用的 **34 个共享单元**（18 个效果类、`AstralEventType`/`EventContext`/`EventEffect`/`EventTargetCollector` 事件框架、`TargetSelectionAction`/`TargetSelectionRegistry`/`TargetType` 目标选择框架、`SignActiveTriggeredEvent`、`AmethystDiceHandler`、`BossEntityUtil`、`ClientDamageNumbers`、`GameplayConstants` 等）下沉至该库，为后续 Astral Dice Extra（星之骰戏：扩展）模组复用铺路；库**不注册任何注册表条目**（物品/效果/附件/数据组件/能力全部留在本模组），因此搬迁不改变任何 `ResourceLocation` 归属，不破坏存档与数据包（双版本一致）。
- 库工程形态：独立 Gradle 单仓，`common/src/main/java` 为共享**源码目录**（非 Gradle 子项目），由 `neoforge-1.21.1`（Java 21 · Mojmap · ModDevGradle 2.0.141）与 `forge-1.20.1`（Java 17 · reobf SRG · ModDevGradle Legacy 2.0.144）各编译一次；`common` 源码禁用 Java 21 独有语法，由 Java 17 侧编译同一份源码强制保证（双版本一致）。
- 加载器差异以 shim 收敛：`platform/LoaderEvent`（abstract，两侧分别 extends NeoForge / Forge 的 `Event`）与 `platform/LoaderTags`（`c:bosses` tag，两侧取自各自的 `Tags` 类）各平台一份，共享的 `SignActiveTriggeredEvent` / `BossEntityUtil` 只引用 shim，消除原先的加载器命名空间分歧（双版本一致）。
- 配置解耦，且配置**不**进库：共享的 `GameplayConstants` 不再直接读取加载器专有的配置（`ModConfigSpec` / `ForgeConfigSpec` 不通用，无法下沉）。库内只提供平台无关的值快照 `GameplayConfigValues`（record）与 `GameplayConstants.applyConfig(...)`；**配置项定义、TOML 读写与配置 GUI 全部留在本模组** `config/ModCommonConfig`（neoforge 用 `ModConfigSpec`、forge 用 `ForgeConfigSpec`），`AstralDiceMod` 在 `FMLCommonSetupEvent` 里把 `ModCommonConfig.snapshot()` 推给 `applyConfig(...)`。**配置文件仍为 `config/astral_dice-common.toml`（TOML，扁平 snake_case）**，`CONFIG_VERSION` 旧配置自动备份逻辑保持不变。可配置项收敛为 6 项（赠送规则书 / 三个事件作用队伍开关 / actionbar 两个时长），其余玩法数值（星光上限、标记上限、效果牌公共冷却、功能效果牌叠层上限、手持风扇-大范围，以及事件作用范围与女仆开关）一律回归 `GameplayConstants` 常量；库版本随之升至 `1.0.0-SNAPSHOT.3`，两侧消费方 `gradle.properties` / `mods.toml` 同步（双版本一致）。
- **`starengine_lib` 的 Java 包名由 `com.merlinkitsune.starengine` 改为 `com.merlinkitsune.starenginelib`，Maven `groupId` 随之迁移。** 本模组对库的全部引用均已改写 —— 共 **151 个 Java 文件、312 处**（另含两侧 `gradle.properties` 各 1 处坐标）：`import` 语句、行内使用的全限定名（如 `com.merlinkitsune.starengine.event.SignActiveTriggeredEvent`）与 javadoc `{@link}` 目标，覆盖两个平台目录（`forge-1.20.1` 93 个文件，`neoforge-1.21.1` 58 个）。两侧 `gradle.properties` 已换用新坐标（`starengine_lib_group=com.merlinkitsune.starenginelib`）并锁定 `starengine_lib_version=1.0.0-SNAPSHOT.2`。**mod id `starengine_lib`、显示名 `StarEngine Lib` 与全部 `StarEngine*` 类名保持不变**，故 `mods.toml` 的前置 id、整合包文件名与所有语言键均不受影响，迁移的只是类名。替换按字面量、字节级执行（`starengine(?![A-Za-z0-9_])`，大小写敏感，保留原行尾），故 `starengine_lib` / `starenginelib` / `StarEngine` 均不可能被命中，diff 无行尾噪音 —— 151 个文件，每处改动恰一行。因此旧库 jar 与本构建**在同一 `modId` 上二进制不兼容**，这正是必须抬高区间下界的原因（见下一条）。
- **`starengine_lib` 的版本区间下界必须写 `1.0.0-SNAPSHOT`，不能写 `1.0`；现已进一步收紧到 `1.0.0-SNAPSHOT.3`。** 按 Maven `ComparableVersion` 语义 `1.0.0-SNAPSHOT.1 < 1.0`（预发布限定符排在正式版本之前），因此原先的 `versionRange="[1.0,2.0)"` 匹配不到任何 `1.0.0-SNAPSHOT.x`，游戏会以「缺失/不满足必需前置」直接拒绝启动。已用 `maven-artifact` 3.8.5 实测（两侧加载器均经 `MavenVersionAdapter.createFromVersionSpec` → `VersionRange.createFromVersionSpec` 解析依赖区间）：`[1.0,2.0)` 拒绝 `1.0.0-SNAPSHOT.1`，而 `[1.0.0-SNAPSHOT,2.0)` 连同 `1.0.0` / `1.1.0` 一并接受。包名重构后又收紧一档：`[1.0.0-SNAPSHOT,2.0)` 同时匹配**改名前**的 `.1` jar（同为 `starengine_lib` 但包名是旧的），一份残留在 `mavenLocal` 的旧 jar 会被静默解析、运行期再以 `NoClassDefFoundError` 崩溃；`[1.0.0-SNAPSHOT.2,2.0)` 拒绝 `.1`，同时仍接受 `.2` / `.3` / `.10` / `1.0.0` / `1.1.0`，且 `.10` 按数值而非字典序比较。两侧 `mods.toml` 现均声明 `versionRange="[1.0.0-SNAPSHOT.3,2.0)"`，两侧 `gradle.properties` 均锁定 `starengine_lib_version=1.0.0-SNAPSHOT.3`。再收紧一档的原因同前：`.2` 库 jar 与本模组 `modId` 相同、版本号只差 patch 段，却带已删除的 `StarEngineConfigs` 等类，会被宽松区间静默放行后在编译期报「找不到符号」。
- 消费方接入前置库：两侧 `build.gradle` 增加 `mavenLocal()` 与库依赖（neoforge 用 `implementation` —— NeoForge 编译与生产同为 Mojmap、无需重映射，`net.neoforged.moddev` 亦不提供 `modImplementation`；forge 1.20.1 用 `modImplementation`，由 MDG legacyforge 把生产 SRG jar 重映射为 dev Mojmap 命名），两侧 `mods.toml` 声明 `starengine_lib` 为必需前置（`type="required"` / `mandatory=true`，`versionRange="[1.0.0-SNAPSHOT.3,2.0)"`、`ordering="AFTER"`）。
- 库的 `gradlew build` 默认把自身 jar 推送到两个整合包 mods 目录（forge 推 reobf SRG jar、neoforge 推普通 jar），与本模组既有的「build 即部署」约定一致 —— 否则整合包会因缺失必需前置直接拒绝启动。⚠️ **自 2026-09-12 起，库侧在开发分支上也一并停推** —— 库侧黑名单为其 `main`，与本模组侧的白名单(非发布线一律不自动推送)成对，故 2.0.0/前置库这条线**不**自动推整合包，把整合包冻结在最后一个匹配的版本组合上，而不是被单侧覆盖。`-PpackPush` / `-PdeployToPack` 仍可强制手动推送；使用时应把库 jar 与本模组 jar 成对推送。
- **CI 改为先构建前置库再构建本模组**：库仅在 `mavenLocal` 发布，全新 `ubuntu-latest` 检出无法解析 `com.merlinkitsune.starenginelib:starengine_lib-*`，`./gradlew build` 会断在依赖解析。`.github/workflows/build.yml` 现在先 checkout `merlin-kitsune/starengine_lib@main` 到 `.ci/starengine_lib`（已加入 `.gitignore`），在其目录内执行 `./gradlew publishToMavenLocal`，再构建本模组；两仓库版本号必须严格对齐，工作流中 `ref` 固定为 `main`。库侧同样补齐了与本模组一致的 CI/Release 流水线（双 JDK 构建、产物上传、`main` 正式版推送自动打 tag、tag 推送自动发 GitHub Release，附件为两个平台 jar）。
- 打 tag 规则增加预发布/快照守卫：两仓库 `build.yml` 的自动打 tag 步骤在版本号含 `-` 时直接退出。此前 `mod_version=2.0.0-SNAPSHOT.5` 会在 push `main` 时被截成 `2.0.0` 并自动打成正式 tag。本模组版本号升至 `2.0.0-SNAPSHOT.5`，库版本号改为 `1.0.0-SNAPSHOT.3`（双版本一致）。
- 新增两条回归用例与配套探针命令:定向爆破 AOE 口径守卫(`DIRECTIONAL-BLAST-AOE`,断言「自身 5 点 + 效果牌伤害加成」不被其它伤害牌污染)与绿宝石骰子交易(`EMERALD-DICE-TRADE`,断言客户端载荷为星币且成交后重发报价);探针新增 `blastbonus`/`emeraldtrade`/`tradeclose` 三条命令(改探针后必须冷启动)。
- 1.20.1 引入 Mixin Booster 并改为强依赖(`mixinbooster` 0.1.3):运行时以 Sponge Mixin 接管 mixin 执行并自动处理 Mojmap→SRG 重映射,不再需要 refmap 与注解处理器(村民交易 mixin 不再需要构建期 refmap);同时**补齐 Mixin 配置发现**——已核实 Forge 1.20.1 的 FML 47.4.10 对 Mixin 零集成(fmlloader/fmlcore 全文无 mixin 处理),mods.toml 的 `[[mixins]] config=` 在 1.20.1 被完全忽略,故开发环境经 `mixin { config 'astral_dice.mixins.json' }` 注入 ModLauncher 的 `--mixin.config`,生产 jar 由所有 `Jar` 任务写入 `MixinConfigs` 清单属性(已核验产物 `META-INF/MANIFEST.MF` 含 `MixinConfigs: astral_dice.mixins.json`);两条通道缺一,物品栏效果角标、村民交易、猪灵 AI 等 Mixin 会全部静默失效。
  > ⚠️ **本条的两处判断已被 1.2.1 更正**:① 「Booster 自动处理 Mojmap→SRG 重映射、**不再需要 refmap**」**不成立**——Booster 只重映射**字节码引用**、**不翻译注解里的方法选择器**,生产 reobf jar 因此启动即 FATAL(`No refMap loaded`);② 此处的「强依赖」当时并未落成硬门槛,缺 Booster 时本模组**不报任何错、全部 Mixin 静默失效**。1.2.1 已恢复 refmap 与注解处理器,并把 Booster 明确为 1.20.1 硬前置(`mandatory=true` + `versionRange="[0.1.3,)"`),详见 1.2.1 的「状态、存档与同步」。
- 新增 tooltip 统一染色规则(非时间数值连同其前后符号染黄 `§e`;时间数值染蓝 `§9`,覆盖 `M:SS` 与 `N 秒`/`Ns` 两种写法;形如「<效果名> (<效果时间>)」的条目整段染蓝;**规则 0:行内回落码必须等于本行底色码,`§r`/`§7` 都不是「恢复本行底色」**;**规则 4:值内含 `%%` 的文案必须走 `tt()`**;负面数值保留红 `§c`、按键提示 `§f` 与行内语义色保留;优先级 例外 > 规则 3 > 规则 2 > 规则 1),双版本语言文件与硬编码 tooltip 已全量按规则处理完毕;规则 0/4 已并入审计脚本(审计脚本与用法见 `scripts/test/TESTING-SPEC.md` §9)。
- 补全 Bountiful「赏金板」联动数据(1.2.0 新增物品此前整体遗漏同步):`astral_objs` 需求池新增 7 条(玻璃/下界岩/绿宝石/黑曜石/诡异/紫晶骰子、袋装星币),`astral_rews` 奖励池新增 24 条(上述 7 条 + 枪匠/肉弹战车立牌 + 以毒攻毒效果牌 + 13 个新增筹码),补全后 objs 13 条 / rews 92 条;入池判定按规则闭集化(**传奇品质的骰子、筹码、立牌一律不进池**,货币与卡牌例外),价值按骰子阶层与物品品质落在既有价值带内(校验脚本见 `scripts/test/TESTING-SPEC.md` §9)(双版本一致)。；**同时修复悬赏令无名**:Bountiful 用 lang 键 `bountiful.decree.<id>.name` 取悬赏令名(本体自带 16 个悬赏令都如此),而我们 4 个 lang 文件里**一个 `bountiful.decree.*` 键都没有**,于是我们的悬赏令(`astral`)取不到名、落到兜底 `bountiful.decree.invalid`(Invalid Decree);已补 `bountiful.decree.astral.name`(zh「星愿悬赏」/ en "Astral Bounties"),键集仍四方一致。该悬赏令**不绑定职业（全局）**——decree JSON 刻意不写 `linkedProfessions`,与 Bountiful 8.0.0 内置的唯一全局悬赏令 `inventor.json` 同形(内置 12 个中 11 个绑职业、仅该条不绑);即「任何赏金板都可能提供它」,请勿把缺字段当成遗漏去补。
- 清理失效内容与测试残留(全部为静态不可达内容,行为无变化,双版本一致):① 事件系统失效链路——事件类型注册表与触发入口(`AstralEvents`/`AstralEventType`/`EventContext`/`EventEffect`/`AstralEventSystem.trigger`)、护甲惩罚处理器与附件 `armor_penalty_end` 及其空转 tick 调用(**在用的「事件后置钩子」原样保留**:大侦探 +3 星币 / 调查员活体书页);② 未被任何代码引用的女仆目标收集链路(`EventTargetCollector.collectTargets`/`collectMaids`/`isMaidOwnedBy`);③ 3 个无引用语言键(`event.astral_dice.random_buff`/`reduce_armor`、`msg.astral_dice.event_triggered`);④ 测试/调试残留——GameTest 类 `SignSkillTests` 与其结构 NBT、`gameTestServer` 运行配置与 GameTest 命名空间系统属性、孤儿二进制 `lib/junit-platform-console-standalone-6.1.2.jar`(全 build 文件零引用);⑤ 仓库描述文件校正——README 的版本号/立牌数/卡牌栏与流派描述更新为 1.2.0 实际值,`neoforge.mods.toml` 去除重复的 curios 依赖声明。
- 清理筹码相关死代码与陈旧描述(行为无变化,双版本一致):① `AdvancedPeripheralsChipItem.BLESSING_CONSUME` 原为声明后零引用的死常量,现接入 `ChargeManager.consume(player, BLESSING_CONSUME)` 使其成为唯一来源;② 缓冲盾牌的注释写「每分钟一次」(实际常量 300 tick = 15 秒),`DiceCombatEvents` 与 `ModItems` 两处注释更正为「每 15 秒一次」;③ `BaseChipItem` javadoc 引用的 `CurioSlotUtil#onChipUnequip` 并不存在,更正为「子类覆写 `onChipUnequip` 自行清理」;④ AGENTS.md 筹码表数值校正(拳击手套 +1/+3/+5 → +2/+4/+8、速度轮滑 +5%/+10%/+20% → +5%/+15%/+25%、摩托头盔护甲 +2/+4/+8 → +2/+4/+6),删除已不存在的 `countsForCopy()` 过滤描述,并补充「攻击/防御修饰器仅在骰神赐福骰战生效」的通用说明。
- 工程:AGENTS.md 补充与校正全局约定——①「防御力」文案口径(面向玩家一律写防御力并保持设计数值,护甲值只在代码层按 1 防御力 = 2 护甲值换算);② 百分比计算的「下限为 1」策略(按比例算出的产出/伤害截断后不足 1 时一律按 1 计);③ 需指定目标的三个立牌(占星师/秘密侦探/枪匠)的释放触发条件与骰神赐福完全一致;④「破绽」在 2:00 内始终有效,奖励标记不得拦截闪避/反击;⑤ 复制计数范围为全部效果牌;⑥ 出牌数固定封顶 9 且优先级最高、出牌数打满后才进入冷却;⑦ 立牌技能表按最新实现校正(看板/占星师/枪匠/吸血鬼/调查员/上班族),并补充 1.20.1 复仇之戟文案差异为唯一登记项的说明。
- 工程:AGENTS.md 补充本批口径——①「攻击时」一律指触发骰神赐福的规则,效果伤害写「使用远程/魔法伤害」;② 可对他人使用的效果牌操作文案统一句式;③ 移动距离类来源(扫地机 300 米/能量回收器 150 米)按三维位移统计;④ 效果牌冷却 tooltip 必须显示实际值(含充能减免);⑤ 嘲讽只对敌对生物生效;⑥ 死亡清理定案(养精蓄锐清零、骇客无敌死代码删除、其余冷却类维持现状);⑦ `damage_effect_bonus` 死键的「1.21.1 删除、1.20.1 保留」已登记为允许的实现差异;⑧ 物品名以「标记喷罐」为正确写法(「标记喷灌」为错别字)。
- 测试列表精简:移除 BUG1–BUG5(骰神赐福 HUD / 赋能递减 / 效果角标 Mixin / Oculus 雷击渲染 / 电磁炮延迟与冷却)的全部判定内容——删除 10 个用例文件与运行态判定残留 `cases/.mt_run_state.json`、`cases/.mt_active_run`;TESTING-SPEC 移除 §8 中这五项的用例行与判定说明、把 §7 以 `BUG5-RAILGUN-DELAY-CD` 为模板的示例改写为现存用例(`KOMACHI-EXTRA-PLAY`)、删除已失效的「最近一次全量结果」结论句;并清理 KubeJS 探针中仅服务这五项的 13 条命令(`status`/`setup`/`railtest`/`cdguard`/`charge`/`empower`/`empowerclear`/`blessingclear`/`decayinterval`/`decaydue`/`boltvis`/`watch`/`hud`)及其级联辅助机件(探针双版本各 −465 行)——`attack`(近战命中)因被新用例 `NANCY-LU-CLOAK` 复用而保留。历史报告 `scripts/test/reports/2026*` 保留作证据存档,不再复跑(双版本一致)。
- 新增「版本互通门槛(Version Gate)」长期规则(见 AGENTS.md):客户端与服务端只有 `mod_version` 的**二号位(major.minor)相同**时才允许互联——`1.2.x` ↔ `1.2.y` 放行,`1.1.x` ↔ `1.2.0` 拒绝;判据**自动从工程版本号派生、禁止硬编码**,升级二号位(1.2→1.3)无需改任何代码。判据链:`gradle.properties` 的 `mod_version` →(构建期 `generateModMetadata` 展开模板 `src/main/templates/astral_dice_version.properties`)→ 资源 `astral_dice_version.properties`(随 jar 发布)→(运行期 `network/VersionGate.majorMinor()`)→ 互通号(`1.2.0+neoforge_1.21.1`/`1.2.0+forge_1.20.1` → `1.2`;`1.1.3` → `1.1`)。实现按各平台原生机制:①`neoforge-1.21.1` 把互通号作为载荷注册器的网络版本号(`RegisterPayloadHandlersEvent#registrar(String)`;NeoForge 在**配置阶段**逐通道用 `String.equals` 比较,不等即下发 `ModdedNetworkSetupFailedPayload` 并断开连接);②`forge-1.20.1` 把互通号作为 `SimpleChannel` 的通道协议版本号(`clientAcceptedVersions`/`serverAcceptedVersions` 均为 `VersionGate::accepts`;FML **登录握手**交换通道版本后两端各自校验,不匹配即断开),并以 `IExtensionPoint.DisplayTest`(`ModLoadingContext#registerDisplayTest`)用同一判据取代 Forge 默认的 `MATCH_VERSION`——后者要求**完整版本号**完全相同,比本规则更严,会把 `1.2.0` ↔ `1.2.1` 这类同二号位组合误标为不兼容。拒绝时的用户可见表现:1.20.1 显示 Forge「模组不匹配」屏幕(模组名 + 「你拥有 / 服务端拥有」两端完整版本号 + 「安装与服务端相同版本的这些模组」提示);1.21.1 显示 NeoForge「连接已丢失」模组版本不匹配屏幕(通道名 + 失配原因,原因中含两端互通号)。单机/开发环境不受影响(内嵌服务端与客户端同一构建,互通号必然相同)(双版本一致)。
- **新增 NeoForge 21.1.x 上游 BUG 的可选补丁(不会导致启动失败)**:NeoForge 21.1.235 的 `LivingEntity#hurt` 在「伤害事件被取消」的分支直接 `return false` 而**不把 `DamageContainer` 出栈**,每次取消都在 `damageContainers` 上永久残留一层(本模组三处取消点都命中该路径:骇客「网络防火墙」末影珍珠免疫、枪匠破绽闪避、肾上腺素-高效闪避);上游修复(把 `pop` 补进取消分支)至今**未 backport 到任何 21.x**。新增独立 mixin 配置 `astral_dice.neoforge_fixes.mixins.json`(**`required: false` + `injectors.defaultRequire: 0` + 注入器 `require = 0`**),注入 `hurt` 的 HEAD/RETURN **把栈深恢复为进入时的深度**而非盲目 `pop`——将来上游 backport 后本补丁自动退化为 0 次 `pop`,**不会重复弹栈、不会 `EmptyStackException`、不会 `MixinApplyError` 导致启动失败**(三种未来情形均按运行时 Mixin 与 FML 源码逐条核验);补丁首次生效会在 `logs/latest.log` 打印一行 `[astral_dice/neoforge_fixes] …` 存活日志,便于日后确认它是否仍在工作(配置为 `required: false` 的代价是:上游将来重构时补丁会**静默失效**,该日志缺席即是失效信号)。语义由纯 Java 测试覆盖(`tools/run_mixin_stack_sanitizer_test.ps1`,10/10)。⚠️ **已修复(2026-09-13 游戏内实测)**:该补丁最初的辅助类 `NeoForgeFixesLog`/`DamageStackSanitizer` 被放在 mixin 配置声明的包 `...mixin.fixes` 内,而 Mixin 会**拒绝正常加载声明包下的非 mixin 类**(`IllegalClassLoadError: ... is in a defined mixin package`),导致**第一次真实 `hurt` 就崩服务端/客户端**(实测:`Player#attack` → `Zombie#hurt` → `LivingEntity#hurt` → 注入的 `recordDamageContainerDepth` 触发类加载即崩,`crash-reports/crash-*-server.txt`);已把两个辅助类迁到普通包 `com.merlinkitsune.astral_dice.fixes`,mixin 包内只保留真正的 `@Mixin`(`LivingEntityDamageContainerMixin`),纯 Java 测试仍 10/10。**仅 1.21.1 侧**:1.20.1 无 `DamageContainer` 伤害管线(平台差异)。

## 1.1.3

### 新内容

- 骰神赐福期间,骰子卡牌界面禁止插入/移除卡牌(服务端权威 clicked/quickMoveStack 拦截 + 客户端同步拦截),界面顶部显示红色提醒文字(双版本)。
- 效果牌复制/返还覆盖全部效果牌(忍者立牌/魔法秘典/魔法箭袋不再有排除项;含治疗/伤害/专属牌,专属牌复制后绑定获得者)(双版本)。
- 帕秋莉手册:1.21.1 原有;1.20.1 新增移植(109 个页面 + 175×2 条 guide key,新增 Patchouli 1.20.1-85-forge 依赖,整合包补装 Patchouli;4 处联动文案按 1.20.1 改写为「神秘遗物」)。
- 手册分类图标:基本介绍=头号玩家立牌、材料=星币、卡牌=攻击-特大(双版本)。

### 已修复BUG

- **1.20.1 手册移植补全**:此前仅复制了手册页面(assets)而缺失书籍定义(book.json)、合成配方、创造栏书籍物品与书籍模型,导致手册物品丢失、全部页面文本(含联动文本)不可用;已补全(配方按 1.20.1 NBT 格式适配)并重新发布。
- 效果移除拦截误伤本模组主动移除:新增内部移除通道(ModEffectRemoval),「待命」/计数/治愈等效果不再永久残留(双版本)。
- 以毒攻毒移除效果时遍历实时视图导致 ConcurrentModificationException(服务端 tick 崩溃风险):改为快照迭代(双版本)。
- 效果牌复制/返还映射缺失导致错发「王之力」:统一为 BaseEffectCardItem.cardByTypeId 单一映射,补齐以毒攻毒/活体书页等全部效果牌(双版本)。
- 随机卡牌池不一致:ALL 补齐以毒攻毒、BATTLE 按定义含防御牌(双版本)。
- 帕鲁南(经商)立牌手持右键可绕过冷却/等待并意外触发主动:右键行为统一为仅装备或替换装备,主动技能统一由快捷键/立牌栏触发(双版本)。
- FTB Teams UUID 成员分支永不入队:与 OPAC 分支对齐修复(双版本)。
- 顺劈/定向爆破等 AOE 波及目标二次进入骰战结算:改为跳过(1.21.1 标志位 / 1.20.1 dice_damage 源判定)(双版本)。
- 骇客立牌卸载误清公共无敌/隐身,且 onCurioTick 隐身到期判断恒真(默认附件 0)每 tick 误删其他来源隐身:仅清除自身授予的状态(双版本)。
- 标记携带的发光与标记同寿命(牛奶/effect clear 不再单独清除),且不再显示 HUD 效果标识器(双版本)。
- 帕秋莉手册条目 `Format error`:guide 文本字面 `%` 未转义(18 条目中英 36 条)改为 `%%`;4 个同隐患 tooltip key 同步修复(1.21.1;1.20.1 手册为移植的已转义文案,4 个 tooltip key 同步修复)。
- 客户端伤害数字因错误总线订阅永不消失 + 网络线程竞态(1.21.1;1.20.1 无此问题)。
- 技能改名:大当家立牌主动「运攻」→「运功」、骇客立牌主动「远程骇入」→「远程侵入」(双版本)。

### 工程

- 版本号:1.21.1 = `1.1.3-rc1+neoforge_1.21.1`,1.20.1 = `1.1.3-pre1+forge_1.20.1`;重新编译并发布至各自整合包(自动清理旧版本产物)。
- ModEventHandlers 订阅拆分到各自功能实现处(DiceCombatEvents/ModTooltipHandler/LootInjectionHandler/AnvilUpgradeHandler/PlayerLifecycleHandler/ModEffectEvents/PlayerTickEvents + 各立牌/筹码/卡牌/管理器类)。
- 移除蓄力卡"赐福进行中放入"的延迟转换代码(charge_defer 附件及分支)——卡牌栏锁定后不再可能发生;赐福结束一律返还「全力攻击」(双版本)。
- 移除写而未读的 PlayerResourceRegistry(PlayerResource/ResourceType 及治愈/星光注册实现)(双版本)。
- 全量代码审计与清理(1.21.1 为主,1.20.1 同步移植):消除每 tick 重复施加效果同步包(美工刀/复仇之戟/治愈)、每 tick 附魔资源键分配、菜单打开时每 tick 全量重算(20 tick 节流)、法伤修饰器 isActive 双次求值;跳数字发送/效果牌类型映射等重复实现收敛为单一入口;删除 8 个未用数据组件、isChipItem 手写链、骰子冗余构造参数、空覆写、未用方法/导入与死分支。
- 仓库结构迁移为 multiloader 单仓(1.21.1-main → neoforge-1.21.1/、1.20.1-forge → forge-1.20.1/;跨 MC 大版本不设 common 共享层)。
- 原仓库 git 排除内容(AGENTS.md、docs/、temp/、scripts/、run/、deploy.ps1 等)迁入;tools/check_lang_sync.py 移至根 tools/。
- CI(build.yml) 双 JDK(17/21) 构建两子项目并对两者执行 lang 同步检查。

## 1.1.2-rc1

### 新内容

- **复仇之戟**:tooltip 新增当前攻击力/防御力加成显示;触发任意加成时显示"复仇之戟"状态效果(图标为物品本体图标),加成消失时自动移除。
- 新增**计时器守卫**:本模组所有有时长效果严格按 20 tick/秒 流动,不受任何加快/减慢效果时间的 buff(如神秘遗物+ 的烈焰之核)影响。
- 卡牌栏选择器改为 **3 列** 显示,同屏可见更多卡牌。
- 同步本地中文文本修改(看板立牌 tooltip 措辞、命运的指引描述)。
- 移除 CurseForge / Modrinth 独立更新日志文件及其关联。

### 内容与平衡性调整

- 岿然不动、狂暴持续时间调整为 **3:00**。

### 已修复BUG

- 修复骰神赐福进行中放入蓄力会立即转换为全力攻击的问题:赐福进行中放入的蓄力本次赐福不生效、不转换,改为下次触发骰神赐福时生效并在其结束时转换。
- 修复标记效果消失后发光效果残留的问题:发光改为与标记同寿命(不再使用无限时长),多层标记同步刷新,标记结束时发光随之消失。

### 工程

- 版本号更新为 `1.1.2-rc1`。
## 1.1.1-rc1

### 新内容

- 看板立牌（mimi）技能重做：被动改为“合成、奖励、返还卡牌时，每获得一张战斗牌 +1 星币；装备时筹码栏位 +1；每累计 25 个星币获得 1 个随机筹码（蓝色 60% / 紫色 35% / 金色 5%）”；主动改为“回收物品栏中全部卡牌（含专属牌），返还 N+1 张随机卡牌（不含专属牌）”。
- 新增通用谜之炖菜配方；诅咒之剑配方改用 `astral_dice:suspicious_stews` 标签。

### 界面与显示

- 新增骇客立牌“远程骇入”效果图标与描述；立牌主动技能 ActionBar 改用本地化名称；命运指引备注颜色修正。

### 已修复BUG

- 修复治愈图标在计时结束且未再次触发时未移除的问题。

### 工程

- 版本号更新为 `1.1.1-rc1`。

## 1.1.0-rc1

### 新内容

- 新增效果牌：以毒攻毒（移除最多 3 个原版负面效果，并获得生命恢复）。
- 新增效果：青之诅咒（护甲值 -20%，盔甲韧性归 0）。
- 新增筹码：维生素药丸、诅咒之剑、复仇之戟、贯穿之铳、可口糖果、友情徽章、探天卫星。
- 新增立牌：骇客立牌（nancy_lu）。
- 新增手持风扇-小 / 手持风扇-大筹码。

### 内容与平衡性调整

- 手持风扇-小配方调整为羽毛上排；手持风扇-大改为通用蓝→紫升级配方（以手持风扇-小为原料）。
- 狂暴配方简化为 1 火药 + 1 星币。
- 诅咒之剑：骰神赐福期间每轮最多触发一次击杀攻击力加成；上限默认 16、最大 32。
- 占星师立牌、忍者立牌品质提升为史诗，配方由黄金骰子改为钻石骰子。
- 完全隐身期间生物无法将玩家设为索敌目标。
- 创造栏筹码按星光、治愈、标记、无流派分类摆放；美工刀归入治愈类。

### 界面与显示

- 治愈类筹码与史莱姆立牌 tooltip 显示当前治愈点。
- 修复 tooltip 中按键名、`%` 号、计数器符号无法正确染色的问题。
- 修复诅咒之剑 tooltip 中青之诅咒描述多余空行。
- 立牌/材料 tooltip 分类从 `card` 移至 `sign`/`material`。

### 已修复BUG

- 修复大侦探立牌被动无法触发（主动技能与击杀“隐匿调查”目标）。
- 修复维生素药丸 tooltip 缺失；拾取卡牌不再触发维生素药丸效果。
- 友情徽章配方改为必须使用瞬间治疗药水。
- 修复 tooltip 颜色代码导致部分文本变白的问题。

## 1.0.3-rc1

### 内容与平衡性调整

- 防御牌耐久消耗恢复为仅 PvP 生效：玩家攻击带骰子的玩家时，双方触发骰神赐福，并消耗被攻击方防御牌耐久（每次赐福仅一次）；怪物攻击不再消耗防御牌耐久。
- 新增防御力/护甲值换算：1 防御力 = 2 护甲值。
- 效果牌、立牌、事件、筹码提供的防御力，无论是否触发骰神赐福，均按 1:2 折算为护甲值；只有战斗防御牌直接作为防御点加入骰神赐福。
- 重写“岿然不动”：使用后防御力 +2，并获得 抗性提升 II，持续 1:00。

### 工程

- 版本号更新为 `1.0.3-rc1`。

## 1.0.2-rc1

### 内容与平衡性调整

- 调整骰子筹码栏位数量（按 0★~3★）：普通 0/1/2/3，黄金 1/2/3/4，钻石 2/3/4/5，下界合金 3/4/5/6。
- 调整骰子攻击/防御卡牌栏数量（每侧）：普通 3，黄金 4，钻石 5，下界合金 6。
- 主动触发“骰神赐福”时不再消耗防御牌耐久。
- 玩家若攻击带有骰子的玩家，则自动触发双方拥有骰子玩家的骰神赐福，并消耗被攻击方的防御牌耐久度（每次骰神赐福仅消耗一次）。
- 秘密侦探立牌击杀奖励只返还攻击牌，攻击玩家不返还卡牌。
- 所有效果牌、立牌、事件、筹码提供的防御力加成，默认只作为护甲值加成；触发骰神赐福后作为防御点加入。
- 防御卡仅在骰神赐福期间作为防御点生效。

### 界面与显示

- 所有立牌 Tooltip 增加主动/被动技能名称，并将主动技能按键提示移至最上方。
- 主动技能按键提示整体改为白色，按键符本身为黄色。
- 大当家立牌主动技能图标改为使用大当家立牌图标。
- 统一 Tooltip 时间与数值格式：时间蓝色、数值黄色、不足 1:00 使用秒、药水时间使用括号。

### 已修复BUG

- 移除立牌主动技能触发成功后的通用“技能已激活”ActionBar，避免覆盖各立牌自身的特殊提示。
- 修复溅射/范围伤害未在被影响目标身上显示伤害数字的问题（大当家立牌扩散、定向爆破 AOE）。
- 本 Mod 创建的自定义效果无法被牛奶、蜂蜜瓶或 `/effect clear` 清除，仅玩家死亡可清除。
- 修复玩家死亡时部分效果状态未正确重置的问题，统一清理立牌等待、扩散、出牌、魔法箭袋、命运指引、调查阶段等状态。

## 1.0.1-rc1

### 内容与平衡性调整

- 重新设计治愈体系：为治愈增加独立计时器，默认每 30 秒触发一次治愈效果（仅在骰神赐福期间）。
- 触发骰神赐福时按治愈点×2 回血，计时结束治愈点减半。
- 美工刀触发阈值改为最大生命值 60% 以上即可触发，避免大部分时候都触发不了。
- 护法立牌：主动技能攻击力加成提高至 +4，持续时间提高至 2:00。
- 吸血鬼立牌：主动效果调整第二项为：不论玩家当前血量为多少，都同时视为满血和半血以下状态。
- 秘密侦探立牌：被动技能返还卡牌只对击杀不少于 20 血的敌对目标生效。
- 巧克力蛋糕/汉堡改为恢复最大生命值 20%/40%；奢华大餐改为恢复自身最大生命值 30%，并治疗同队/无队伍玩家。
- 治愈盾牌触发间隔改为 15 秒。
- 调整战斗牌耐久值：中/大/特大=10，暗影突袭=10，名刀=5，蓄力=1，全力攻击=2。
- 蓄力在赐福结束后返还全力攻击，并增加兜底检测与 ActionBar 提示。

### 已修复BUG

- 修复骰神赐福的触发目标判定和武器判定问题：仅近战武器触发，且友好/被动/未激怒中立生物不再触发。
- 修复骰子/筹码/立牌下蹲右键装备不生效。
- 修复战斗牌耐久条显示异常、Tooltip 丢失问题，恢复剩余次数显示。
- 修复大当家立牌触发伤害扩散时意外递归导致游戏崩溃。
- 治愈效果不再产生药水粒子。

### 质量更新

- 为部分行为增加 ActionBar 提示，同时受到事件影响的玩家也会有 ActionBar 提示。
- 大侦探随机事件提示会显示具体事件名称。
- 微调骰子卡牌选择界面的卡牌显示。
- 替换卡牌选择界面背景贴图。
- 修复 GitHub Actions 中 `gradlew` 无执行权限问题，并增加构建产物上传。
- 更新 README 模组介绍。
- 版本号更新为 `1.0.1-rc1`。

## 1.0-rc1

### 卡牌选择界面重写

- 使用了新卡牌容器贴图。
- 右侧显示攻击/防御 `<下限>-<上限>` 范围，并实时随卡牌增删更新。
- 下方新增卡牌选择器：攻击左列、防御右列，按物品栏顺序排列，支持滚动。
- 支持点击下方存放区域/右键将手持卡牌放回物品栏。
- 暗影突袭、名刀、蓄力、全力攻击图标缩放并向右下对齐。

### 耐久度机制

- 战斗牌改用 MC 原生耐久数据（damage/maxDamage），可被 Durability Tooltip 模组识别。
- 移除战斗牌自定义“剩余次数”Tooltip 文本。
- 防御牌现在会在受到伤害时正确消耗耐久。

### 配方调整

- 忍者立牌：铁锭 → 回响碎片。
- 占星师立牌：青金石块 → 海晶砂砾。
- 魔法秘典：中间书与笔 → 回响碎片。
- 魔法箭袋：中间光灵箭 → 回响碎片。
- 忍术飞镖：磁石 → 红石块。

### 效果牌提示

- 出牌数用完后显示 ActionBar 提示，冷却按所有效果牌中最长剩余时间计算。

### 工程配置

- 版本号更新为 `1.0-rc1`。

## 1.0-SNAPSHOT.23

### 物品 ID 与 Tag

- 效果牌统一为 `effect_card_*`。
- 筹码统一为 `*_chip`。
- 活体书页改为 `effect_card_living_page`。
- 新增物品 Tag：`dices`、`combat_cards`、`effect_cards`、`is_exclusive`、`signs`、`chips`、`materials`。

### 配方

- 基础骰子改为红石 + 石英块。
- 黄金/钻石/下界合金骰子统一为 `dice_upgrade` 有序升级；移除下界合金骰子锻造配方。
- 调整大量卡牌、效果牌、立牌、筹码配方。
- 筹码通用升级模板：
  - 蓝→紫：`LGL/GTG/PPP`
  - 紫→金：`RDR/DTD/GGG`
- 定向爆破改为有序对称配方。

### 战利品

- 星盘可从所有原版宝箱开出。
- 黄金星盘可从 `minecraft:chests/trial_chambers/reward_ominous` 开出。

### 创造栏

- 材料移至最前端。
- 调整效果牌与活体书页顺序。

### 数值与 Tooltip

- 拳击手套攻击：+2/+4/+8。
- 速度轮滑移速：+5%/+15%/+25%。
- 摩托头盔防御：+2/+4/+6；高级额外盔甲韧性 +2。
- 夹心饼干生命：+4/+8/+12。
- 缓冲盾牌冷却改为 30 秒。
- 手电筒：每 4 点星光 +1 攻击力。
- 效果牌 Tooltip 新增出牌数与当前周期伤害加成提示。
