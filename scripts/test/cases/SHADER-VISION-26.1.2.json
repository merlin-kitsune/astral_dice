{
  "case_id": "SHADER-VISION-26.1.2",
  "title": "26.1.2 光影开启验证(视觉识别 + 游戏内手动开启 + 崩溃判定)",
  "version": "26.1.2",
  "target": {
    "type": "existing-feature",
    "ref": "mc-26.1.2-shader-enable"
  },
  "steps": [
    {
      "op": "note",
      "text": "【目的(2026-09-17 用户要求)】用**视觉识别**确认游戏内光影是否已开启；若未开启，则**在游戏内操作开启**，然后检查游戏是否仍然崩溃。\n\n【前置】本用例要求客户端以 `config/iris.properties` 的 `enableShaders=false` **冷启动**。**驱动方式(2026-09-17 新增专用子命令，不联网、幂等)**：先 `pwsh -File scripts/test/mt_env.ps1 shaders --version 26.1.2 --state off`（可选 `--state status` 只读回显），再 `mt.ps1 --phase launch`。为什么必须有这一步：**游戏内的光影开关会持久化该键** —— 本用例按 K 之后（以及光影界面点 Apply 之后）`enableShaders` 就留在 `true`，下一次冷启动会在**进入世界的第一帧**崩掉（launch 阶段报 `MT_LAUNCH: ERROR — 进入世界后立即崩溃`），后续用例全部跑不起来；跑完本用例请再执行一次 `--state off`。步骤 2/3 的视觉读数则是对这个前置的**显式确认**；若视觉判定为「已有光影」，说明前置不成立，此时按 K 只会把光影**关掉**，步骤 6 的 `Using shaderpack:` 断言会以「未进入建管线」失败——不会给出假绿。\n\n【「在游戏内开启」怎么操作】用注入按键 **K** = Iris 的 `iris.keybind.toggleShaders`，与光影界面里点 Apply 走的是**同一入口** `IrisApiV0ConfigImpl.setShadersEnabledAndApply → Iris.reload → createPipeline`（本机崩溃堆栈里两者都能看到这一帧）。键位取自安装 jar 的字节码（`Iris#onEarlyInitialize`：`toggleShaders`=GLFW 75='k'、`shaderPackSelection`=79='o'、`reload`=82='r'），已加入 `mt_inject.ps1` 的键表（语义键 `shadertoggle`/`shaderscreen`/`shaderreload`）。\n\n【为什么这样判】崩点不在建管线而在**启用后的第一帧 draw**：原版 `GlCommandEncoder#trySetup`(26.1.2 源码 526~532 行)要求 `renderPass.pipeline.program()` 里每个 `Uniform.Sampler` 都能在本次 draw 的 `renderPass.samplers` 里找到同名绑定，找不到即 `IllegalStateException: Missing sampler Sampler1`。故判据 = ① `Using shaderpack:` 出现(开启动作真的走到了建管线) ② `Missing sampler Sampler1` / `Unreported exception thrown` 不出现 ③ 无新增崩溃报告；再补一张崩溃后的抓拍做视觉留痕(游戏已死时 `mt_capture` 会退化为抓桌面，不会报错)。\n\n【预期 = 绿（2026-09-17 二轮实测）】当前测试环境是 **Sodium 0.9.1（与整合包一致）+ Iris 1.11.4**，本用例 **11/11 PASS**：光影可正常开启并渲染（视觉读数：原版方块云 → 光影体积云/大气散射/色调映射，游戏存活、聊天栏有 Iris 的「光影包已切换…」）。**本用例因此是渲染栈版本的回归守卫**：一旦把 Sodium 升回 0.9.2（或换 Iris/光影包版本），它会立刻转红——这正是它存在的意义，不要为了让套件全绿而放宽它的断言。\n\n【注意】本用例会让客户端**崩溃退出**(这正是被验证的行为)，故必须单独跑；`expect_crash: true` 声明了这一点 —— 否则 `mt_case` 的收尾校验会把「用例期间新增崩溃报告」强行改判为 `ERROR`（= 伪装成工具链故障）并尝试自动重启去重跑（2026-09-17 实测；该键为本用例新增）。抓图口径：两张都带 `crop: true`（按窗口矩形裁剪）—— 窗口位置由系统决定（2026-09-17 起已**全局移除**「把游戏窗口搬到第二显示器」的规则），而 `mt_capture` 不带 crop 时抓的是**主显示器**整屏，抓到的只会是桌面（2026-09-17 实测：SH_ON 抓到 DSH 窗口而非游戏，一度被误读成「游戏已消失」）；带 crop 时游戏活着就给游戏画面，游戏已死则退化为整屏并只 WARN，两种情形都成立。跑完用 `mt.ps1 --phase stop --force` 释放。"
    },
    {
      "op": "screenshot",
      "tag": "SH_OFF",
      "mode": "window",
      "crop": true
    },
    {
      "op": "assert",
      "type": "vision",
      "image": "SH_OFF",
      "question": "这是客户端进入测试世界后的窗口抓拍(启用光影之前)。请判定画面是否为**无光影的原版观感**(无方向性阴影、无水面/屏幕空间反射、无体积光、天空与雾效为原版)。回答『无光影』或『已有光影』，并用一句话给出可见依据(例如阴影/反射/色调特征)。"
    },
    {
      "op": "inject_key",
      "key": "k"
    },
    {
      "op": "wait",
      "ms": 5000
    },
    {
      "op": "assert",
      "type": "log",
      "source": "latest",
      "pattern": "Using shaderpack:.*ComplementaryUnbound"
    },
    {
      "op": "screenshot",
      "tag": "SH_ON",
      "mode": "window",
      "crop": true
    },
    {
      "op": "assert",
      "type": "vision",
      "image": "SH_ON",
      "question": "这是按下光影开关(K)约 5 秒后的窗口抓拍。请判定：(a) 画面里是否还有正在运行的 Minecraft 游戏画面？(b) 若有，是否已呈现光影包效果(明显阴影/反射/体积光/色调映射)？(c) 若已没有游戏画面，抓到的是什么(桌面/其它窗口/崩溃提示)？"
    },
    {
      "op": "assert",
      "type": "absent",
      "source": "latest",
      "pattern": "Missing sampler Sampler1|Unreported exception thrown"
    },
    {
      "op": "assert",
      "type": "crash"
    },
    {
      "op": "assert",
      "type": "kubejs"
    }
  ],
  "on_fail": "keep_game_running",
  "expect_crash": true
}
